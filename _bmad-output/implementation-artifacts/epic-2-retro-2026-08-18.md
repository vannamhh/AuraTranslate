# Retrospective — Epic 2: Biên tập theo segment (một vòng dịch tay hoàn chỉnh)

**Ngày:** 2026-08-18 · **Chủ trì:** Amelia (Developer) · **Chủ dự án:** Ice
**Chế độ:** retro **MỘT PHẦN** — 12/14 story `done`; `2-3` và `2-4` còn `in-progress`.
Ice chốt: hai story đó là **ĐỐI TƯỢNG** của phiên này, không phải phần thiếu của nó.
**Baseline:** `67bb147` *(master, cây sạch)*

---

## 1. Lượt rà này đo bằng gì

Không lấy một bảng số nào trong story file làm đúng sẵn. Mọi con số dưới đây chạy lại
trên cây làm việc hôm nay:

| Đường đo | Lệnh | Kết quả |
|---|---|---|
| Chín cổng đọc-tệp | `check:deps` `check:tokens` `check:i18n` `check:commands` `check:layout` `check:dict` `check:dict-manifest` `check:lint` `check:gates` | **9/9 exit 0** |
| Frontend | `npm test` *(vitest 4.1.10)* | **249/249** · 21 tệp · 4,02 s |
| Rust | `cargo test --locked` | **409 passed · 0 failed** |
| Cây nguồn | `find` | 16 `.vue` · 39 `.ts` · 45 `.rs` sản phẩm · 15 `tests/*.rs` · 21 tệp test frontend · 11 spec e2e · **227** khoá `vi.json` |

**Khớp đúng các số ghi trong story `2-11`.** Không phát hiện một khai sai nào trong bảng
số của story file — kết quả tốt thứ hai liên tiếp *(Epic 1 cũng vậy)*.

**Chưa chạy trong lượt rà này, ghi thẳng:** bộ e2e trọn bộ *(~15 phút, cần máy rảnh)* ·
`check:scope` + `check:scope:bundled` *(cần cổng 1420 trống)*.

---

## 2. Epic 2 giao được gì

**14 story** *(11 số chính + `2.5b` · `2.5c` · `2.5d`)* · **12 done** · **2 `in-progress`**
*(`2-3` · `2-4`)*. Story `2-12-sync-scrolling` bị **XOÁ** 2026-08-14 khi Ice ký rút FR20.

**So với mốc cuối Epic 1** *(264 ca Rust · 0 ca frontend · 15 `.vue` · 31 `.ts` · 40 `.rs` ·
154 khoá)*:

- **+145 ca Rust**
- **Toàn bộ cây test frontend dựng từ số 0**: 21 tệp / **249 ca**. NFR15 *"không bộ chạy
  test frontend"* được **LẬT** ở Story 2.3, đi **qua** cửa rà giấy phép chứ không xoá nó.
- **Bảy bước di trú tiêu**: 5 *(2.1)* · 6 *(2.2)* · 7 *(2.5)* · 8 *(2.5c)* · 9 *(2.5d)* ·
  10 *(2.6)* · 11 *(2.7)*. `2.8`/`2.9`/`2.10`/`2.11` tiêu **0** bước. Số kế tiếp: **12**.
- **Hai `AD` mới ra đời từ chính lượt thi công**: `AD-46` *(2.5d)* · `AD-47` *(2.7)*.
  `AD-48` *(mô hình hoàn tác)* đã soạn hồ sơ bàn giao nhưng **chưa được viết** — nó đang
  chặn AC5 của Story 2.9.
- **Một lượt correct-course giữa epic** *(2026-08-14, Ice duyệt trọn gói)*: bề mặt nhập lật
  sang **lưới hai cột đối chiếu**. Story 2.2 mất **4/8 AC**; ba story chèn `2.5b`/`2.5c`/`2.5d`.

**Sản phẩm thật:** Ice dịch được trọn một vòng bằng tay trên máy mình — gõ tiếng Việt bằng
bộ gõ, xác nhận, gộp/tách, cắt bỏ câu, ngắt đoạn, xem và khôi phục lịch sử, điều hướng,
chuyển Chương.

---

## 3. Việc chạy tốt — và vì sao đáng giữ

### 3.1 Cửa chữ ký Task 0, và số lần **phép đo LẬT một chữ ký**

Hơn bảy mươi quyết định mở được Ice ký trước dòng mã đầu tiên. Thứ đáng giữ **không phải**
con số đó, mà là số lần một chữ ký đã ký **bị chính bản đồ lật**:

| Story | Ký | Lật thành | Số lật nó |
|---|---|---|---|
| 2.5d | #1 (c) | **(d)** | `A<br>` hiện **1 dòng** *(phải `A<br><br>` mới ra 2)* ở đúng ca thường nhất; dưới `pre-line`, `insertLineBreak` của chính WebKit dựng một **text node `\n`** |
| 2.8 | #2 (a) | **(e)** — một đường **không có trong bảng bốn đường của story**, dev đề xuất từ số đo | Ba vòng bản đồ: **không cử chỉ chuột nào** tạo được vùng chọn ở cột nguyên văn *(bấm đơn "None"/0 · kéo 6 bước "None"/0 · kéo sau khi tài liệu có tiêu điểm "None"/0)* ⇒ **tiền đề KHÔNG TỒN TẠI** |
| 2.8 | #6 (a) → (b) | **LỌC** *(lật lần ba)* | Một lượt **Ice dùng thật**: *"đã tách ra 2 câu, nhưng câu cũ vẫn tồn tại và số thứ tự vẫn chiếm"* |

🔴 **Điều đáng ghi nhất của lần lật thứ ba không phải quyết định, mà là:** cái giá của
`#6(b)` **đã được viết ra bằng chữ TRƯỚC KHI KÝ** — bảng đường của nó có đúng câu *"lưới
phình theo số lần sửa, VĨNH VIỄN"* — và vẫn không đủ để thấy. Ba lý lẽ đứng sau `#6(b)`
hôm nay **vẫn đúng**; cả ba cộng lại **thua một lượt người thật nhìn vào một Chương thật**.

### 3.2 Cửa `AD` (Task 0.4) kích hoạt THẬT — hai lần, và dev không tự soạn `AD`

- **Story 2.7:** chữ ký `#8(b)` đòi thêm luật vào `AD-31` và `AD-5`. Story **DỪNG LẠI** với
  **0 dòng mã sản phẩm bị chạm** *(`git diff --stat` trên `src/ src-tauri/ scripts/ tests/
  e2e/` = RỖNG)*, soạn hồ sơ bàn giao, Winston giao **`AD-47`**. Năm điều kiện nghiệm thu
  đo lại từ nguồn, `lint_spine.py` 0 finding, `grep -c "^### AD-"` = 47.
- **Story 2.9:** lặp đúng khuôn cho `AD-48`. Ice phân định cửa chỉ chặn **AC5**, không chặn
  cả story — khác 2.7, nơi nó dừng trọn.

Đây là luật duy nhất trong kho **tự chứng minh nó dừng được một story đang chạy**.

### 3.3 Rà mã ba tầng bắt thứ mọi cổng bỏ lọt

Nặng nhất, và nó là một khuyết tật của **mã ĐÃ PHÁT HÀNH**:

> `document.caretPositionFromPoint` **KHÔNG TỒN TẠI** trên WKWebView *(`typeof` =
> `"undefined"`)*, và một lời gọi trần **NÉM `TypeError`** — nó không trả `null`.
> `GridPanel.vue::placeCaretAtPoint` *(Story **2.5b**, đang chạy trong bản phát hành)* gọi
> đúng API đó, trần, ở **dòng đầu** ⇒ `onCellMouseUp` chết ngay tại đó ở **mọi** cú bấm
> vào ô bản dịch, và `ensureCaretNextFrame` — thứ doc-comment của chính nó gọi là *"đường
> DUY NHẤT chạy được khi engine không làm"* — **CHƯA BAO GIỜ CHẠY**.

⚠️ **Vì sao không ai thấy suốt hai story:** caret **vẫn hiện** *(`selectionType "Caret"`,
`rangeCount 1`, `activeElement` = chính ô)* — nó đến từ `cell.focus()` cộng hành vi mặc
định của engine, **không** từ đường mã mà ba vòng chẩn đoán của 2.3 và 2.5b đã mua.
`grid-empty-cell.e2e.mjs` **xanh trên một sản phẩm mà nửa cơ chế của nó đang chết**.

### 3.4 Đột biến MÃ SẢN PHẨM thành phương pháp nghiệm thu chuẩn

Không phải đột biến từng test — **đột biến mã sản phẩm**, rồi hỏi *"ca nào đỏ?"*:

| Lượt | Gỡ gì | Kết quả |
|---|---|---|
| 2.9 | chốt `cell.contains(...)` | **10/10 VẪN XANH** — ca ⑨ sống sót **nhờ tình cờ** *(Range xuôi nên `toString()` ra đúng chuỗi, chốt không tham gia)* |
| 2.9 review | `data-src-atomic` khỏi `.hv-text` | **191/191 VẪN XANH** — `editorSourceCut.test.ts` dựng DOM bằng tay rồi tự gắn neo, nên nó canh `sourceCutOffsetOf` **tôn trọng** neo mà không canh component **PHÁT RA** neo |
| 2.6 | ba nhánh phân xử | bốn phép tự kiểm đều đỏ đúng ca của nó |
| 2.11 | bỏ `await` trước lượt flush | ca ban đầu **vẫn xanh** *(chỉ đo mốc `save:` bắt đầu)* ⇒ thêm mốc `save-done:`, đột biến chạy lại thì ca **ĐỎ** |

### 3.5 `deferred-work.md` vẫn hoạt động như một sổ nợ

22 mục đóng bằng cách **nối tiếp** `→ ✅ ĐÃ ĐÓNG`, không mục nào bị xoá. Đọc lại được vì
sao một quyết định cũ được ra — và **hai** lần trong Epic 2, một tiền đề trong chính sổ nợ
bị **ĐÍNH CHÍNH** thay vì đóng *(2.7 về "cột xuất xứ trả lời được theo id"; 2.11 về chỗ đặt
trước `⌥←/⌥→`)*.

### 3.6 Đo lại từ NGUỒN thành thói quen — action item **A6** của retro Epic 1 trả lãi

A6 chỉ đòi *"ghi vết sẹo `PROJECT_MIGRATIONS` số 4 vào Story 2.1"*. Thứ thực sự ra đời là
một **thói quen**: mọi story sau đều mở bằng *"đọc `PROJECT_MIGRATIONS` (schema.rs), đừng
đọc một ghi chép ở nơi khác — kể cả dòng này"*, và bảy bước di trú đi qua **không một lần
sai số**. Đây là action item hiệu quả nhất trong lịch sử dự án.

---

## 4. Bảy phát hiện — và cả bảy là HỆ THỐNG, không phải lỗi của một story

### 🔴 F1 — "Chữ ký thi hành ĐÚNG MỘT NỬA", lặp **năm** lần

Khuôn giống hệt mỗi lần: nửa **khó, có chú thích 🔵 đẹp** thì làm; nửa là **một dòng chuỗi**
hoặc **một câu phải xoá** thì rơi. **Không cổng nào canh nửa đó.**

| Story | Nửa rơi | Hậu quả đo được |
|---|---|---|
| 2.5b | chữ ký `#5(a)` · `#8` · `#9(a)` — cả ba | **Nhãn bố cục NÓI DỐI**: người dùng bấm *"Bố cục bốn cột"* — một bố cục **ĐÃ RÚT** — để nhận B-1 |
| 2.5c | `DESIGN.md:145`/`:146` vẫn khai `ornament` | Đặc tả **tự mâu thuẫn** với chính `DESIGN.md:213` |
| 2.6 | khuôn *"hai lượt flush"* khai trong doc-comment, mã chạy **một** lượt | Bảo đảm của chữ ký `#2(a)` mất **trong im lặng** |
| 2.7 | cổng AC8 mù với **ba** cột *(`.10` `.11` `.12`)* | Lặp **trên chính cái lưới dựng để chống lớp lỗi đó** |
| 2.9 review | `confirmNotice` đè `regroupNotice` · `resetEditorPanel()` sót 2 ô | AC4 **không đạt trong một ca thật**; chú thích tại chỗ tự khai *"hai ô nhớ không bao giờ cùng mang giá trị"* — **SAI** |

**Quan sát rộng hơn, và nó là món thật:** luật *"mọi ô nhớ mới phải qua `resetEditorPanel()`"*
**không có cổng nào canh**, và đã bị bỏ sót **hai story liên tiếp** *(`sourceCut` ở 2.8,
`omitError` ở 2.9)*.

### 🔴 F2 — vitest KHÔNG bắt được lớp lỗi đắt nhất, đã chứng minh **bốn** lần

| Story | Số xanh trong lúc sản phẩm đang hỏng |
|---|---|
| 2.5 | **74/74 vitest xanh** trong khi `read_open_chapter_segments` không gửi `status` ⇒ `isConfirmed` **LUÔN false** trong app thật |
| 2.6 | **130/130 vitest xanh**; hai ca e2e là đường **DUY NHẤT** đo được bốn trường của `SegmentVersionRow` trên dây |
| 2.7 | **382 Rust + 133 vitest ĐỀU XANH** trong khi hình dạng dây gãy *(`invalid args: missing required key textAtLoad`)* |
| 2.9 review | **191/191 xanh** sau khi gỡ `data-src-atomic` |

**Nguyên nhân chung, đã viết ra bằng chữ:** *fixture chép tay LUÔN có sẵn trường.* vitest
kiểm cái fixture, không kiểm cái **dây**. Khuôn *"cột mới phải đi qua đây"* đã lặp **bốn**
lần *(`status` · `is_omitted` · `is_target_paragraph_end` · `translation_origin`)*.

🔵 **Một mệnh đề đang giữ, đáng ghi:** Tauri **từ chối một tham số THIẾU một cách ỒN ÀO**.
Nếu nó im lặng cấp `""` thì lượt 2.7 đã **XANH** trong khi mọi câu duyệt-nguyên-văn bị gắn
nhãn `"tôi dịch"`.

### 🔴 F3 — Bộ e2e là lưới DUY NHẤT cho hình dạng dây, **VÀ** nó là một bộ đo nói dối

*"Xanh riêng, đỏ trong bộ"* xuất hiện ở **2.6 · 2.8 · 2.9 · 2.10 · 2.11**. Nguyên nhân đã
đo được **một phần**:

- `wdio.conf.mjs::devServerIsUp()` **tin một Vite hấp hối** vì nó vẫn trả 200 cho `/`;
- fixture **không reset state panel** giữa các spec;
- khuôn `waitForExist(...)` rồi đọc ngay **không phân biệt** *"Chương mới đã nạp"* với
  *"Chương CŨ còn nằm đó"* — bắt được ở 2.9 bằng `Expected: 3, Received: 2`;
- `FLUSH_WAIT_MS = 3500 ms` so với idle `AD-35` = 2000 ms ⇒ **biên 1500 ms**, và trong biên
  đó phải lọt: timer idle + một lượt `invoke` + `Store::write` **nối tiếp** của `AD-11` +
  một lượt `fsync` WAL. **Một máy đang biên dịch Rust ăn hết biên đó.**
  *(Phân xử bằng năm lượt trọn bộ ở 2.7: máy bận 7/8 ba lần liên tiếp; máy rảnh **8/8** trên
  cả cây story lẫn baseline.)*

🔴 **Chủ của cả cụm này là *"một story hạ tầng e2e"* — và story đó KHÔNG TỒN TẠI trong bất
kỳ epic nào từ 3 tới 10.** Đo: `grep "^### Story .*(e2e|hạ tầng|cổng)"` trên `epics.md` chỉ
trả về Story 1.22 *(đã có, `in-progress`)* và Story 8.8 *(cổng `.docx`)*. Cụm này sở hữu
**11+** món nợ và **không có ngày**.

### 🔴 F4 — "Một con số THẬT, trả lời SAI câu hỏi" — bộ đo tự nó là nguồn lỗi

Lớp lỗi này được **đặt tên** ở `2-5d-ban-do`, rồi tái diễn **hai** lần nữa:

- **2.8:** listener chẩn đoán **của chính bản đồ** gọi `caretPositionFromPoint` trần ⇒ nó
  **NÉM trước dòng `push`** ⇒ mảng rỗng ở mọi vòng ⇒ dev đọc con số rỗng ấy thành *"không
  một `mouseup` nào tới `document`"*. Một mệnh đề về **ENGINE** rút ra từ một cú ném của
  **BẢN ĐỒ**.
- **2.9:** bản đồ **CHÉP** hàm sản phẩm ⇒ sau khi vá AC9, nó vẫn cho **17** và **19** y hệt
  lượt trước, trong khi DOM đã mang neo. Nó **báo "chưa vá" trên một sản phẩm ĐÃ VÁ**.

**Hai luật rút ra, viết thành câu:**
1. *Một listener chẩn đoán phải **CHỊU ĐƯỢC** engine mà nó đang đo.*
2. *Một bản đồ **CHÉP** hàm sản phẩm sẽ đo **bản chép**, và bản chép cứ đi.*

⚠️ Cộng một giới hạn thứ ba đã đo: `browser.keys(['Meta','/'])` giao `code: "/"`, không
`"Slash"` ⇒ hợp âm không khớp, **0 command chạy**; đối chứng `⌘M` giao `code: "KeyM"` ⇒
khớp. Bộ đo **không phân biệt được** *"driver gửi sai `code`"* với *"engine báo vây"*, theo
cấu tạo.

### 🔴 F5 — `in-progress` lại bị dùng làm chỗ đậu — **lặp nguyên văn từ retro Epic 1 §8.2**

Và nặng hơn: hai story đậu ở đó **chính là hai story chở bằng chứng nặng nhất của cả epic**.

**`2-3` — tiền đề chặn ĐÃ HẾT ĐÚNG, đo 2026-08-18:**

| Mệnh đề giữ nó ở `in-progress` | Đo hôm nay |
|---|---|
| *"lượt gõ ĐẦU TIÊN vào một câu CHƯA DỊCH — `<span>` rỗng, 0 px, không text node neo caret ⇒ `execCommand('insertText')` trả `false`"* | 🔵 **HẾT ĐÚNG.** `execCommand` không còn trên đường đó *(`GridPanel.vue:987`: "insertText, các lượt xoá ⇒ để engine làm")*. `e2e/specs/grid-empty-cell.e2e.mjs` khẳng định **đúng ca đó** trên WKWebView thật: bấm chuột thật vào ô rỗng ⇒ `selectionType 'Caret'` · `rangeCount 1` · `neoTrongO true`; gõ ⇒ `inserted.ok === true`, `beforeinput/insertText`, chữ hạ cánh |
| *"Thanh trạng thái CHƯA TỒN TẠI (AC7 buộc dùng nó)"* | 🔵 **HẾT ĐÚNG.** `src/StatusBar.vue`, 339 dòng |
| *"đường flush lúc THOÁT app chưa có hook nào chạy trước `RunEvent::Exit`"* | 🔵 **HẾT ĐÚNG.** `lib.rs:670` — *"Nối `WindowEvent::CloseRequested` tới một lượt flush ở webview — **Story 2.3, AD-35 vế (e)**"* |

🔴 **KHÔNG tự chấm `2-3` là `done`** — luật kho: *không đánh dấu đạt bằng suy luận*. Cái đo
được là **tiền đề chặn đã hết đúng**, không phải *"17 AC đều đạt"*. Nó cần một lượt đóng có
chủ. Và nó vẫn mang một món nợ **có chủ**: `EDITOR_IDLE_MS`/`EDITOR_HARD_CAP_MS` tự khai
*"⚠️ TẠM — chủ là Story 2.4"*.

**Bài học thật:** đây là khuôn *"kiểm điều kiện đo trước khi lật quyết định"* chạy **ngược**
— một quyết định **TREO** hết lý do treo từ 2026-08-15 *(lượt 2.5b)* mà không ai đo lại
suốt sáu story kế tiếp.

**`2-4`** treo ở chỗ khác hẳn: chưa tiêm được `bench.js` vào webview bản release *(bốn lượt
build hỏng, bốn giả thuyết bị bác)*. Tức **BỘ ĐO chưa đứng nổi** — mọi số của AC1/AC2/AC12/
AC13 đều đọc ra từ cái bộ đo đó.

### 🔴 F6 — NFR2 là một vi phạm ĐANG SỐNG, đã đo, và trên thực tế **VÔ CHỦ**

- **2.5b đo:** đổi con trỏ trên 9.850 câu = **706–770 ms**. Trần NFR2 = **50 ms** ⇒ **vượt
  ~15 lần**.
- Mốc hiệu năng cũ *(`deferred-work.md:2113-2129`)* **MẤT HIỆU LỰC THEO CẤU TRÚC** — mọi số
  cũ đo trên mô hình *"N `<span>` trong một dòng văn liên tục"*, mô hình đó không còn.
- **2.10** thêm hai lệnh và **2.11** thêm một lượt chuyển, **cả hai đi qua đúng đường nóng
  đó**, và cả hai ghi *"chủ vẫn là Story 2.4"*.

**Nhưng 2.4 không chạy được.** ⇒ Con số lớn dần trong khi người chủ của nó bị đóng băng.
Đó không phải giao nợ, đó là **xếp nợ vào một cái hộp khoá**.

### 🔴 F7 — Lời tiên đoán §10.4 của retro Epic 1 đúng, và **nặng hơn dự đoán**

Retro Epic 1 viết: *"Nợ thị giác của Epic 1 không chặn Epic 2 về mặt kỹ thuật, nhưng nó sẽ
NHÂN LÊN: Panel Editor của 2.2 là bề mặt thị giác lớn nhất dự án tới nay."*

Thực tế: 2.2 **không chỉ mắc nợ, nó bị thay thế toàn bộ giữa epic** *(correct-course
2026-08-14)* — **4/8 AC chết**. Câu chẩn đoán trong `epics.md` rất trung thực và đáng giữ:

> *"Không có lỗi cài đặt nào ở gốc: **ba quyết định ĐÚNG chồng lên nhau thành một bề mặt
> SAI**. Nguyên nhân gốc nằm ở HÌNH DẠNG — một dòng văn liên tục là hình dạng để **ĐỌC**
> một bản dịch đã xong; suốt thời gian dịch nó gần như rỗng, và nó không có cách nào diễn
> đạt 'ở đây sẽ có một câu, nhưng chưa' mà không hoặc **chiếm chỗ** (bố cục nhảy) hoặc
> **tàng hình** (không bấm được). Hai vế đó đã được đo và chúng XUNG KHẮC."*

### 🔴 F8 — Đường nghiệm thu giá trị nhất của Epic 2 là **bàn tay Ice**, và nó không phải một quy trình

Mọi phát hiện muộn và đắt nhất đều đến từ Ice **dùng app thật**:

| Story | Ice tìm ra bằng cách dùng | Không đường tự động nào thấy vì |
|---|---|---|
| 2.5b | gõ tiếng Việt **bằng bộ gõ** — chữ hạ cánh, dấu không rơi | Mọi sự kiện driver giao đều `isTrusted: false`; một sự kiện không tin cậy **không có default action** |
| 2.8 | *"tách ra 2 câu nhưng câu cũ vẫn tồn tại và số thứ tự vẫn chiếm"* | Lật chữ ký `#6` lần **thứ ba**; không phép đọc nguồn nào thấy được |
| 2.9 AC7 | **double-click bắn HAI `mouseup`** ⇒ để lại **hai** dấu cắt | Hai cử chỉ của hai story khác nhau treo trên cùng một `mouseup` |
| 2.9 AC9 | *"chưa thấy điểm cắt, và chưa cắt được"* | Bản đồ cho **17/19** ký tự trên một câu **5 chữ** — dòng `Nguồn: thiều-chửu` (17 ký tự) nằm TRONG ô và bị phép đếm mù cộng vào. **Hôm nay chưa hỏng im lặng CHỈ VÌ MAY**: hai số tình cờ vượt biên nên Rust từ chối. Trên một câu Chương thật (40–60 chữ), `19` **nằm TRONG BIÊN** và `⌘/` cắt SAI CHỖ, **IM LẶNG**, trên dữ liệu mà `AD-5` không cho hoàn tác |
| 2.9 | dấu cắt quá mờ | `ornament` = 2,44/2,64 tương phản — mờ đến mức `check:tokens` **CẤM** nó làm màu chữ |

**Đây không phải điểm yếu — nó là điểm mạnh KHÔNG CÓ TÊN TRONG SỔ.** Không story nào sở
hữu nó, không lịch nào chở nó, và nó phụ thuộc vào việc Ice có rảnh để ngồi dịch hay không.

---

## 5. Theo dấu action item của retro Epic 1

| # | Việc | Trạng thái thật hôm nay |
|---|---|---|
| **A0** | AC5 của 1.7 nói *"chững lại"* | ✅ đã áp |
| **A1** | merge nhánh vá Windows | ✅ |
| **A2** | mở lại GitHub Actions | ✅ mở 2026-08-12, lượt xanh đầu tiên của cả kho *(`31568723620`)* · 🔴 **AC6/AC7 của Story 1.3 VẪN `open`** — bốn số đo nằm trong `GITHUB_STEP_SUMMARY`, chờ Ice đọc và ký |
| **A3** | `check:gates` buộc hai danh sách cổng khớp | ✅ — nay canh **ba** danh sách |
| **A4** | nghiệm thu tay 28 hàng bàn đo của 1.20 + 1.21 | ❌ **chưa động suốt cả Epic 2**. 1.20 và 1.21 vẫn `in-progress` |
| **A5** | **một bảng nghiệm thu Windows duy nhất** | ❌ **chưa tồn tại** — và retro Epic 1 §10.3 đã viết bằng chữ: *"Story 2.4 đo NFR18/NFR2 đồng thời, và NFR14 là mệnh đề hai nền tảng. Một bảng nghiệm thu Windows phải tồn tại **TRƯỚC** lúc đó, không sau."* Epic 2 chạy **macOS-only từ đầu tới cuối**: 145 ca Rust mới + 249 ca vitest + 11 spec e2e có **0** bằng chứng Windows |
| **A6** | vết sẹo `PROJECT_MIGRATIONS` số 4 vào Story 2.1 | ✅ — và nó **vượt xa** yêu cầu, xem §3.6 |
| **A7** | bộ chạy e2e | ✅ dựng · 🔴 nay chính là **F3** |
| *(mới)* | bộ e2e chập chờn *(ghi 2026-08-12)* | ⏳ **có tiến triển** — nguyên nhân đã đo được một phần, xem F3 |

**Đọc bảng này cho đúng:** 5 xong · 1 tiến triển · **2 không được đụng tới** — và cả hai
món không đụng *(A4, A5)* đều là **nợ nghiệm thu bằng mắt/bằng máy thật**, đúng lớp mà F8
vừa chứng minh là lớp giá trị nhất.

---

## 6. 🚨 PHÁT HIỆN LỚN cho Epic 3, và nó đã được XỬ trong phiên này

### 6.1 Tên panel chết trong tài liệu quy hoạch

**Đo:** `grep SourcePanel|EditorPanel` trên `src/` = **0 kết quả** *(cả hai tệp bị xoá ở
Story 2.5b)*. Nhưng `epics.md` còn nhắc tên cũ ở **29 chỗ**, trong đó **5 nằm trong Epic 3**
và 1 trong Epic 6; PRD có 10 chỗ. Sprint change proposal 2026-08-14 **có** xử FR121 ở Epic 8
— nhưng **không rà tên panel ở Epic 3 và Epic 6**.

🔴 **Đây KHÔNG phải "sửa spec cho khớp mã".** Luật `project-context.md:456-458` cấm đúng
thứ khác: *năng lực chưa dựng ≠ lệch spec*. Ở đây là một lượt **ĐỔI TÊN đã được Ice ký**
mà lượt rà **sót phạm vi** — không sửa thì Epic 3 dựng lên một cái tên không tồn tại.

**✅ ĐÃ SỬA trong phiên này** *(Ice ký 2026-08-18)*, tất cả kèm 🔵 + ngày + lý do:

| Tệp | Chỗ |
|---|---|
| `epics.md` | FR42 · FR50 *(kèm ràng buộc kỹ thuật, xem 6.2)* · hai hàng bảng FR→Epic · hai đoạn blurb Epic 3 · Story 3.3 AC · **tiêu đề Story 3.4** · Story 3.4 AC · Story 3.9 AC · Epic 6 AC · hàng bảng *"Thư viện editor"* |
| `prd.md` | FR42 · FR50 |

**Đã KIỂM sau khi sửa:** Epic 3 và Epic 6 trong `epics.md` nay **0 tên chết**. Còn lại là
**bản ghi lịch sử**, cố ý giữ: Epic 1 *(8 chỗ — Story 1.16 giao đúng cái panel đó, đã `done`)*
· Epic 2 *(4 chỗ — Story 2.2 đã có khối 🔵 supersede riêng)* · bảng giai đoạn/roadmap.

### 6.2 Và nó nặng hơn một lượt đổi tên — ràng buộc kỹ thuật đã ghi vào FR50

Story 3.4 đòi *"đánh dấu thuật ngữ ở cột nguyên văn"*. Cột đó **hôm nay** đã mang:

- ánh xạ ngược **bằng CHỈ SỐ** *(`host.children[i] ↔ segments[i]`)*, với doc-comment của
  template ghi thẳng: *"thêm/bớt/đổi thứ tự một phần tử ở đây là làm truy vấn tra cứu **SAI
  IM LẶNG**"*. Đây là lý do dấu cắt phải vẽ bằng `::before` chứ không bằng một phần tử con;
- neo `data-src-start` · `<rt>` ruby Hán Việt · dòng `.hv-sources` — **chính bề mặt** nơi
  lỗi đếm mù của 2.9 sống *(19 ký tự cho một câu 5 chữ)*;
- **ba** cử chỉ chuột treo trên cùng một `mouseup`: Auto-Lookup *(FR21, Story 1.18, đã phát
  hành)* · đánh dấu chỗ cắt *(`Mod`+click, 2.8/2.9)* · bấm trơn.

Epic 3 thêm cử chỉ **thứ tư** *(bôi đen → thêm vào Glossary)* và kênh trang trí **thứ hai**
lên đúng cái DOM đó — trong khi **không cử chỉ chuột nào của cột này có đường nghiệm thu
tự động** *(driver không tạo được vùng chọn ở cột `src`; Ice xác nhận **bằng tay** rằng
double-click **có** tra được ⇒ FR21 sống, và vế kia là giới hạn của **BỘ ĐO**)*.

---

## 7. Sẵn sàng cho Epic 3 — cửa chặn và điều kiện

**Epic 3 dựng lên trên:** `ScopeResolver` *(Epic 1)* · lưới hai cột *(2.5b)* · hợp đồng vùng
chọn theo **CỘT** *(`selectionContract.ts`, không sửa một dòng suốt 2.5b)* · đường Hán Việt
*(1.10c, 1.18b)* cho Story 3.7 · `project.db` bước di trú kế tiếp **12**.

### 🔴 Hai cửa chặn Ice đã ký 2026-08-18

**① Story 2.4 phải gỡ được TRƯỚC Epic 3.**
Lý do: NFR2 đang vượt trần **~15 lần** và Epic 3 trang trí thêm lên **đúng đường nóng đó**.
Một con số 15× mà không ai sở hữu sẽ thành 20× trong im lặng — đúng khuôn F6.

**② Một story hạ tầng e2e phải chạy TRƯỚC Epic 3.**
Lý do: theo F2, e2e là lưới **DUY NHẤT** cho hình dạng dây; theo F3, nó là một bộ đo nói
dối. Epic 3 thêm một cột dữ liệu mới *(Glossary)* và một bề mặt đánh dấu mới — chính lớp
lỗi mà chỉ e2e bắt được.
⚠️ **Số hiệu:** `2-12` từng được dùng cho `2-12-sync-scrolling` *(XOÁ 2026-08-14)*. Slug
khác nên khoá không đụng nhau, nhưng **ghi ra bằng chữ** để không ai đọc nhầm nó là story
sync-scrolling sống lại.

### Điều kiện mang theo, không chặn

3. **`2-3` cần một lượt đóng có chủ** — tiền đề chặn đã hết đúng *(§F5)*, cơ chế đã đủ.
4. **A5 của Epic 1** *(bảng nghiệm thu Windows)* đã **lỡ mốc** mà retro trước đặt ra. Khoảng
   mù dày thêm mỗi epic.
5. **`AD-48`** *(mô hình hoàn tác)* chưa được viết ⇒ AC5 của Story 2.9 còn nợ. Hồ sơ bàn giao
   đã có: `ad-brief-2026-08-17-mo-hinh-hoan-tac.md`.
6. **Sổ nợ đã quá tải:** ~**157** mục ghi trong khoảng Epic 2 của `deferred-work.md`, **22**
   đóng. ~**30** mục chủ là **Ice** đích danh; ~**11** mục chủ là một *"story hạ tầng"* nay
   mới có chỗ đứng nhờ cửa chặn ②. Sổ nợ vẫn **trung thực** — nhưng nó đã thành **một hàng
   đợi không có người phục vụ**.

---

## 8. Action items

| # | Việc | Chủ | Điều kiện xong |
|---|---|---|---|
| **B1** | 🔴 **CỬA CHẶN** — gỡ Story 2.4: dựng cho được bộ đo *(tiêm `bench.js` vào webview bản release)*, rồi đo NFR18 + NFR2 **đồng thời**, và ra một con số NFR2 sau khi vá | Ice | 2.4 lên `done` hoặc mang một con số NFR2 đã vá; Epic 3 **không mở** trước đó |
| **B2** | 🔴 **CỬA CHẶN** — dựng **Story 2.12: hạ tầng e2e và cổng còn thiếu**. Gom: `devServerIsUp` tin Vite hấp hối · fixture không reset state panel · khuôn `waitForExist` · `FLUSH_WAIT_MS` vs biên `AD-35` *(chờ một SỰ KIỆN, không chờ một khoảng thời gian — **không** nới hằng số)* · cổng đếm `ref` cấp module đối chiếu `resetEditorPanel` *(F1)* · cổng canh "cột mới phải qua AC8" *(F2)* | Ice → create-story | Story tồn tại, ready-for-dev, và chạy **trước** Epic 3 |
| **B3** | Lượt đóng có chủ cho Story `2-3` — tiền đề chặn đã hết đúng *(đo 2026-08-18, §F5)*; duyệt lại 17 AC, đừng chấm bằng suy luận | Ice | `2-3` lên `done` hoặc mang một mệnh đề chặn **MỚI** viết ra bằng chữ |
| **B4** | ✅ **XONG 2026-08-18** — sửa tên panel chết ở `epics.md` *(11 chỗ, Epic 3 + Epic 6 + FR + bảng)* và `prd.md` *(FR42, FR50)*, kèm 🔵 + ngày + lý do. FR50 nay chở luôn ràng buộc kỹ thuật của §6.2 | Dev | Epic 3 và Epic 6 trong `epics.md` = **0** tên chết *(đã kiểm)* |
| **B5** | Rà tồn dư tài liệu quy hoạch sau correct-course 2026-08-14 — `prd.md` còn mục `#### Panel Source` *(:401)*, bảng thuật ngữ *(:221)*, hai bảng giai đoạn; `epics.md` còn hàng `FR19 \| Epic 1`. Soạn như một lượt đồng bộ có phạm vi, không sửa lén | Winston | Một lượt rà có danh sách, có ngày; mỗi chỗ hoặc sửa kèm 🔵 hoặc khai bằng chữ là **bản ghi lịch sử** |
| **B6** | `AD-48` *(mô hình hoàn tác)* — Ice phân định hai đường (A)/(B) trong hồ sơ bàn giao, Winston soạn `AD`. Nó đang chặn **AC5 của Story 2.9** | Ice → Winston | `AD-48` có mặt trong `ARCHITECTURE-SPINE.md`, `lint_spine.py` sạch, AC5 mở lại |
| **B7** | **A5 của retro Epic 1 — lặp lần thứ hai.** Một bảng nghiệm thu Windows duy nhất. Nay đã lỡ mốc *("trước Story 2.4")*; Epic 2 khép lại với **0** bằng chứng Windows cho 145 ca Rust mới + 249 ca vitest + 11 spec e2e | Ice | Bảng tồn tại, có ngày; mọi nợ Windows của Epic 1 và Epic 2 trỏ vào nó |
| **B8** | **A4 của retro Epic 1 — chưa được đụng suốt Epic 2.** 28 hàng bàn đo của 1.20 + 1.21 | Ice | 1.20 và 1.21 lên `done`, trừ hàng cần máy Windows *(→ B7)* |
| **B9** | Đặt tên và ghi thành luật hai bài học của **F4** vào `project-context.md`: ① *một listener chẩn đoán phải CHỊU ĐƯỢC engine nó đang đo*; ② *một bản đồ CHÉP hàm sản phẩm sẽ đo bản chép, và bản chép cứ đi*. Cả hai đã tốn ba vòng chẩn đoán mỗi cái | Dev | Hai mục có mặt ở §Testing Rules, kèm `tệp:dòng` làm bằng chứng |
| **B10** | **F8 chưa có chủ và chưa có lịch** — mọi phát hiện đắt nhất của Epic 2 đến từ Ice dùng app thật. Quyết một hình dạng: một hàng nghiệm thu tay **bắt buộc** mỗi story chạm bề mặt, hay một lượt dùng thật có lịch mỗi cuối epic | Ice | Có một hình dạng viết ra; hôm nay nó là một thói quen may mắn, không một quy trình |

---

## 9. Ba bài học, gọn lại

1. 🔴 **Một chữ ký được thi hành ĐÚNG MỘT NỬA năm lần, và nửa rơi luôn là nửa RẺ.** Nửa khó
   có chú thích đẹp thì làm; một dòng chuỗi hoặc một câu phải xoá thì rơi. Không cổng nào
   canh nửa đó — đó là một **khoảng trống cưỡng chế**, không phải một chuỗi bất cẩn.

2. 🔴 **Bộ đo là một sản phẩm, và Epic 2 đã trả giá ba lần cho việc quên điều đó.** Một
   listener ném, một bản đồ chép hàm sản phẩm, một driver gửi sai `code` — cả ba cho **con
   số THẬT trả lời SAI câu hỏi**, và cả ba tốn nhiều vòng chẩn đoán hơn khuyết tật mà chúng
   đang đi tìm.

3. 🔴 **Thứ có giá trị nghiệm thu cao nhất trong Epic 2 không nằm trong bất kỳ cổng nào —
   nó là Ice ngồi dịch một Chương thật.** Nó lật ba chữ ký, cứu một lỗ dữ liệu im lặng
   *(`⌘/` cắt sai chỗ trên dữ liệu không hoàn tác được)*, và đóng một món nợ 🔴 to hơn cả
   story chứa nó. **Và nó không có chủ, không có lịch, không có tên trong sổ.**

---

## 10. Còn tồn đọng sau lượt này, ghi thẳng

Lượt rà này **không** đóng được, và không giả vờ đóng được:

- **Bộ e2e trọn bộ** *(~15 phút, cần máy rảnh)* và `check:scope` + `check:scope:bundled`
  *(cần cổng 1420 trống)* — chưa chạy trong phiên này. Chín cổng đọc-tệp, `vitest` và
  `cargo test` thì đã chạy và ghi số ở §1.
- **Mọi món nợ Windows-only** — không có máy Windows *(→ B7)*.
- **17 AC của Story `2-3`** — tôi chỉ đo được **tiền đề chặn**, không duyệt từng AC *(→ B3)*.
- **Con số NFR2 sau khi vá** — cần bộ đo của 2.4 đứng được *(→ B1)*.

Bốn nhóm trên là **giới hạn công cụ và giới hạn phạm vi**, không phải giới hạn hiểu biết:
mỗi món đều có chủ, có điều kiện xong đo được, và có đường đi ghi ở §8.
