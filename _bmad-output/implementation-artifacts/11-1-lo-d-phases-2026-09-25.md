# Lot D — phase handoff (working notes, not spec)

Phase 1 (Rust half of Decision 2) done. Scope: `src-tauri/tests/support/boundary_scan.rs` +
the 20 Rust `*_boundary.rs`/`config_invariants.rs` files. Did NOT touch `scripts/`, `src/`,
`deferred-work.md`, or the spec. No commit made — working tree has the 21 modified files
plus the untracked spec (pre-existing).

## Helper added

`src-tauri/tests/support/boundary_scan.rs`:

```rust
pub fn assert_population_floor(floor: usize, live: usize, label: &str, population: &str)
```

Two asserts: `live >= floor` ("Cây quá nhỏ để là thật" — truncated tree, same as before) and
`floor * 5 >= live * 4` (floor ≥ 80% of live — the new drift check). The drift panic message
names `label` and states `ceil(0.85 × live)` via `(live * 85).div_ceil(100)`, matching the
spec's I/O matrix row "Floor drifted". Every one of the 20 consumer files already had
`#[path = "support/boundary_scan.rs"] mod boundary_scan;` except `naming_boundary.rs`, which
I added it to (it keeps its own local `rust_root()`/`frontend_root()`/`walk` — only the two
asserts now route through the shared helper, per the spec's Code Map note).

Self-check lives in `src-tauri/tests/boundary_scan_contract.rs` (existing file that already
contract-tests every other `boundary_scan.rs` primitive — did not create a new test file,
in-scope per Code Map's "20 Rust test files"): 5 new tests — floor at exactly 80%/85% of
live both pass (not red wrongly), `live < floor` panics with "Cây quá nhỏ để là thật", floor
< 80% of live panics with "đã trôi dưới 80%", and one test asserts the panic message names
both the constant and the correct `ceil(0.85 × live)` value.

## Counter-check (real seam removal, not left in the tree)

Used `store_boundary.rs::RS_FLOOR` (live 98, raised to 84) as the probe:
1. Edited it to `78` (78/98 = 79.6%, just under 80%) → `cargo test --test store_boundary`
   went red on `the_scanned_tree_is_large_enough_to_be_real` with exactly
   `"sàn RS_FLOOR = 78 đã trôi dưới 80% số thật 98 ... nâng lên tối đa ceil(0.85 × live) = 84."`
2. Restored to `84` → green again (4/4 passed).
3. Edited it to `99` (above live 98) → red with
   `"chỉ tìm thấy 98 tệp .rs ... (sàn RS_FLOOR = 99). Cây quá nhỏ để là thật ..."`.
4. Restored to `84` → green again (4/4 passed, confirmed a second time).

Both failure branches of the helper were fired for their real, measured reason and cleanly
reverted — the constant sits at 84 in the working tree, same as every other pass.

## Per-constant before → after (all 27 Rust floors, live counts measured on HEAD 90d4428)

Roots and their live counts (re-measured via `find`/`git ls-tree`, matched the scratchpad
inventory exactly): `src-tauri/src/**` = 98 → new shared root value **84**;
`src-tauri/{src,tests}/**` = 161 → **137**; frontend `src/**` (.ts+.vue) = 110 → **94**;
`core/dict` = 5 → **5**; `core/matching` = 1 → **1**; `core/ai` = 4 → **4**; `core/aiconfig`
= 3 → **3**. All are `min(live, ceil(0.85 × live))`.

| file | constant | before | after | root |
|---|---|---|---|---|
| ai_boundary.rs | SRC_RS_FLOOR | 68 | 84 | src-tauri/src/** |
| ai_boundary.rs | AI_FLOOR | 1 | 4 | core/ai |
| aiconfig_keychain_boundary.rs | SRC_RS_FLOOR | 70 | 84 | src-tauri/src/** |
| aiconfig_keychain_boundary.rs | AICONFIG_FLOOR | 3 | 3 | core/aiconfig |
| cleanup_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| docx_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| config_invariants.rs | RS_FLOOR_FOR_DIALOG_CHECK | 65 | 84 | src-tauri/src/** |
| library_index_boundary.rs | RS_FLOOR | 44 | 84 | src-tauri/src/** |
| glossary_boundary.rs | RS_FLOOR | 44 | 84 | src-tauri/src/** |
| dict_boundary.rs | DICT_FLOOR | 4 | 5 | core/dict |
| dict_boundary.rs | SRC_TAURI_RS_FLOOR | 61 | 137 | src-tauri/{src,tests}/** |
| dict_boundary.rs | SRC_ONLY_RS_FLOOR | 43 | 84 | src-tauri/src/** |
| scope_boundary.rs | RS_FLOOR | 43 | 84 | src-tauri/src/** |
| matching_boundary.rs | MATCHING_FLOOR | 1 | 1 | core/matching |
| matching_boundary.rs | SRC_RS_FLOOR | 43 | 84 | src-tauri/src/** |
| meta_write_boundary.rs | RS_FLOOR | 46 | 84 | src-tauri/src/** |
| naming_boundary.rs | RUST_FLOOR | 44 | 84 | src-tauri/src/** (own root fn) |
| naming_boundary.rs | FRONTEND_FLOOR | 58 | 94 | frontend src/** (own root fn) |
| segment_encoding_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| segment_boundary.rs | SRC_RS_FLOOR | 43 | 84 | src-tauri/src/** |
| segment_boundary.rs | WEBVIEW_FLOOR | 56 | 94 | frontend src/** |
| store_boundary.rs | RS_FLOOR | 43 | 84 | src-tauri/src/** |
| segment_files_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| segment_normalize_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| segment_chapterpattern_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| segment_pipeline_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |
| webimport_boundary.rs | SRC_RS_FLOOR | 50 | 84 | src-tauri/src/** |

All 27 now route through `boundary_scan::assert_population_floor` (28 call-sites — the
dialog-check constant in `config_invariants.rs` is asserted at two call sites).

## Verified (live counts, this run)

Every one of the 21 touched cargo test targets (`boundary_scan_contract` + the 20 consumer
files) run individually: all green. `boundary_scan_contract`: 21 passed. The 20 consumer
targets: each still shows its pre-existing test count, all passed, 0 failed. Did not run the
full `cargo test` suite (not required for this phase; touches only `tests/**`, no
`Cargo.toml`/migrations/`lib.rs` registration change) — Phase 4 or whoever closes the story
should run the full suite once per the spec's Verification section, since the JS-side changes
(phases 2-3) also touch shared wiring territory (`scripts/lib/`, `check-gates.mjs`).

## Surprises / things phase 2 (JS) should know

- `naming_boundary.rs` did NOT get migrated to use `boundary_scan`'s `walk`/`rust_sources` —
  only the assert. Its own `rust_root()`/`frontend_root()` stayed. This matches the spec's
  constraint note ("A general checker keyed only off boundary_scan's helpers would miss it
  unless it also recognizes this file's own root functions, or the file gets migrated
  first") — migrating the walk itself was explicitly NOT asked for in Decision 2, so I left
  it.
- `check-i18n.mjs::RS_FLOOR`'s live count (99) is one more than the plain `src-tauri/src/**`
  count (98) per the scratchpad's flagged discrepancy — not a Rust-side floor, so out of
  this phase's scope, but the JS phase should re-verify it directly (not assume 98) when
  computing its own `ceil(0.85 × live)`.
- The bypass Kiểm (every `*_FLOOR` constant in `scripts/**/*.mjs` AND `src-tauri/tests/**/*.rs`
  reaches the helper) lives in `scripts/check-gates.mjs` per the Code Map — Rust side is now
  100% routed (grep `>= .*FLOOR,\|FLOOR >=` across `src-tauri/tests/*.rs` returns nothing),
  so the Rust half of that Kiểm should find zero violations once written.
- Several pre-existing multi-line historical/measurement comments attached to floor consts
  I edited were removed (the line I was changing carried them); a few untouched trailing
  blocks near `glossary_boundary.rs:69-82` still reference stale ratios (e.g. "44/53") — left
  alone per "no mass cleanup of existing comments" (AGENTS.md), since those are separate
  lines I did not otherwise touch.

## Phase 2 (JS half of Decision 2 + bypass Kiểm) — done

Scope: `scripts/lib/floor-judge.mjs` (new) + its vitest file + the 9 JS gates that own a
`*_FLOOR` constant + the bypass Kiểm (Kiểm G) in `scripts/check-gates.mjs`. Did NOT touch
`src/`, `src-tauri/`, `deferred-work.md`, the spec, or the non-floor Tasks-list items
(debt-owner predicate/Kiểm D, tokens suffix, i18n regex, inlineStyleBlocks, webview,
pre-push). No commit made.

### Helper added

`scripts/lib/floor-judge.mjs`, pure, no I/O:

```js
export function judgeFloor(floor, live, label, population)
  // -> {ok: true} | {ok: false, message: string}
```

Same two checks as `boundary_scan::assert_population_floor` (`live < floor` → "Cây quá nhỏ
để là thật"; `floor * 5 < live * 4` → "đã trôi dưới 80%", names `ceil(0.85 × live)` via
`Math.ceil((live * 85) / 100)`), same message wording (mirrors the Rust strings verbatim)
so both languages read the same when a gate goes red. Every call site passes the floor
constant as arg 1 verbatim (`judgeFloor(FOO_FLOOR, live, 'FOO_FLOOR', population)`) — that
convention is what the bypass Kiểm's regex leans on.

Vitest: `tests/frontend/floorJudge.test.ts`, 7 cases (not-red-wrongly at 80%/85%/100%; red
at `live < floor` and at 79% naming the constant; message names both the constant and the
correct `ceil(0.85 × live)`).

### Self-check + counter-check (floor-judge.mjs)

Removed the `floor * 5 < live * 4` branch entirely (real deletion, not commented out),
ran `npx vitest run tests/frontend/floorJudge.test.ts`: 2/7 red for exactly the drift
cases, message assertion failures (`expected true to be false`) — the removed branch was
the only thing producing `ok: false` for those two cases. Restored from a `cp` backup (not
`git checkout`), re-ran: 7/7 green again.

### Per-constant before → after (all 21 JS floors, live counts measured on HEAD with this
phase's own 2 new files already in the tree — `scripts/lib/floor-judge.mjs` and
`tests/frontend/floorJudge.test.ts` both fall under `check-doc-refs.mjs`'s scan roots, so
its own `FILE_FLOOR` live count already includes them)

| file | constant | before | live (this run) | after | note |
|---|---|---|---|---|---|
| check-doc-refs.mjs | FILE_FLOOR | 350 | 430 | 366 | live includes this phase's own 2 new files |
| check-deps.mjs | RUST_TREE_FLOOR | 200 | 360 | 306 | live = raw `cargo tree --no-dedupe` LINE count (pre-dedup), NOT the 337 deduped-name count the scratchpad inventory recorded — see Surprises |
| check-deps.mjs | NPM_TREE_FLOOR | 30 | 522 | 444 | matches scratchpad |
| check-debt-owner.mjs | SPRINT_KEY_FLOOR | 130 | 163 | 139 | |
| check-debt-owner.mjs | ITEM_FLOOR | 579 | 710 | 604 | |
| check-dict-build.mjs | RS_FILE_FLOOR | 24 | 24 | 21 | live was already 100% of floor; formula still applies (ceil(0.85×24)=21 < 24) — a deliberate *lowering* to restore margin, allowed since it lands exactly at ceil(0.85×live), never below it |
| check-panel-refs.mjs | FILE_FLOOR | 39 | 79 | 68 | |
| check-layout.mjs | FILE_FLOOR | 56 | 110 | 94 | |
| check-i18n.mjs | RS_FLOOR | 44 | 99 | 85 | live is 99, one more than the plain `src-tauri/src/**` count of 98 (own filter differs slightly) — confirmed by running the gate itself, not assumed |
| check-i18n.mjs | VUE_FLOOR | 16 | 31 | 27 | |
| check-commands.mjs | VUE_FLOOR | 16 | 31 | 27 | |
| check-commands.mjs | TS_FLOOR | 39 | 79 | 68 | |
| check-commands.mjs | COMMAND_FLOOR | 52 | 177 | 151 | |
| check-commands.mjs | CLICK_FLOOR | 27 | 130 | 111 | |
| check-commands.mjs | DISPATCH_FLOOR | 40 | 184 | 157 | |
| check-commands.mjs | SELECTION_SURFACE_FLOOR | 8 | 16 | 14 | |
| check-commands.mjs | HANDLER_ATTR_FLOOR | 75 | 91 | 78 | |
| check-tokens.mjs | FILE_FLOOR | 58 | 113 | 97 | |
| check-tokens.mjs | COMPONENT_FILE_FLOOR | 56 | 109 | 93 | |
| check-tokens.mjs | EMITTED_VAR_FLOOR | 100 | 161 | 137 | |
| check-tokens.mjs | DOCKVIEW_VAR_FLOOR | 90 | 110 | 94 | |

All 21 verified by actually running each gate (`node scripts/check-X.mjs`) both before and
after, reading the gate's own printed live count — not by trusting the scratchpad's
numbers blind (the `check-deps.mjs` RUST_TREE_FLOOR case shows why: the scratchpad's 337
was the wrong population for that specific floor).

### Bypass Kiểm — Kiểm G in `scripts/check-gates.mjs`

New seventh check (doc comment/header updated A–F → A–G). Scans `scripts/**/*.mjs` and
`src-tauri/tests/**/*.rs` for `const NAME = …` / `const NAME: usize = …` declarations,
keeps names matching `/_FLOORS?(_|$)/` (a `_` immediately before `FLOOR`/`FLOORS` — not a
bare "contains FLOOR" or "ends with FLOOR" test), skips two named exemptions
(`CONTRAST_FLOORS`, `LINE_HEIGHT_FLOOR` — thresholds, not population counts), then checks
the name is passed as arg 1 to `judgeFloor(`/`assert_population_floor(` somewhere in the
same file. Green run: **48** constants scanned (21 JS + 27 Rust), matching the spec's
"48 floor constants" total exactly.

Self-check cases added to the existing Kiểm C block (which already aggregates A/B/D/E/F
self-tests): 6 new cases exercising `reachesJsFloorHelper`/`reachesRustFloorHelper` (both
directions, both languages) plus the two extraction regexes + `isFloorConst` filter
directly (not a duplicate — same functions Kiểm G calls). Kiểm C's total went from 13 to
19 cases.

Counter-check (real seam removal): reverted `check-dict-build.mjs`'s `judgeFloor(...)`
call back to a hand-written `if (rsFiles.length < RS_FILE_FLOOR)` (from a `cp` backup,
restored the same way — never `git checkout`). `node scripts/check-gates.mjs` went red:
`` Kiểm G — `RS_FILE_FLOOR` (scripts/check-dict-build.mjs) KHÔNG đi qua judgeFloor ``.
Restored from backup, re-ran: green, 48/48 again. Also positive-probed with a throwaway
scratch file (`scripts/zz_scratch_floor_probe.mjs`, deleted immediately after) declaring
an unwired `SCRATCH_FLOOR` — caught and named correctly, confirming the "Floor bypass"
row of the spec's I/O matrix.

### Surprises

- **`check-deps.mjs::RUST_TREE_FLOOR`'s real population is NOT what the scratchpad
  inventory said.** The scratchpad's `lotd-floors.md` records live=337 ("crates in `cargo
  metadata` dep tree"), but the actual code path this floor guards is
  `rustCrates.length` — the RAW line count from `cargo tree --locked --prefix none
  --no-dedupe` (duplicated per dependency edge), computed and checked BEFORE the
  deduped-by-name `rustNames` set (337) is ever built. Running the gate confirmed
  live=360 at this floor's real check site. Used 360, not 337 — `ceil(0.85×360)=306`.
  Left a short in-code comment stating the formula, not the historical mismatch (per root
  AGENTS.md: no measurements/history in code comments).
- **`check-dict-build.mjs::RS_FILE_FLOOR` was already at exactly 100%** (24/24, flagged in
  the scratchpad as the edge case to expect). Applying `ceil(0.85×live)` to a floor
  already at 100% *lowers* it (21 < 24) — allowed under the spec's "never lower a floor
  BELOW ceil(0.85×live)" wording (21 lands exactly ON that value), and matches the pattern
  phase 1 already established for small Rust population roots.
- **Kiểm G's own scan caught itself twice while being built**, both fixed by design
  changes rather than exclusions: (1) fixture strings inside Kiểm G's own self-test cases
  that literally spelled `const CLICK_FLOOR = 27` etc. were read by Kiểm G's own file-level
  scan as real declarations — fixed the same way `check-doc-refs.mjs` avoids self-matching
  its own forbidden-pattern self-test (string-concatenate the parts so the raw source text
  never contains the contiguous forbidden shape). (2) A first version of the name filter
  (`name.split('_').includes('FLOOR')`) matched `FLOOR_EXEMPT`/`FLOOR_HELPER_CASES` (FLOOR
  as the FIRST segment, no `_` before it) as false positives, while also MISSING the real
  `RS_FLOOR_FOR_DIALOG_CHECK` (`config_invariants.rs` — FLOOR in the MIDDLE, not the
  suffix). Fixed with one regex, `/_FLOORS?(_|$)/` (a literal `_` must precede
  `FLOOR`/`FLOORS`), which correctly includes the middle-of-name Rust case and excludes
  this file's own FLOOR-prefixed helper names. Also renamed the two extraction regexes
  from `JS_FLOOR_CONST_RE`/`RUST_FLOOR_CONST_RE` to `CONST_DECL_JS_RE`/`CONST_DECL_RUST_RE`
  once the regex became a generic "any const declaration" pattern (matching happens via
  the separate `isFloorConst` filter now, so the old names were no longer accurate) — this
  also incidentally removed those two identifiers from self-matching.
  A separate near-miss: a comment I wrote citing `check-doc-refs.mjs`'s own forbidden
  shape as an example (`` `epics.md:1234` ``) made `check:doc-refs` fail on
  `check-gates.mjs` itself — reworded the comment to describe the technique without
  spelling the literal banned pattern.
- Left `check-i18n.mjs::RS_FLOOR`'s live count as 99 (the gate's own number, confirmed by
  running it), not 98 — per the scratchpad's flagged note, this gate's `.rs` filter isn't
  byte-identical to the plain `src-tauri/src/**` count other gates use; not investigated
  further (out of phase scope), just measured directly rather than assumed.
- Removed several pre-existing multi-line historical/measurement comment blocks (dates,
  story ids, ratio history) directly attached to the floor consts being edited — per root
  AGENTS.md ("in lines you are already changing, delete comments that break these rules")
  and the same precedent phase 1 set on the Rust side. Largest ones:
  `check-debt-owner.mjs::ITEM_FLOOR` (~40 lines), `check-i18n.mjs::RS_FLOOR`/`VUE_FLOOR`
  (~90 lines), `check-commands.mjs::VUE_FLOOR`/`TS_FLOOR`/`COMMAND_FLOOR`/`CLICK_FLOOR`/
  `DISPATCH_FLOOR` (~200 lines, one shared block). Did not touch comments on lines I did
  not otherwise edit.
- Did not run the full `cargo test`/vitest/gate suite (not required for this phase — no
  `Cargo.toml`/migrations/`lib.rs`/`vitest.config.ts` change). Ran only the 10 touched JS
  gates individually plus `tests/frontend/floorJudge.test.ts`; all green. Whoever closes
  the story (or phase 4/5) should still run the full suite once per the spec's
  Verification section, since phase 3 (debt-owner Kiểm D, tokens/i18n items) also touches
  `scripts/check-*`.

## Phase 3 (L11951 predicate · Decision 7 Kiểm D · Decision 4 · L12241 · L12238) — done

Scope: `scripts/check-debt-owner.mjs`, `scripts/check-tokens.mjs`, `scripts/lib/tokens-scan.mjs`,
`scripts/check-i18n.mjs`, `src/layout/dockview-theme.css`, `tests/frontend/checkTokensScan.test.ts`,
and the `2-13`/`spec-5-14` status fixes. Did NOT touch floors (phases 1/2 already routed every
floor through `judgeFloor`/`assert_population_floor` — left untouched), `src/commands`, the
cut-mark blocks, `reset.css`, new frontend tests beyond the pinned one, `pre-push`, `src-tauri/`,
`deferred-work.md`, or the spec. No commit made.

### L11951 predicate (phiếu quyết #42) — `check-debt-owner.mjs`

Added `hasUnreversedHalfPhrase(text)`: finds the LAST match of a half-phrase (`một nửa`/`một
phần`/`vẫn (còn )?mở`/`còn mở`/`còn hở`) and the LAST match of `đã đóng`; the half-phrase wins
only if it is not followed by a later `đã đóng` (handles `… CẢ HAI VẾ CÒN HỞ đã đóng` staying
`closed`). Wired into both `leadingStatus` (bullet first line `- ✅ …`) and `continuationStatus`
(`→ ✅ …`) — previously the phrase-downgrade only fired for `→ ⚠️ …một nửa|một phần…` and used a
DIFFERENT phrase set (`vẫn mở|còn mở` only, missing `một nửa`/`còn hở`), so `✅ ĐÓNG MỘT NỬA` never
downgraded. A `→` line that mentions "một nửa" mid-sentence WITHOUT a leading `✅`/`🟡`/`⚠️một
nửa` no longer changes status (matches the spec's "Phrase on a non-✅ line" row — this is a
narrowing from the old unconditional check).

5 self-check cases added (`HALF_PHRASE_SELFTEST_CASES`, wired into `runSelftest`): bullet-✅ +
"ĐÓNG MỘT NỬA" ⇒ half, `→ ✅` + "ĐÓNG MỘT NỬA" ⇒ half, `→ ✅ ĐÃ ĐÓNG` cùng dòng `⚠️ Còn mở` ⇒ half,
reversed "CÒN HỞ đã đóng" ⇒ stays closed, "một nửa" on a non-✅ `→` line ⇒ status unchanged (open).

**Counter-check:** reverted both `hasUnreversedHalfPhrase(...)` call sites to unconditional
`'closed'` (real deletion, restored via `cp` backup — not `git checkout`) → exactly 3/5 new
self-check cases went red for the measured reason (`mong status=half, nhận closed`); the reversed
and non-✅ cases stayed correct either way (not a regression, confirms they don't depend on the
seam). Restored → 5/5 green again.

### Decision 7 — Kiểm D in `check-debt-owner.mjs`

New pure `statusPairProblems(sprintStatus, grouped, readText)` + `groupStatusFiles(names)` (glob
`<key>-*.md`/`spec-<key>-*.md`, key = `\d+-\d+[a-z]?`, excludes any filename containing
`-phases-`; multiple lot specs for one key sort by filename and the check uses the LAST entry).
`readFileStatus(text)` tries frontmatter `status: '<word>'` FIRST (strips a trailing `#` comment
by construction — the regex only captures the quoted value), falls back to body `Status: <word>`
— **this differs from the spec's Code Map phrasing** ("`<key>-*.md` with a body `Status:` line, or
`spec-<key>-*.md` with a frontmatter `status:`"): measurement showed 16/65 plain (non-`spec-`)
story files ALSO carry frontmatter `status:` (e.g. `3-1-…md`), so keying the parser off the
filename prefix produced 15 false "no status line" reds. Trying frontmatter-then-body regardless
of prefix fixed all of them with 0 remaining false positives.

Allowed pairs: equal, or sprint `review` + file `done`. **Also had to add one more carve-out not
in the spec's explicit exemption list**: a sprint-status key with NO matching file is only a
problem when its status is NOT `backlog` — measured 51 "sprint has key, no file" keys on HEAD;
50 were `backlog` (never drafted, no file is the correct shape) and exactly 1 was `1-22` (`done`,
the one Decision 7 names). Without the `backlog` carve-out, Kiểm D would need 50 named exemptions
instead of the 2 the spec lists, so backlog-without-file is treated as `groupStatusFiles` returning
nothing there is "correct absence," not a status-pair mismatch — only `1-22` needed
`STATUS_PAIR_EXEMPT`. Checked the reverse direction too: exactly 1 file-without-sprint-key case
exists (`6-18`), matching the spec exactly, no extra carve-out needed there.

Fixed `2-13-phan-loai-so-no-va-luat-khong-mo-coi.md` (`Status: review` → `done`, 🔵 note added —
the file's own 2026-08-19 blockquote argued for `review` pending Ice's sign-off on Decision #5,
which sprint-status.yaml already shows happened) and `spec-5-14-…md` (frontmatter `status: 'review'`
→ `'done'`, Spec Change Log entry added) — both per Decision 7 exactly.

11 self-check cases (`STATUS_PAIR_SELFTEST_CASES`, pure — no disk I/O, fake `sprintStatus`/
`grouped`/`readText`) + 1 `groupStatusFiles` self-check (phases-exclusion, kind classification,
multi-lot ordering) wired into `runSelftest`.

**Counter-check 1** (exemption list): emptied `STATUS_PAIR_EXEMPT` → real gate went red with
exactly 2 items (`1-22`, `6-18`) for exactly the measured reasons, plus the same 2 dedicated
self-check cases went red; restored → green, 0 lệch, 102 khoá có tệp.
**Counter-check 2** (backlog carve-out): removed the `if (sprintVal === 'backlog') continue` line
→ real gate went red with exactly the measured 50 items; restored → green.

### Decision 4 — `check-tokens.mjs` + `dockview-theme.css`

Added `classifyCustomProp(prop)`: a custom property (`--dv-*`) whose name ends in `-box-shadow`/
`-text-shadow`/`-opacity`/`-z-index` is reclassified to that bare property name before Kiểm D
(opacity) and Kiểm F (box-shadow/text-shadow/z-index) test it; a real (non-`--`) property is
unaffected. Kiểm F gained `aura-allow-shadow: <lý do>` — but **only for classified custom
properties** (`d.prop.startsWith('--')`); a literal `box-shadow`/`text-shadow` property still has
no escape (verified with a throwaway scratch `.css` file carrying an adjacent `aura-allow-shadow`
comment next to a literal `box-shadow:` — still FAILed, then deleted). `z-index` reuses the
existing `EXEMPTABLE_PROPS`/`aura-allow-z-index` path unchanged.

Found and fixed all 4 vars the spec's Code Map lines (87/101/129/137) point at:
`--dv-floating-box-shadow: none` (marked `aura-allow-shadow`), `--dv-floating-group-dragging-
opacity: 1` (passes Kiểm D silently — value is 1, no exemption needed), `--dv-tab-group-line-
opacity: 1` (same, previously undocumented and untouched by any gate — this is the 4th var the
ledger's "ba biến" text had missed), `--dv-overlay-z-index: 999` (marked `aura-allow-z-index`).
Rewrote the three stale explanatory comments that claimed "Kiểm D/F không thấy dòng này" (now
false) — replaced with the one-line marker comment carrying the reason (AGENTS.md: a claim that
stops being true in code is just replaced, no 🔵 needed).

Had to add explicit JSDoc types (`@type {[RegExp, string][]}`) — `vue-tsc` (`npm run build`)
inferred the suffix-table as `(RegExp|string)[]` and failed on `re.test(prop)`.

Before → after (this gate only): Kiểm D `opacity` declarations checked 3→5 (both new ones pass
silently, value 1). Kiểm F named exemptions 15→17 (the 2 new `--dv-*` markers). 0 FAIL either way.

**Counter-check 1:** reverted `classifyCustomProp(d.prop)` to plain `d.prop)` at both call sites
→ gate stayed GREEN but blind again (dChecked back to 3, the two dockview vars invisible) — this
IS the original bug, so instead counter-checked the enforcement path directly. Restored, then:
**Counter-check 2:** deleted the `aura-allow-shadow` comment above `--dv-floating-box-shadow` →
real FAIL at `dockview-theme.css:80` for exactly `bị cấm (không bóng đổ, không lớp nổi)`; restored
→ green.

### L12241 — `inlineStyleBlocks` (`scripts/lib/tokens-scan.mjs`) + pinned test

Bug: a whole-body string binding (`:style="'color: red'"`) fell into the object-literal `objRe`
parser, whose fallback branch `([^,}]+)` swallowed the closing `'` into `value` (`"red'"`). Fix:
detect `body` as a whole string literal (`/^(['"])([\s\S]*)\1$/`) BEFORE trying `objRe`, and parse
its inner text the same way a static `style="…"` attribute is parsed (split on `;`, colon-split).
The object-literal shape (`:style="{ color: 'red', … }"`) is untouched — its `body` starts with
`{`, never matches the whole-string regex.

Updated the pinned test (was asserting `value: "red'"` as documented-buggy-but-real behaviour) to
assert the correct `value: 'red'`, plus added a multi-declaration `:style` string case
(`'color: red; gap: 4px'` → `['color','gap']`/`['red','4px']`). `tests/frontend/
checkTokensScan.test.ts`: 18 → 20 tests, all green. `npm run check:tokens` scan count unchanged
(113 files, 3751 decls) — confirmed no live `:style="'…'"` string binding exists anywhere in
`src/**` today (`grep -rn ':style="\'' src/` → 0 hits), so this was a latent/theoretical fix, not
a live scan-result change.

**Counter-check:** replaced the `stringLiteral` regex with `null` (always fall through to
`objRe`) → exactly the 2 new/changed pinned cases went red (`expected 'red' vs "red'"`, and the
multi-decl case dropped `gap`), 18/20 other tests unaffected; restored → 20/20 green.

### L12238 — `aura-allow-text` regex (`scripts/check-i18n.mjs`)

Extracted the inline regex into `AURA_ALLOW_TEXT_RE = /aura-allow-text\s*:\s*(?!-->)\S/` (mirrors
lot C's `exemptAt`/`neverTextExemptAt` fix in `check-tokens.mjs`, which uses `(?!\*\/)` for CSS
comments — here the closing token is `-->`). Added an inline self-check block (same style as this
file's existing `vueRegions`/`PANEL_TITLE_KEYS` self-checks — there is no central `SELFTEST_CASES`
array in this file) testing both directions directly against the regex. Confirmed `grep -rn
"aura-allow-text\s*:\s*-->" src/` → 0 hits: no real file currently relies on the buggy empty-
exemption shape.

**Counter-check:** reverted the regex to the old `\S` (no `(?!-->)`) → the self-check went red for
the measured reason (`không lý do: true (phải false)`); restored → green. Also ran an end-to-end
probe: a throwaway scratch `.vue` with `<!-- aura-allow-text: -->` next to a non-`t()` expression
now correctly FAILs Kiểm A2 (previously would have silently passed); scratch file deleted after.

### Verification run this phase

`npm run check:debt-owner` / `check:tokens` / `check:i18n` / `check:gates` — all green, 0 FAIL
each. `npx vitest run tests/frontend/checkTokensScan.test.ts` — 20/20. `npx eslint tests/frontend/
checkTokensScan.test.ts` — clean. `npm run build` — green (needed for the JSDoc-type fix above).
Did not run the full `cargo test`/full vitest suite (not required — no `Cargo.toml`/migrations/
`lib.rs`/`vitest.config.ts` touched this phase either). Whoever closes the story should still run
the full suite once per the spec's Verification section — phases 1–3 together now touch a wide
slice of `scripts/check-*`.

### Surprises / things later phases should know

- `check-debt-owner.mjs`'s new Kiểm D reads `_bmad-output/implementation-artifacts/` via
  `readdirSync` (added to the existing `node:fs` import) — a NEW filesystem read surface for this
  gate beyond the two paths (`deferred-work.md`, `sprint-status.yaml`) it read before. Still no
  npm dependency added.
- The spec's Code Map line for `2-13`/`spec-5-14` said only "Fix 2-13 and 5-14 to done" — did that
  literally (both files), plus a short in-place note per AGENTS.md's "a claim that stops being
  true is fixed in place with 🔵 and a date" rule for the `2-13` story file, since its own
  2026-08-19 blockquote argued at length for why it was deliberately `review`.
- Ledger items for L11951/Decision 7/Decision 4/L12241/L12238 were NOT touched (ledger is a later
  phase's/closing story's job per the spec boundary) — they still read as open/half pointing at
  `Chủ: Story 11.1`.

## Phase 4 (L674 · Decision 5 · L5868 · L4761) — done

Scope: `src/commands/index.ts`, `src/layout/workspaceLayout.ts` (read-only), `src/tokens/reset.css`,
`src/panels/GridPanel.vue`, `src/panels/SourceHanViet.vue`, one new file
`tests/frontend/commandsRegistryPortMissing.test.ts`, `.githooks/pre-push`. Did NOT touch
`scripts/check-debt-owner.mjs`, `src-tauri/`, `deferred-work.md`, or the spec. No commit, no push.

### L674 — `PANEL_SUFFIXES` derived from `PANEL_IDS`

`src/commands/index.ts` now imports `PANEL_IDS` (value) from `../layout/workspaceLayout.ts` and
derives `const PANEL_SUFFIXES = PANEL_IDS.map((id) => id.slice('panel.'.length))`, replacing the
hand-copied literal array. No import cycle (confirmed by the lot D investigation notes and by a
successful plain-Node `import('./src/commands/index.ts')`, same shape `check-commands.mjs` already
runs). This is a source fix, not a new guard — no gate asserted the old array against `PANEL_IDS`
before, and none was added; the fix removes the seam that used to let them drift, structurally.

**Verification, not a red/green guard test (there was no prior guard):** temporarily hardcoded
`PANEL_SUFFIXES` back to `['grid', 'lookup']` (2 of 3, the exact drift shape the ledger item
describes) and ran `check:commands` — it stayed **green**, command count silently dropped
177 → 176, `COMMAND_FLOOR` (151) still satisfied. This confirms the ledger's original complaint
(no gate would have caught the old copy-paste drift) and confirms the fix is the correct remedy
(a counting floor can't tell 176 valid commands from 177 valid commands). Restored via `cp` backup,
re-ran `check:commands`: 177 again, green.

### Decision 5 — shared `:root` cut-mark shape, `src/tokens/reset.css`

Added `:root { --cut-mark-width: 2px; --cut-mark-height: 1.3em; }` to `reset.css` (loaded globally
via `main.ts`, before `.cut-mark`/`.cut-here::before` mount — custom-property inheritance crosses
`scoped` boundaries, confirmed by the lot D investigation and by Kiểm I already accepting any
locally-declared `--name`). `GridPanel.vue::.cut-mark` and `SourceHanViet.vue::.cut-here::before`
now both read `var(--cut-mark-width)`/`var(--cut-mark-height)` instead of the literal `2px`/`1.3em`
each carried by hand. `background-color: var(--color-primary)` was already a real shared token in
both blocks — left untouched, no new variable needed for color. Rewrote the stale
"🔴 Hai khối này phải đổi CÙNG LÚC. Không cổng nào canh…" comment in `SourceHanViet.vue` (that claim
stopped being true for width/height) to say plainly which properties are now single-sourced and
which (`display`/`vertical-align`/`background-color`) are still two hand-kept declarations.

**No visible change:** the two `var()` calls resolve to the exact same literals the two blocks
carried before (`2px`/`1.3em`) — definitional, not measured, since there is now exactly one
declaration instead of two copies of the same numbers. `check:tokens` scan count moved 113 files ·
3751 → 3753 declarations (+2, the two new `:root` declarations in `reset.css`); no other count
changed.

**Real seam-removal counter-check:** renamed the `:root` declaration to
`--cut-mark-width-TYPO-PROBE` (backup via `cp`, not `git checkout`) and ran `check:tokens` — Kiểm I
("mọi `var(--x)` … trỏ tới biến có thật") went red at exactly the two consuming lines
(`GridPanel.vue:2157`, `SourceHanViet.vue:957`), each reporting `var(--cut-mark-width)` không trỏ
tới biến nào có thật. This is a real, new guard interaction: before this change Kiểm I never looked
at these two declarations (they were literals, no `var()`), so today a typo in the shared variable
name is caught where a typo in either of the old duplicated literals never could be (a wrong number
just silently rendered wrong). Restored from backup, re-ran: green, 2328 `var()` refs resolved
again.

### L5868 — `tests/frontend/commandsRegistryPortMissing.test.ts` (new)

Two describe blocks, 4 tests total, following the `editorClearSourceCuts.test.ts` precedent
(`installCommands({ isMac: true, setMode: () => {} })`, fresh module per test via
`vi.resetModules()`, real `KeyboardEvent` through `attachKeyboard`).

1. **`portMissing` without its port.** Installs with only the mandatory `setMode`, then iterates
   `commandRegistry.list()` and runs every command whose id is not `mode.*`. Measured directly
   (not assumed from the stale scratchpad note of "132 commands," which undercounted — the real,
   current count is **177**, confirmed by `check:commands`' own printed total): with only `setMode`
   provided, **174/177** commands log `console.error` naming their own id; the **3** exceptions are
   exactly `mode.library`/`mode.workspace`/`mode.reading`, which don't check a port at all because
   `setMode` is the one *mandatory* dep, always present — that's a real, structural exclusion, not
   an arbitrary skip, and the second test asserts it explicitly (`mode.*` ⇒ zero `console.error`
   calls). The loop asserts "at least one call names this id" rather than "exactly one call,"
   because `focus.next_panel`/`focus.prev_panel` legitimately log **twice** per run (their own
   `portMissing('panelRing')`, then a real fallback to `focus.next(PANEL_PREFIX)` which itself logs
   when zero focus owners are declared in a bare unit test) — asserting "exactly one" would have
   been a test bug, not a product bug; found this by actually running the loop first, not by
   guessing the shape.
2. **`Mod+Alt+C` through the real keymap.** `attachKeyboard(host)` + a real
   `new KeyboardEvent('keydown', { code: 'KeyC', metaKey: true, altKey: true, … })` on a bare
   `<div>` (not a typing zone, so `isTypingZone` doesn't apply — chord has `Mod`, so it wouldn't
   apply anyway). Without `focusGlossaryConfirmStrip`: exactly one `portMissing` call naming both
   `glossary.confirm.focus` and `focusGlossaryConfirmStrip`. With it provided: zero `portMissing`
   calls, and the dep is called with `''` (empty prefill, since `currentSelectionForGlossary` is
   also absent) — a positive control proving the guard isn't vacuous.

Spy lifecycle moved to `beforeEach`/`afterEach` (not per-test `vi.spyOn`/`mockRestore` at the end of
each `it`), specifically because the first draft threw mid-loop on an assertion (the "exactly one
call" bug above) and skipped its own `spy.mockRestore()`, leaking mocked `console.error` state
(and a delayed nothing — the leak was call-history, not timers) into the next test. `afterEach`
guarantees restore regardless of pass/fail.

**Real seam-removal counter-check:** removed the `if (deps.focusGlossaryConfirmStrip === undefined)
return portMissing(...)` guard from `glossary.confirm.focus`'s handler, replacing the body with an
unguarded `deps.focusGlossaryConfirmStrip?.(...)` (backup via `cp`). Ran the new test file: **2 of
4 tests went red** for exactly the measured reason (`glossary.confirm.focus` stopped reporting
`portMissing`) — both the all-commands loop (fails at that one id) and the dedicated Mod+Alt+C test
(0 calls instead of 1). Restored from backup, re-ran: 4/4 green.

`npx eslint` clean; `npm run build` (vue-tsc) clean after one fix — `errSpy.mock.calls` needed an
explicit `as unknown[][]` cast (`ReturnType<typeof vi.spyOn>` alone left the callback parameter
implicitly `any` under strict mode).

### L4761 — CI-verdict warning step, `.githooks/pre-push`

Plain shell, no `npm run check:*` name, appended after the 12-gate loop and before the
build/test/cargo-test block. Confirmed safe against `check:gates` Kiểm D/A/E/F by actually running
`check:gates` after the edit (still "12 cổng", still green) — matches the lot D investigation's
prediction that a nameless raw-shell step is invisible to those Kiểm by construction, not by
inspection alone.

Design follows the investigation's draft closely: `command -v gh` → `git rev-parse '@{u}'` (falls
back to empty via `||`, never aborts under `set -e`) → `gh auth status` → `perl -e 'alarm shift;
exec @ARGV' 8 gh run list --workflow ci.yml --branch master --event push --json …` piped to a
small inline `node -e` that reads stdin synchronously (`fs.readFileSync(0, 'utf8')`), wrapped in
`try/catch` so it always prints a verdict string and always exits 0 (required so the pipe's exit
status — which is the last command's, `node`, since this script never sets `pipefail` — never trips
`set -e`). `case` over the verdict: `failure` prints one yellow warning line naming the upstream SHA
and never calls `fail()`/`exit 1`; `success` prints green `OK`; `in_progress`/`queued` prints a
neutral grey note; everything else (`none`, missing `gh`, no upstream, unauthenticated, timeout)
prints a silent grey `—`.

**Exercised directly (not by pushing), all four required branches, using fake `gh` executables
prepended to `PATH` plus the real `gh`/real upstream for the green case:**
- **Real green run** (real `gh`, real upstream `2abddd3…`): `OK`, confirms the happy path against
  live data, not just fakes.
- **Red-previous-run:** fake `gh run list` returns `{"headSha":"<real upstream sha>","conclusion":
  "failure","status":"completed"}` ⇒ printed the yellow warning line naming that SHA, push not
  blocked (script exits 0 for this step regardless).
- **No-match:** fake `gh run list` returns a run with a different `headSha` ⇒ `none` ⇒ silent `—`.
- **Unauthenticated:** fake `gh auth status` exits 1 ⇒ short-circuits before the `perl`/network
  call at all ⇒ silent `—`.
- **Forced timeout:** fake `gh run list` blocks for 60s ⇒ killed at ~8s by the `perl` alarm ⇒ silent
  `—`. First attempt at this fake used `sleep 60` as a child of a `#!/bin/sh` wrapper and measured
  **60s, not 8s** — a real Unix pipe gotcha, not a bug in the alarm mechanism: the wrapper shell
  forked a child (`sleep`) that inherited the pipe's write-end fd; SIGALRM killed the *parent* shell
  at 8s (confirmed independently: identical construct with no pipe attached DID die at the correct
  mark), but the orphaned `sleep` child kept the pipe's write end open for its own full 60s,
  so the downstream `node` reader never saw EOF until then. Fixed the fake by using `exec sleep 60`
  (replaces the wrapper process instead of forking a child — matching how the real, single-process
  `gh` binary behaves) — re-ran, killed cleanly at 8.17s. Left as a fact for whoever next writes a
  `perl alarm | shell-script-that-forks` test fixture: pipe an artificial timeout through a real
  external command directly, or `exec` inside the wrapper, never a bare forked child.

### Verification run this phase

`npm run check:tokens && check:i18n && check:layout && check:commands && check:gates` — all green,
0 FAIL each (printed scan counts: tokens 113 files/3753 decls, commands 177 commands/184 dispatch
calls, gates 48/48 floor constants + 19 self-check cases, all unchanged from phase 3 except the
tokens count noted above). `npx vitest run tests/frontend/commandsRegistryPortMissing.test.ts
tests/frontend/checkTokensScan.test.ts tests/frontend/floorJudge.test.ts` — 31/31 (new file's own
4, plus the two phase-3 files re-run as a cross-check since they share `scripts/lib/`). `npx eslint`
clean on every touched file. `npm run build` (vue-tsc + vite) clean, twice (before and after the
final test-file type fix). Did not run the full `cargo test`/full vitest suite (no
`Cargo.toml`/migrations/`lib.rs`/`vitest.config.ts` touched this phase) — this phase's own files
are all frontend/`scripts`-adjacent (`src/**`, `.githooks/**`); phases 1-3 already flagged the same
"run the full suite once before the story closes" note, still true and now covering phase 4 too.

### Surprises / things phase 5 should know

- `PANEL_SUFFIXES`'s derivation and the cut-mark `:root` vars are **source fixes**, not new guards
  — neither one has a red/green gate of its own (Decision 5's protection is Kiểm I, which already
  existed and now happens to reach two more lines; L674 has no gate at all, by design, per the lot
  D investigation's "no gate needed" conclusion). If phase 5 or the closing story writes ledger
  dispositions expecting a named new Kiểm for either item, there isn't one — the disposition should
  say "fixed at the source" with the measurements above as evidence, not point at a check.
- The lot D investigation notes' "132 registered commands" figure (used for the L5868 feasibility
  read) was stale even against its own same-day HEAD re-read — today's real count is **177**
  (matches `check:commands`' own total, and matches phases 1-2's `COMMAND_FLOOR` work). The new
  test measures this itself (`checked.length`) rather than hardcoding either number, with a loose
  `>= 150` floor so it doesn't need editing every time a story adds a command.
- `focus.next_panel`/`focus.prev_panel` log `console.error` **twice** per bare call when no
  `panelRing` and no focus owners are declared (their own `portMissing` plus a real fallback path).
  Any future test iterating "every command" against a minimal-deps registry should assert
  "at least once," not "exactly once," or re-derive this per-command from a real run first.
- Ledger dispositions for L674/Decision 5/L5868/L4761 were **not** written (ledger is out of this
  phase's scope per the task brief) — they still read as open, `Chủ: Story 11.1`.

## Phase 5 (ledger dispositions for all 18 items) — done

Scope: `_bmad-output/implementation-artifacts/deferred-work.md` only — one `→` line appended to
each of the 18 items (item text untouched). No code, no spec, no `sprint-status.yaml`. No commit.

Found the 18 items by `grep -n 'Chủ: Story 11.1'` plus full-context `Read` around every hit
(grep-only context caused one real mistake, caught before writing: `Read` showed two items —
`:653` dv-* naming, `:674` `PANEL_SUFFIXES` — each carry a **third** chain line one line past
what the grep snippet showed, and that third line is the one naming `Chủ: Story 11.1`; anchored
the insertion there instead). Final 18 anchors (current-HEAD line of each item's last existing
line, before this phase's edit): `:122` `:193` `:291` `:655` `:676` `:4173` `:4654` `:4783`
`:4792` `:5640` `:5807` `:5882` `:7195` `:9445` `:11211` `:11968` `:12240` `:12243`.

Dispositions: 15 `✅ ĐÃ ĐÓNG` (5 floor items closed by phases 1–2's shared helper + Kiểm G; Quyết
định 4/5/7 items; L674/L5868 source-fix/test items; L4761 CI-verdict warning; L11951 predicate;
L12238/L12241 regex/parser bugs), 3 `KHÔNG LÀM` (L119, L190 — per Quyết định 3, naming the class
a shared early-return still misses; L5628 — per Quyết định 6, the "bàn đo" bench item, reopen
condition = bench reused again), 1 reassignment (`:4785` WAL regression window → `Chủ: Story
11.5`, Quyết định 8, with the 7/14-red CI measurement given by the orchestrator, not re-derived).

Wrote the 18 lines via a one-off Python line-inserter (`/private/tmp/.../scratchpad/
insert_ledger_lines.py`, bottom-to-top insertion by 1-indexed line number so earlier anchors stay
valid) rather than `Edit`, because the boilerplate closing line (`giao theo
sprint-change-proposal-...`) is byte-identical across ~30+ items in this file and is not a safe
`old_string` anchor even with a few lines of context in several cases.

Self-inflicted regex trap, caught before writing: `check-debt-owner.mjs`'s `continuationStatus`
tests `STILL_OPEN_RE` (`vẫn (còn )?mở|còn mở`) against the **raw text of every `→` line**,
unconditional on a leading `✅`. The `L11951` closing line's first draft literally quoted the
predicate's own trigger phrases (`một nửa`/`một phần`/`vẫn mở`/`còn mở`) — which would have
flipped that very line's own status to `half` on write. Reworded to describe the predicate
without reproducing those four substrings contiguously (kept `còn hở`, which is not a trigger and
already used elsewhere in the file). Grepped every other one of the 18 drafts for the same two
regexes (`còn mở`/`vẫn mở` and, on `✅` lines only, `một nửa`/`một phần`) before writing — 0 more
hits.

**Verification:** `npm run check:debt-owner` — green (Kiểm A/B/C/D all OK; AC5: 710 total · 392
open · 102 half · 150 closed · 66 decided · 0 orphans). `node scripts/check-debt-owner.mjs
--report` — same four numbers. Confirmed the "no open/half item whose last concrete `Chủ:` is
Story 11.1" requirement with the file's **own parser**, not a bare grep: extracted lines 1-896 of
`check-debt-owner.mjs` (every function/regex, stops right before the file's own top-level
"read the real ledger and run the gate" tail) into a temporary `scripts/_tmp_debt_probe.mjs`,
called its real `parseItems`/`summarize` against the actual ledger, filtered
`(status === 'open' || status === 'half') && latestOwnerKey === '11-1'` — **0** matches. Deleted
the probe file immediately after (not committed, not left behind).

Did not run `cargo test`/full vitest/`check:gates` this phase — no code, `Cargo.toml`,
migration, `lib.rs`, or `vitest.config.ts` touched; only `check:debt-owner` is in scope per this
phase's brief. Whoever closes Story 11.1 still owes the spec's Verification section run once
(full suite + all four `check:*` gates together) and the CI/nightly-e2e reads AGENTS.md requires
before `done` — neither was done by any lot D phase.

## Audit fixes

A test audit against the spec's I/O & Edge-Case Matrix found two rows with no automated test
that runs — closed both, nothing else touched.

**Shadow var (Decision 4).** `check-tokens.mjs` Kiểm F gained an in-script self-check
(`F_CLASSIFY_CASES` + `F_SHADOW_EXEMPT_CASES`, ~10 cases) mirroring Kiểm K/I's own pattern —
calls the real `classifyCustomProp`/`exemptAt`/`BANNED_PROPS` production values directly, no
extracted lib function needed (`classifyCustomProp` already lives in `check-tokens.mjs`, already
pure). Covers: unmarked shadow custom prop ⇒ not exempt, marked-with-reason ⇒ exempt,
marked-without-reason ⇒ not exempt, a literal `box-shadow` carrying the marker ⇒ still not exempt
(no escape hatch by design), `--x-opacity`/`--x-z-index` classified correctly, `--x-color`
unchanged. Counter-check: removed the `-box-shadow$` suffix rule → self-check went red for
exactly that reason (`mong 'box-shadow', nhận '--x-box-shadow'` + the exemption case flipping);
restored from a `cp` backup, green again, scan totals unchanged (113 files/3753 decls).

**Previous CI run red / offline / unauthenticated (I/O matrix rows).** Extracted the
shell+inline-`node -e` CI-verdict block out of `.githooks/pre-push` into
`scripts/ci-previous-verdict.mjs`: a pure `ciPreviousVerdict({ run })` core (no `fs`, no
`process`, no timers) plus one impure edge `spawnRun` using `child_process.spawnSync` with an 8s
timeout for `git rev-parse @{u}` (name written without literal `@{u}` in JSDoc — TS's doc-comment
parser reads `@{` as a broken tag start and throws `TS1003`), `gh auth status` and `gh run list`.
Runs `main()` only when invoked directly (`import.meta.url` vs `pathToFileURL(process.argv[1])`).
No shell, no `perl`, no `/dev/null` — same behaviour on Windows. `.githooks/pre-push` now calls it
with a single `node scripts/ci-previous-verdict.mjs || true` line; it prints the same label/colours
the shell did and never throws or exits non-zero, so the `|| true` is a second belt on top of the
script's own guarantee. Deliberately NOT named `check-*` and NOT added to any of the three gate
lists — `check:gates` confirmed unaffected (still 12 cổng / 48 floor constants / 19 self-check
cases). Added to `tsconfig.node.json`'s `include` (mirrors `check-tokens.mjs` + its lib file
already being there); `npm run build` green.

`tests/frontend/ciPreviousVerdict.test.ts` (14 cases): failure/success/in_progress/queued via the
injected `run`, plus every silent branch (no matching sha, `gh` missing via `ENOENT`, unauthenticated,
upstream timeout via `ETIMEDOUT`, a signal-killed spawn with no Node-set `.error`, malformed JSON,
non-array JSON), plus two `printVerdict` output-shape cases. Counter-check: loosened the
`headSha === sha` match in `ciPreviousVerdict` to match any run → the "no matching sha" case went
red for exactly that reason (kind flipped from `silent` to `failure`); restored from a `cp` backup,
14/14 green again.

**Real run, both branches observed live** (not just via the injected fakes): first
`node scripts/ci-previous-verdict.mjs` on this machine printed the silent `—` (an 8s `gh`
cold-start via direct `spawnSync`, no shell, took ~14s here — confirmed independently with
`gh --version` alone); the very next invocation, with `gh`/keychain warm, printed a real green
`OK` matching the actual upstream commit's CI conclusion. Both are the script behaving correctly,
not a bug — matches root AGENTS.md's "suspect the environment first, re-run the same binary
twice" guidance. Not a code change; noted here as a measured fact for whoever next reads a
`ci (trước)` verdict on a cold shell.

Verified: `npm run check:tokens` and `npm run check:gates` green (0 FAIL each);
`npx vitest run tests/frontend/checkTokensScan.test.ts tests/frontend/ciPreviousVerdict.test.ts`
34/34; `npx eslint tests/frontend/ciPreviousVerdict.test.ts` clean; `npm run build` green;
`sh -n .githooks/pre-push` clean. Did not run the full `cargo test`/full vitest suite — this pass
touched no `Cargo.toml`, migration, `lib.rs`, or `vitest.config.ts`. `deferred-work.md` not
touched (out of scope — this pass closes a test-coverage gap the spec's own matrix flagged, not a
ledger item).

## Orchestrator corrections (review)

- The Phase 3 note on L11951 describes `hasUnreversedHalfPhrase` (last half-phrase vs last "đã đóng"). That version was replaced: on the real ledger it turned two 🟡 items (L139, L9170) back to open and missed L204. Shipped: `HALF_ON_CLOSED_LINE_RE` (một nửa|một phần|vẫn mở|còn mở) on ✅ lines, plus the old `STILL_OPEN_RE` rule on every `→` line; "còn hở" is not a trigger. Measured: 4 flips (L105, L204, L268, L2012), 0 false positives.
- Kiểm D (sprint-status ↔ story file status) was removed at review on Ice's call: gates check source code and app behaviour only, not task status or `_bmad-output` documents. L5794 is `KHÔNG LÀM`; `2-13`/`spec-5-14` restored.
- `check-doc-refs.mjs::FILE_FLOOR` 366 → 369: the lot's own new files moved live to 433.
