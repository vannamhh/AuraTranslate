---
epic: 4
date: 2026-09-23
verdict: rejected
criteria: profiled
headless: false
---

# Retro Epic 4 — Dịch có AI hỗ trợ

Ice yêu cầu retro này đặt trọng tâm vào QUY TRÌNH: một story mất 4–5 giờ, test chạy quá nhiều (sửa nhỏ cũng chạy full), agent sa đà vào chi tiết giao diện (chiều cao, chiều rộng) trong khi giao diện còn đổi. Ice cũng dặn tìm thêm các vấn đề khác. Vì vậy phần A (quy trình) đứng trước phần B–C (mã).

## Epic summary

- **Epic:** 4, gồm 12 story (4-1 … 4-12). Thứ tự chạy thật: 4-1 xong 2026-08-26, trước Epic 5 và 6. Từ 4-2 đến 4-12 chạy 2026-09-16 → 2026-09-23.
- **Dải diff:** `334a3d0^..HEAD` (`505c8bc`), gồm 17 commit và 0 merge. Riêng 4-1 là `4df42f8`, nằm ngoài dải. Nguồn: `git_evidence.py`, tệp tạm của phiên retro là `scratchpad/git_evidence.json`.
- **Khối lượng:** thêm 47.376 dòng. Chia ra: tài liệu `_bmad-output` 12.624 · test Rust 9.848 · test frontend 8.177 · mã frontend 8.774 · mã Rust 7.614. **Dòng test (18.025) nhiều hơn dòng sản phẩm (16.388).**
- **Story chưa `done`, Ice đồng ý retro dù vậy:** cả 12 story đều ở `review` (`sprint-status.yaml:118-129`), còn `epic-4: in-progress`. Frontmatter của 12 spec đều ghi `status: done`, tức đang chờ Ice ký. Vì còn story chưa `done`, phán quyết máy bị ép thành **rejected**.
- **Chi phí và thời gian đo được:** nguồn là bản ghi `cost-state` trong nhật ký phiên `~/.claude/projects/-Users-hoangnam-LocalSites-addon-AuraTranslate/<sid>.jsonl`. Wall là khoảng từ sự kiện đầu đến sự kiện cuối của phiên chính.

| Story | Commit | Phiên chính | Wall | Chi phí |
|---|---|---|---:|---:|
| 4-1 | `4df42f8` | `1be60755` | 1,65 h | — |
| 4-2 | `c7d5cb3` + `93fe113` | `7fbf256f` | 3,45 h | $91,76 |
| 4-3 | `8a1d8f0` | `a00bacd6` | 12,31 h (qua đêm; hoạt động 1,75 h) | $66,75 |
| 4-4 | `3cebcc6` | `309c2db9` | 4,39 h | $102,21 |
| 4-5 | `a4a5526` | `be7d936b` | 4,35 h | $92,77 |
| 4-6 | `6de227d` | `af402405` | 4,04 h | $99,05 |
| 4-7 | `df5e576` + `77923f0` (xem A8) | `80cb5b41` | 7,51 h | $222,42 |
| 4-8 | `2376aeb` | `8f949d3e` | 6,52 h | $156,18 |
| 4-9 | `915f1af` | `bc052533` | 4,20 h | $105,18 |
| 4-10 | `d1451a8` | `56420f63` | 7,66 h (có 4h57' Ice vắng) | $69,31 |
| 4-11 | `bb902f9` | `29fe5d7e` | 4,02 h | $70,52 |
| 4-12 | `505c8bc` | `8234adfe` + `f7d6ad2e` + `ff1fd6aa` | ~19,7 h wall, 3 phiên | ~$135 |

Tổng `cost-state` của 16 phiên Epic 4 (gồm cả phiên dọn nợ `15dc8969`): **$1.224,94**.

- **Bằng chứng đã có:** 12 spec, 4 tệp `*-phases-*`, sprint-status, retro Epic 6, nhật ký 16 phiên chính kèm subagent, `gh run list`.
- **Bằng chứng thiếu:** nhật ký phiên của 4-1 chỉ được lấy chỉ số, không đọc định tính. Hành vi thật của các luồng AI không được chạy lại trong retro này (xem Behavior verification).
- **Cách phân tích:** 4 agent con, mỗi agent ghi phát hiện ra tệp trong scratchpad của phiên (`retro/10-long-stories.md` · `20-ui-geometry.md` · `30-aggregate.md` · `40-code-review.md`). Các số chịu tải được kiểm lại bằng lệnh riêng; những chỗ agent sai đã sửa ngay trong tài liệu này.

## Findings

### A. Quy trình — trọng tâm Ice yêu cầu

**A1. Story dài vì luồng chính ngồi chờ một chuỗi subagent, mỗi subagent phải đọc lại từ đầu.**
- Luồng chính nằm yên chờ subagent: 77% thời gian ở 4-7 (5,77/7,51 h), 79% ở 4-8, khoảng 80% ở 4-10. Ở 4-11 chỉ có 48' chết thật. Nguồn: `retro/10-long-stories.md` §bảng tổng.
- 4-7 sinh 22 subagent. 16/22 agent tự đọc lại spec 1.993 dòng, tổng 144 lượt Read/Edit trên spec. 9 lượt reviewer, mỗi lượt phân trang lại từ đầu một diff khoảng 7.000 dòng, tổng hơn 90 lượt Read. Nguồn: `80cb5b41` + meta subagent.
- Subagent vượt trần 250k của AGENTS.md:
  - 4-7: 8/22, đỉnh 572k. Cả ba "Phase" tách ra CHÍNH ĐỂ né trần đều vượt.
  - 4-8: 6/14, đỉnh 541k. 3/6 agent còn bị resume sau vòng rà để tự vá.
  - 4-11: 1/8, đỉnh 637k.
- ⇒ Luật "chia story thành ~4 pha, mỗi pha một agent mới" (AGENTS.md §Policy) đang được làm theo: đủ pha, đủ tệp bàn giao. Nhưng mỗi pha vẫn nạp lại cùng một spec 1–2 nghìn dòng và vẫn phình quá trần. Nguyên nhân là bài đọc bắt buộc quá dày, không phải số pha quá ít. Điều này khớp memory `lever-context-la-bai-doc-khong-phai-lat-viec`.
- ⚠️ Đối chứng chưa kiểm soát được biến: 4-11 rẻ nhất ($70,5) dù chỉ có MỘT agent thi công ôm trọn story, đỉnh 637k. Nhưng 4-11 cũng là story nhỏ (spec 336 dòng). Xem Open questions Q2.

**A2. Test chạy quá nhiều. Nguyên nhân chính nằm ở mẫu spec, agent không tự ý.**
- Đo trên nhật ký 15 phiên thi công cộng subagent. Thời lượng là của lượt chạy foreground, tính từ `tool_use` đến `tool_result`.

| Loại | Số lượt | Trung vị | Tổng chờ |
|---|---:|---:|---:|
| `cargo test` toàn bộ | 130 | 121 s (p90 268 s) | ~227 phút |
| `cargo test` theo phạm vi | 416 | 17 s | ~186 phút |
| `vitest` toàn bộ | 121 | 56 s | ~87 phút |
| `vitest` theo phạm vi | 265 | 5 s | ~51 phút |
| `npm run test:story` | **35** | 2 s | ~9 phút |

- **`test:story` dùng 0 lần từ 4-1 đến 4-8**, lần đầu xuất hiện ở 4-9. Trong khi đó AGENTS.md đã bắt dùng nó làm vòng dev từ 2026-09-12. Riêng 4-7 chạy khoảng 22 lượt full cargo và 23 lượt full vitest.
- **Nguyên nhân cấu trúc 1 — mẫu Verification của spec.** Mọi spec từ 4-2 đến 4-12 đều ghi: `npm run build` → `cargo test --locked` (toàn bộ) → `npx vitest run` (toàn bộ) → 11 cổng pre-push chạy lẻ. Spec 4-10 và 4-11 còn thêm: *"Run all fourteen before calling a **phase** done"* (`spec-4-10-loi-mang-va-loi-api.md` §Verification). Câu đó xuất phát từ một sự cố ở 4-8, khi `vue-tsc` đỏ bị che mất. Một bài học của một lần đã cứng lại thành luật full suite cho MỖI pha. Mỗi story có ~4 pha, cộng một lượt đo nền *"measure the baseline on this story's own starting commit"*, cộng lượt sau vá review, nên tối thiểu 6 lượt full suite cho mỗi story.
- **Nguyên nhân cấu trúc 2 — subagent tự chạy `.githooks/pre-push` bằng tay.** Ở 4-5 có hai lượt: 5,1 phút và **44,7 phút**. Lượt thứ hai bị đẩy xuống nền rồi treo. Nguồn: `be7d936b`, agent `a9438135…`, 2026-09-17T12:01Z.
- ⇒ Ý của Ice "sửa nhỏ cũng test full" ĐÚNG, và bằng chứng chỉ ra đúng câu gây ra nó.

**A3. Review bị ép phải sản xuất phát hiện.**
- Lớp "Blind Hunter" trong `bmad-build` ép reviewer: *"Compute your finding floor N … find at least N issues"* và *"If you have zero findings, re-check and keep thinking; do not stop with an empty list"* (`.claude/skills/bmad-build/customize.toml:91-94`). Với diff 100–200 kB, N = 10 phát hiện bắt buộc. Cùng khuôn này có trong `bmad-code-review/customize.toml:56-59` và `bmad-build-auto/customize.toml:62-65`.
- 4-7 chạy 3 vòng × 3 vai = 9 lượt rà. Phân loại các phát hiện: 13 lỗi sản phẩm thật, 9 lỗi chất lượng test, 2 lỗi tài liệu. Vòng B "vá" lỗi bản ghi sống sót qua lượt đổi Tác phẩm nhưng chưa vá ở webview, nên vòng C bắt lại đúng lỗi cũ. Nguồn: `retro/10-long-stories.md` §4-7.
- ⇒ Review KHÔNG vô ích: nó bắt được lỗi thật ở hầu hết story. Nhưng sàn N biến "không tìm thấy gì" thành điều bị cấm, và mỗi phát hiện ép ra đều tốn một lượt triage kèm có thể một lượt vá.
- Chiều ngược lại, ở 4-10: 14 phát hiện, chỉ 6 được xử lý. **8/14 không thấy vá, cũng không ghi nợ.** Trong số đó có 3 lỗi sản phẩm thật: `retryAiTranslate`/lô từ chối chỉ bằng `console.warn`, đúng lớp SILENT emptiness; khoảng trống lỗi chưa đóng; vi phạm AC1. Nguồn: `56420f63`, 04:19–04:26Z. Vậy khối lượng review lớn không đảm bảo lỗi thật được xử lý.

**A4. Chi tiết giao diện. Ice đúng ở tầng quy trình, còn tầng test thì sạch.**
- **Tầng test sạch pixel.** Lấy mẫu 293 ca trong 16 tệp test frontend của epic: chỉ 1 ca (0,34%) chạm hình học, là `dockTree.test.ts:231`, tỉ lệ sash 50/50. Không có quy tắc viết nào đẩy agent đo pixel. Ngược lại, `tests/AGENTS.md:10` cấm claim hình học trong vitest. Nguồn: `retro/20-ui-geometry.md` §B–C.
- **Công sức thật dồn vào Story 4-12, một story dành trọn cho ngưỡng bề rộng:**
  - Spec 1.396 dòng cộng tệp phases 966 dòng. 113 dòng spec chứa từ vựng px/ngưỡng/width/height.
  - Wall ~19,7 h qua 3 phiên, chi phí ~$135.
  - AC3 đòi *"mỗi số truy về một phép đo thật"*. Task 11 dựng cả một quy trình hiệu chỉnh cho hai preset, trong đó người đo là Ice, trên bản build thật (`4-12-phases-2026-09-22.md:247`).
  - Kết thúc: dừng sau ĐÚNG một phép đo (1280×833), Ice ra Ruling 1 giữ 4 số hạt giống. Chính tiêu đề commit `505c8bc` ghi: *"bốn ngưỡng là hạt giống Ice chốt chứ không phải số đo"*.
  - ⇒ Phần lớn quy trình hiệu chỉnh đã đặc tả không được dùng tới.
- Nợ pixel cũng bị đẩy về Ice: *"`titlebar-height` 40px trong sản phẩm, DESIGN.md/epics.md ghi 38px"*, với dòng "Chủ: Ice — chọn con số đúng". Mục này thêm vào `deferred-work.md` ở commit `505c8bc`.
- **Lỗi thật của 4-12 thì ngược chiều.** Mã ẩn/khôi phục panel đọc `boundingBox`, tức hình học DOM sống, để suy ra hướng panel. Nhưng giá trị đó bằng 0 trước khi dock được đo, nên mọi hướng đều ra `right`. Kết quả: Tra cứu và Đề xuất AI bị dựng cạnh nhau thay vì xếp trên-dưới bên phải (`spec-4-12-…md:234-240`).
  - Đó chính là câu Ice sửa lúc 2026-09-23T06:14Z: *"đang đi sai thiết kế, tôi đã chốt chia 3 khung…"*. Thiết kế 3 khung đã nằm trong mã từ Story 2.5b.
  - Việc làm lại mất 2 pha mới (4d, 4e), khoảng 3h33', chiếm ~18% wall của story.
- ⇒ Kết luận: vấn đề không phải "test đo px". Vấn đề là **(1)** quy hoạch coi các ngưỡng bề rộng là AC cần đo đạc, trong khi giao diện chưa ổn định, và **(2)** mã dùng pixel sống làm nguồn sự thật cho một quyết định cấu trúc.

**A5. Việc phụ nặng hơn việc chính ở luồng chính và ở tài liệu.**
- Epic thêm 12.624 dòng tài liệu `_bmad-output`, bằng 77% số dòng sản phẩm. Spec 4-7 dài 1.993 dòng, 4-8 dài 1.042 dòng.
- Ở 4-10, luồng chính có 143 lượt gọi tool: 34% chạm spec/`_bmad-output`, 15% chạm AGENTS.md/MEMORY, 0 Edit trên mã sản phẩm. Nguồn: `retro/10-long-stories.md` §4-10.
- `deferred-work.md` còn 11.413 dòng. Epic 4 mở thêm 55 mục nợ, đóng 11, còn **44 mục mở, cả 44 đều "Chủ: Ice"** (`retro/30-aggregate.md` §4.4). Sổ nợ là nơi đẩy quyết định sang Ice, và Ice chưa có lượt nào để tiêu hoá chúng.

**A6. Watcher nền bị bỏ quên, lặp lại một cái bẫy đã được ghi.**
- 4-10: subagent Phase 2 bật `until grep -q "^EXIT="…` rồi bàn giao mà không đóng. Watcher treo **6h18'** (03:29:55 → 09:47:55Z) và chỉ lộ ra khi Ice hỏi thẳng.
- 4-11: Monitor hết hạn mà không được bật lại, gây một khoảng chết 48'.
- AGENTS.md đã có cảnh báo này từ 2026-09-12. Memory `canh-nen-phai-nam-trong-loi-giao-khong-phai-trong-agents-md` đã kết luận: cảnh báo phải nằm TRONG lời giao, không phải trong AGENTS.md.

**A7. Năm story chưa từng qua CI, và bộ e2e không có spec nào cho AI.**
- `origin/master` = `5a23410` (2026-09-19). HEAD đi trước 6 commit chưa push: `2376aeb` (4-8), `915f1af` (4-9), `d1451a8` (4-10), `afbe740`, `bb902f9` (4-11), `505c8bc` (4-12). Tôi đã kiểm lại bằng `git log origin/master..HEAD`.
- Lượt nightly xanh gần nhất (`35782683524`, 2026-09-22) đo cây `5a23410`, tức trước 4-8. Các lượt nightly đỏ gần đây (`35658307292`, `35390781942`) đều đỏ ở job `check (windows-2025)`; e2e macOS xanh.
- 0/24 spec e2e chạm tính năng AI.
- Tức là streaming, lô, huỷ, lỗi, token và bố cục mới chưa chạy trên Windows lần nào. Việc dựng cổng CI đã được yêu cầu 4 lần: E1 B7 → E3 AI-8 → E5 AI-9 → E6 AI-1.

**A8. Quy ước commit mất dấu story.**
- 5/17 commit không quy được về story nào: `df5e576`, `77923f0`, `5a23410`, `c7d5cb3`, `93fe113`.
- Riêng 4-7: agent đã commit đúng khuôn là `4d64a70` lúc 16:05 (`git reflog`). 12 phút sau có lệnh `reset: moving to HEAD~1`, rồi commit lại thành hai commit tiêu đề tiếng Anh, không có mã story. Commit mang 8.025 dòng của 4-7 vì thế mất khỏi `git_evidence`.
- Không có hook `commit-msg` nào (`.githooks/` chỉ có `pre-push`).

**A9. Action item của các retro trước: 0/43 mục mở được đóng trong Epic 4, và 3 mục xấu đi.**
- `trapTab` tăng từ 7 lên 14 bản (E3 AI-11).
- Một tiêu đề sổ nợ `deferred-work.md:11194` gom cả 4-10, 4-11 và 4-12 (lặp lại E3 AI-14).
- Không có cổng CI, dẫn tới A7 (E5 AI-9 / E6 AI-1).
- Chi tiết ở mục Previous-retro follow-through.

### B. Góc nhìn tổng hợp trên mã

- **Tệp vượt 1.000 dòng trong epic: 3 tệp sản phẩm và 5 tệp test.**
  - Sản phẩm: `commands/aitranslate.rs` 1.013 · `PromptLibraryOverlay.vue` 1.001 · `layout/WorkspaceDock.vue` 737 → 1.113.
  - `src/commands/index.ts` vốn đã là god-file, nay 3.942 dòng, Epic 4 thêm 428.
  - Test: `ai_translate_contract.rs` 2.506 · `ai_boundary.rs` 523 → 2.011 · `aiPromptInspector.test.ts` 1.273 · `ai_rag_contract.rs` 1.151 · `aiTranslateBatch.test.ts` 1.059.
  - Không có cổng nào canh kích thước tệp. Nguồn: `retro/30-aggregate.md` §1.
- **Tỉ lệ test:sản phẩm:** khu AI dịch 2,11 · layout 2,47 · prompt set 0,53 · toàn epic 1,40.
- **Trùng lặp:** 4-9 dựng đường lô song song với đường đơn của 4-8 mà không rút hàm chung.
  - Năm chỗ chép: `prepare_translate_call`/`prepare_batch_call` (`aitranslate.rs:242-312` / `:362-448`); khối `record_state_missing` và `generation_state_missing` giống từng byte ở hai wire (`:805-816`/`:903-914`, `:842-852`/`:940-950`); `cancelAiTranslate` (`aiTranslateState.ts:153`) và `cancelAiTranslateBatch` (`aiTranslateBatchState.ts:288`).
  - `resolve_two_tiers` có 3 bản; `strip_bom` có 2 bản trong cùng module mới (`exchange.rs:49`, `exchange_io.rs:29`).
  - Chỗ dùng chung đúng để đối chứng: ánh xạ lỗi `openai_client_error_family` và định dạng chi phí được 4-10/4-11 dùng chung.
- **Đối chiếu spec:** 0 FR bị rơi im lặng (FR65–77, FR79, NFR11 đều có spec nhắc tới; đây là kiểm theo tên). Các AC còn dở:
  - 4-2 "test connection": chủ chuyển 4.2 → 4.8 → "story chưa có số" (`deferred-work.md:10321-10335`).
  - 4-7 AC5 và 4-8 AC8 vế 2 (đọc CI trên push), khớp A7.
  - 4-11: 2 kiểm tay chưa chạy lần nào.
  - 4-12 AC3: bị Ruling 1 của Ice thay thế, xem A4.

### C. Review mã ở ranh giới giữa các story (bmad-review, chỉ mã sản phẩm)

Phạm vi đã thu hẹp: chỉ `src-tauri/src/**` và `src/**`, bỏ test. Ba lens: adversarial, edge-case, verification-gap. Agent đã sửa lại toạ độ sai của reviewer. Tôi tự kiểm lại tại HEAD những mục ghi CONFIRMED.

| # | Mức | Ranh giới | Phát hiện | Nguồn | Kiểm |
|---|---|---|---|---|---|
| C1 | Cao | 4-9 ↔ 4-10 | "Thử lại" lô gọi `runAiTranslateBatch(retryIds)`, và hàm này gán `rows.value = ids.map(…)` ⇒ mọi hàng `done` trước đó (text và usage) biến khỏi danh sách, không đưa sang Editor được nữa | `aiTranslateBatchState.ts:183, 217-220` | CONFIRMED |
| C2 | Cao | 4-8 ↔ 4-9 | `promoteAiTranslate` ưu tiên kết quả ĐƠN cũ trước kết quả LÔ ở câu đang có con trỏ. Không nơi nào gọi `resetAiTranslate` khi lô bắt đầu ⇒ sau một lượt dịch đơn, bấm chốt ở câu khác sẽ ghi bản dịch cũ vào câu cũ | `main.ts:1011-1031`; `resetAiTranslate` chỉ có ở `aiTranslateState.ts:174` | PLAUSIBLE (đã đọc mã, chưa chạy) |
| C3 | Cao | 4-12 ↔ applyMerge | `undoMerge` không kiểm panel còn đang gộp trước khi cưỡng chế dời chỗ ⇒ xoá thao tác tay của người dùng | `WorkspaceDock.vue:657` | PLAUSIBLE |
| C4 | Cao | 4-9 ↔ 4-10 | Lô panic giữa chừng trả lỗi không mang `segment_id` ⇒ hàng `running` kẹt vĩnh viễn | `aitranslate.rs:178-183` ↔ `aiTranslateBatchState.ts:251-262` | PLAUSIBLE |
| C5 | Trung bình | 4-11 | Khi provider chỉ báo `total_tokens`, `prompt_tokens`/`completion_tokens` bị đúc `0` (`unwrap_or(0)`), trông như "đo được và bằng 0". `cost_usd` thì đúng là `None`. Đúng lớp lỗi "0 mang hai nghĩa" | `client.rs:314-315` | CONFIRMED (hình dạng) |
| C6 | Trung bình | 4-2/4-3 ↔ 4-8 | `promote_ai_translation` không kiểm segment thuộc Chương đang mở như `prepare_translate_call` có kiểm ⇒ rủi ro ghi chéo khi IPC đua nhau | `retro/40-code-review.md` Nhóm 2 | PLAUSIBLE |
| C7 | Trung bình | 4-12 | `togglePanel` không đánh giá lại merge khi ẩn/hiện trong cùng tầng; Map `hidden` không được dọn khi `undoMerge` chạy | `retro/40-code-review.md` Nhóm 3 | PLAUSIBLE |
| C8 | Thấp | 4-8 | Adapter `promoteAiTranslation` bỏ qua kiểm hình dạng dữ liệu runtime, khác mọi adapter khác của Epic 4 (`src/AGENTS.md`) | `config/segment.ts:948-957` | PLAUSIBLE |

- Sạch: AD-47 (promote ghi cùng lúc `target_text` và cột origin), RAG ↔ prompt inspector, config ↔ keychain ↔ lượt gọi thật.
- Bị loại vì đã là nợ có chủ: `provider` text tự do (AD-2); Global AI config không sửa được sau khi mở Tác phẩm.

## Behavior verification

- **Retro này KHÔNG chạy lại các luồng AI end-to-end.** Chúng cần một provider thật cùng API key trong keychain, và retro không có quyền đó.
- Bằng chứng hành vi hiện có:
  - Ice đã nhìn bố cục 4-12 trên bản build thật (`ff1fd6aa`, 2026-09-23T08:30Z: *"đã ổn, kết thúc đo được rồi"*).
  - Bộ e2e có 0 spec cho tính năng AI.
  - 4-11 có 2 kiểm tay chưa chạy: dashboard provider thật, `stream_options` với server local.
  - 5 story (4-8 → 4-12) chưa qua CI.
- ⇒ Các luồng dịch đơn, lô, huỷ, lỗi/thử lại và token hiện chỉ được chứng minh bởi test đơn vị và hợp đồng trên macOS. C1 và C2 là hai lỗi mà một lượt dùng tay 5 phút sẽ làm lộ.

## Previous-retro follow-through

Nguồn: `sprint-status.yaml action_items`. 43 mục chưa `done`: E1 = 5 · E2 = 8 · E3 = 15 · E5 = 7 · E6 = 6 · E10 = 2. Chi tiết từng mục, kèm bằng chứng, ở `retro/30-aggregate.md` §6. Dưới đây là các mục của hai retro gần nhất.

| id | Đã làm? | Bằng chứng | Trạng thái đề xuất |
|---|---|---|---|
| `epic-6-retro-item-60-ai-1-dung-cong-trang-thai-ma-ai-9-retro` | Không | `.githooks/pre-push` không có logic đọc lượt schedule; A7 là tái diễn nặng hơn | giữ `open` |
| `epic-6-retro-item-64-ai-2-mot-moc-e2e-thay-hop-thoai-chon-tep` | Không | Epic 4 thêm 0 spec e2e | giữ `open` |
| `epic-6-retro-item-65-ai-3-luot-nghiem-thu-tay-co-chu-cho-cac` | Không | không có bản ghi | giữ `open` |
| `epic-6-retro-item-67-ai-6-tach-commands-project-rs-2-044-6-63` | Một phần | stage 1 `cc8b5f0`: `mod.rs` 4.556 / `wire.rs` 1.264 / `tests.rs` 894; stage 2 chưa làm | **`in-progress`** |
| `epic-6-retro-item-68-ai-8-hoa-giai-quy-uoc-tieu-de-commit-sua` | Một nửa | luật gạch có từ `5097f07`; không có hook `commit-msg`; Epic 4 vi phạm 5 lần | giữ `open` |
| `epic-6-retro-item-69-ai-10-dong-mui-tham-do-bang-ma-cap-fixtu` | Không, theo quyết định của Ice | 🔵 2026-09-15 | giữ `open` |
| `epic-5-retro-item-58-ai-9-doi-nhip-doc-ci-mot-luot-gh-run-lis` | Không | 6 commit chưa push (A7) | giữ `open` |
| `epic-5-retro-item-56-ai-7-anh-xa-5-commit-khong-ten-story-95b` | Không | grep 5 hash trong `epic-5-context.md` = 0 | giữ `in-progress` |
| `epic-5-retro-item-54-ai-5-chan-doan-10-spec-e2e-do-va-dua-bo` | Một phần | e2e macOS xanh ở các lượt 09-18/09-21/09-22; cái đỏ là `check (windows-2025)` | giữ `in-progress` |
| `epic-5-retro-item-50/53/55/59` | Không | `retro/30-aggregate.md` §6.2 | giữ nguyên |

15 mục của E3 vẫn `open` kể từ 2026-09-03. `AI-1/35` (B6 "chặn AC5") có tiền đề đã sai từ 2026-08-18, vì AC5 của Story 2.9 đã rút. Nó nên được đóng bằng 🔵, và đó là quyết định của Ice.

## Action items

Tất cả là ĐỀ XUẤT. Retro không tự sửa mã, không tự sửa spec hay skill.

| # | Hành động | Chủ | Loại |
|---|---|---|---|
| 1 | Viết lại mục Verification trong mẫu spec và `bmad-build`: mỗi pha chỉ chạy `npm run test:story <id>`; full suite đúng MỘT lần ở cuối story, qua chính `git push`/pre-push. Gỡ câu "Run all fourteen before calling a phase done", gỡ lượt đo nền full ở đầu story. Cấm subagent chạy `.githooks/pre-push` bằng tay | Ice duyệt → dev (override `_bmad/custom/bmad-build.toml` + spec-template) | quy trình |
| 2 | Bỏ sàn N của Blind Hunter, cho phép trả "0 phát hiện". Phát hiện loại nit/tài liệu gom vào MỘT danh sách để Ice xem, không vá tự động. Vòng rà thứ hai chỉ chạy khi vòng một có lỗi sản phẩm thật | Ice duyệt → dev (override `bmad-build`, `bmad-code-review`) | quy trình |
| 3 | Push 6 commit, đọc CI trên cả hai nền tảng, rồi mới ký 4-8 → 4-12. Nếu Windows đỏ thì ghi lý do | Ice | chặn nghiệm thu |
| 4 | Story sửa lỗi cho C1, C2 (Cao, lộ bằng tay), C3–C5 và 3 lỗi 4-10 bị bỏ rơi (A3). Mỗi mục cần một ca đỏ trước khi vá | dev qua `bmad-build`; Ice xếp lịch | remediation |
| 5 | Ngưỡng giao diện là hạt giống cấu hình, không phải AC đo đạc, cho tới khi giao diện ổn định. Không mở mục nợ cấp pixel (kiểu 38 so với 40px) đẩy về Ice. Đề xuất chỉnh lại AC3 của 4-12 và UX-DR15 qua `bmad-correct-course` | Ice quyết → PM/UX | đối chiếu spec |
| 6 | Mẫu lời giao cho subagent phải mang giới hạn thời gian và sentinel `trap … EXIT` cho mọi watcher nền; luồng chính kiểm `ps` trước khi đóng pha | dev (mẫu bàn giao pha) | quy trình |
| 7 | Hook `commit-msg` chặn tiêu đề thiếu `type(scope)` hoặc thiếu id story `N-M`. Không `reset` rồi commit lại với tiêu đề khác mà bỏ id | dev; Ice duyệt | cổng |
| 8 | Rút hàm chung cho đường đơn và đường lô (5 chỗ ở B), trước khi thêm tính năng AI tiếp theo | dev | nợ |
| 9 | Một lượt Ice triage 44 mục nợ "Chủ: Ice" của Epic 4 và 15 mục E3; tách tiêu đề `deferred-work.md:11194` | Ice | nợ |

## Acceptance verdict

- **Phán quyết máy: `rejected`.** Tiêu chí được suy từ diff và story, không phải tiêu chí epic khai trước.
- Bằng chứng:
  1. 12/12 story chưa `done`, đều ở `review`: `4-1-module-ai-cô-lập-và-test-cưỡng-chế-ranh-giới` · `4-2-cấu-hình-nhà-cung-cấp-ai` · `4-3-api-key-trong-keychain` · `4-4-bộ-prompt-theo-thể-loại` · `4-5-xuất-và-nhập-bộ-prompt` · `4-6-smart-rag-injector-là-một-hàm-thuần` · `4-7-xem-prompt-cuối-cùng-đã-gửi` · `4-8-dịch-một-segment-với-kết-quả-chảy-dần` · `4-9-dịch-theo-lô-và-huỷ-giữa-chừng` · `4-10-lỗi-mạng-và-lỗi-api` · `4-11-số-token-và-ước-tính-chi-phí` · `4-12-bố-cục-màn-hình-hẹp-và-hiệu-chỉnh-ngưỡng`.
  2. Phát hiện chặn: 5 story chưa từng qua CI (A7), cộng C1 là lỗi mất dữ liệu hiển thị trên đường chính.
- Về năng lực, epic đã dựng đủ: 0 FR bị rơi, AD-47 sạch, ranh giới AD-13 có cổng canh.
- **Quyết định của Ice (2026-09-23): giữ `rejected` cho tới khi vá xong C1 và C2** (xem Open questions Q1).

## Open questions

Ice trả lời ngày 2026-09-23:

- **Q1 → giữ `rejected`** cho tới khi vá xong hai lỗi C1 (Thử lại lô xoá hàng `done`) và C2 (chốt bản dịch ưu tiên kết quả đơn cũ). Điều kiện Ice đặt chỉ gồm hai lỗi này. Việc push và đọc CI (action item 3) vẫn nằm trong danh sách đề xuất, nhưng không phải điều kiện của phán quyết.
- **Q2 → phương án (a):** giữ luật chia ~4 pha, cắt bài đọc bắt buộc của mỗi pha (spec gọn, mỗi pha chỉ đọc phần §Code Map của mình). Chưa sửa AGENTS.md trong retro này.
- **Q3 → chưa trả lời:** không chuyển trạng thái action item cũ nào trong `sprint-status.yaml`.
- **Q4 → bỏ qua:** 9 action item ở trên chỉ nằm trong tài liệu này, không ghi vào `sprint-status.yaml`.
- **Q5 → bỏ qua:** các tệp `retro/10-…`, `20-…`, `30-…`, `40-…` là tệp tạm trong scratchpad của phiên retro, KHÔNG được giữ lại. Những phần trích trong tài liệu này là bản còn lại duy nhất; mọi con số chịu tải đều đã kèm nguồn gốc (commit, file:line, sid + giờ UTC) để kiểm lại độc lập.
