---
epic: 6
date: 2026-09-15
verdict: rejected
criteria: declared
headless: false
---

# Retrospective — Epic 6: Đường nhập (mọi nguồn văn bản vào được, và không hỏng im lặng)

> Tài liệu làm việc. Các mục ghi "đang làm" được điền khi phase tương ứng xong.

---

## Epic summary

| Mục | Giá trị |
|---|---|
| Dải diff | `193ec73..1eb781b` (`193ec73` là `baseline_commit` của spec 6.1; commit story đầu `bb1e91f`) |
| Số commit | 38 · 0 merge |
| Tệp chạm | 202 · +67.314 / −3.355 dòng |
| Commit quy nạp được về story | 24/38 (id dạng `6.1`) · 0/38 (id dạng `6-1`) |
| Commit không thuộc story nào | 14 — 3 `fix(e2e)` (AI-5 retro Epic 5), 1 `test(store)` trần WAL, 5 `docs`, 1 `feat(dev-loop)`, 1 `chore(party-mode)`, 1 `fix(naming-boundary)`, 1 `docs(spine)` NFR15, 1 commit không tiền tố (`5097f07`) |
| Story `done` | 18/19 — Ice duyệt từ `review` lên `done` trong lượt này (2026-09-15, thay đổi chưa commit ở `sprint-status.yaml`) |
| Story CHƯA xong | `6-18-đo-lại-nfr3-nfr4-nfr5-trên-thư-viện-5-000-chương-thật` — `in-progress`, Ice chọn tạm ngưng |
| Commit chưa đẩy lên `origin` | `6d68dce`, `7863afb`, `1eb781b` |

Ice đã xem danh sách chưa xong ở câu hỏi cổng (2026-09-15) và chọn chạy retro, kèm quyết định:
18 story `review` được duyệt lên `done`, còn Story 6.18 **tạm ngưng để đó**. Ice cũng chọn dải
diff tới HEAD `1eb781b`, và muốn soi kỹ hai vùng: **chỗ nối pipeline nhập** và **mạng / URL**.

### Kho bằng chứng đã đọc

| Nguồn | Có | Ghi chú |
|---|---|---|
| Đặc tả epic | Có | `epics.md:935-951` (tóm tắt, FR/NFR, ghi chú AD-39..AD-42) + `:4476-5360` (thân 19 story) — **có tiêu chí khai báo** |
| Tệp story | 19/19 | Dạng `spec-6-*.md`; không có tệp `6-*.md` rời. Thư mục bàn đo `6-1-ban-do/`, `6-12-ban-do/`, `6-18-ban-do/` |
| Bối cảnh epic | Có | `epic-6-context.md` |
| Diff + commit | Có | `git_evidence.py --range 193ec73..1eb781b` |
| Sprint status | Có | `sprint-status.yaml` |
| Retro trước | Có | `epic-5-retro-2026-09-03.md` + 10 mục `epic-5-retro-item-50…59` |
| Sổ nợ | Có | `deferred-work.md:9191-12001` (các mục Epic 6) |
| CI thật | Có | 12 lượt `schedule` (2026-09-03 → 09-14) + 7 lượt `push`, đọc bằng `gh run` |
| Nhật ký phiên | Có, chưa đọc | 121 tệp `.jsonl` trong thư mục phiên của dự án, nhiều tệp nằm trong cửa sổ 2026-09-03 → 09-15 (1–3,7 MB mỗi tệp) |

### Phạm vi bị thu hẹp, ghi ra thay vì giấu

- **`bmad-review` không chạy trên toàn dải 67 nghìn dòng.** Ba lens (edge-case, verification-gap,
  adversarial, lấy từ `.claude/skills/bmad-review/references/`) được giao cho hai subagent chỉ đọc,
  mỗi agent **một vùng chỗ nối** Ice chọn: pipeline nhập (6.2–6.6, 6.9, 6.10a, 6.10, 6.12, 6.13,
  6.16, 6.17) và mạng/URL (6.1, 6.7, 6.8, 6.9, 6.11, 6.12, 6.15). Lens adversarial được lệnh
  **không** độn cho đủ số tối thiểu. Mã ngoài hai vùng đó — `role.rs`, `anchor.rs`, `encoding.rs`
  từng dòng, 2.433 dòng `ImportPreviewOverlay.vue` — chỉ được đọc qua chỗ gọi.
- **Bảng đối chiếu AC từng story là sản phẩm của subagent**, được lấy mẫu kiểm lại (tên ca test,
  số dòng, các khẳng định vào F1–F9), không đọc lại toàn bộ. Cả hai agent đối chiếu đều kết luận
  "0 lệch im lặng", và F4 là một phản ví dụ nằm **trong** phạm vi của agent thứ hai (có cả 6.10 lẫn
  6.16). Cách đối chiếu từng AC trên mã của chính story ấy không đem một AC cắt ngang ra so với
  đường song song dựng sau nó. Nên câu "0 lệch im lặng" của hai bảng chỉ có nghĩa là "không lệch
  trong phạm vi từng story", không phải "không lệch trong epic".
- **Không agent nào chạy test hay build.** Mọi câu "đối chứng đỏ", "đột biến cho đỏ" trích từ spec
  là lời khai của phiên dựng story, không được chạy lại ở đây.
- **Nhật ký phiên chỉ được đếm, không được đọc.** 121 tệp `.jsonl`; phép đếm lệnh `gh run`/`git
  push` lọc theo **ngày sửa tệp** và không tính transcript của subagent. Phiên ngày 09-14 có thể lẫn
  với phiên khác nên không dùng số của ngày đó. Phần "bài học quy trình" vì vậy rút từ commit, tệp
  spec, CI và sổ nợ.
- **Đường URL, `.txt`, `.docx`, song ngữ không được chạy trên ứng dụng thật** (§Behavior
  verification). F3 là cơ chế đọc từ nguồn, chưa đo lúc chạy.
- **Chưa đo Windows.** Mọi lượt chạy ở đây trên macOS 15.7.9 của Ice; số Windows chỉ đến từ log CI.
- **Mục action item của Epic 1, 2, 3, 10 chưa kiểm từng mục** (§Previous-retro follow-through).

---

## Behavior verification

### Lượt chạy 1 — trọn bộ e2e trên HEAD, máy Ice

```
HEAD=1eb781b start=2026-09-15 09:26:42 +0700 load={ 4.09 4.33 4.54 }
Spec Files:	 24 passed, 24 total (100% completed) in 00:02:36
rc=0 end=2026-09-15 09:29:49 +0700 load={ 5.20 5.10 4.84 }
```

Cây sản phẩm sạch lúc chạy (`src`, `src-tauri`, `e2e`, `tests`, tệp cấu hình gốc); thứ duy nhất bẩn
là `sprint-status.yaml` do Ice đang duyệt story, không vào nhị phân.

Lượt này cho phép kết luận hai điều. Bản vá cách ly e2e (`6d68dce`, `7863afb`) cho 24/24 ngoài
phiên đã viết nó, n = 1. Và hai spec đi qua form tạo Tác phẩm (`story-5-4`, `story-5-5`, qua
`e2e/support/importForm.mjs`) chạm được `ImportPreviewOverlay.vue` trong WKWebView thật: mở → chờ
xác nhận bật → xác nhận → đóng.

Nó **không** cho phép kết luận ba điều:
- **Không phải CI.** Hai bản vá chưa đẩy; lượt `schedule` gần nhất (`34898445729`, 2026-09-14) vẫn
  16/8. Máy Ice là macOS 15.7.9, runner là `macos-26`.
- **Không nói gì về luồng của Epic 6.** 24 spec đều là spec Epic 1–5; Epic 6 thêm **0** spec
  (`git log --name-status 193ec73..1eb781b -- e2e/specs` chỉ có 3 dòng `M`). Hai spec đi qua màn
  xem trước dán đúng **một ký tự** `'x'` (`importForm.mjs:167`) — không bảng mã, không mẫu Chương,
  không luật làm sạch, không khối.
- **Không có đường lái cho bốn trong năm nguồn.** Kho chỉ có hai móc e2e
  (`AURATRANSLATE_E2E_DATA_DIR`, `AURATRANSLATE_E2E_LIBRARY_ROOT` — grep `src-tauri/src`), không có
  móc nào thay hộp thoại chọn tệp. Nên `.txt`, `.docx`, song ngữ `.csv/.tsv` và danh sách URL
  không có đường nghiệm thu tự động nào qua giao diện thật.

### Lượt chạy 2 — luồng nhập Epic 6 trên ứng dụng thật

Một spec nháp nằm **ngoài kho** (scratchpad của phiên, không commit), chạy bằng
`npm run test:e2e -- --spec <đường dẫn tuyệt đối>`. Nó dùng chính bàn đo cách ly của kho, trên HEAD
`1eb781b` với cây sản phẩm sạch. Mỗi lượt chạy mất khoảng 11 giây, load 4,2–4,6.

**Ca 1 — đường dán: mẫu Chương dạng regex đặt ở màn xem trước, rồi xác nhận.** Văn bản gồm một câu
tựa, ba tiêu đề `第一章 / 第二章 / 第三章` và phần thân. Mẫu `^第.章`, kiểu `regex`, gõ vào
`.ip-chapters-pattern-input`. Sau khi xác nhận, đọc lại bằng `list_chapters`.

```
preview-before-pattern: "Nhận ra 1 Chương" · "Chưa đủ Chương để so"
preview-after-pattern:  "Nhận ra 4 Chương" · "Không có Chương nào cần xem"
                        titles [Không có tiêu đề, 第一章 开始, 第二章 继续, 第三章 结束]
compare:                {"previewCount":4,"writtenCount":4}
```

Số Chương ở màn xem trước bằng số hàng ghi xuống, tiêu đề khớp từng dòng.

Điều kiện đo phải ghi ra: lượt đầu dùng một fixture **sai**. Ba dòng thân mở đầu bằng `第一章的…`,
nên chúng khớp đúng mẫu `^第.章`, và lượt đó cho **7 == 7**. Ba "Chương" thừa hiện rõ tiêu đề trong
danh sách xem trước — đúng AC "chỗ bắt nhầm nhìn thấy được" của Story 6.6. Sản phẩm làm đúng điều mẫu
nói; bất biến "xem trước bằng ghi xuống" đứng ở cả hai lượt. Lượt thứ hai, thân không mở đầu bằng
`第X章`, cho 4 == 4.

**Ca 2 — luật "ô chỉ khoảng trắng ⇒ NULL" của xuất xứ Chương (F7), đo trong chính tiến trình ứng dụng.**

```
origin-trim: {"jsTrimOfFeffIsEmpty":true,"jsTrimOfSpacesIsEmpty":true,
              "authorCodepoints":[65279],"siteName":null}
```

WKWebView coi `'﻿'` là rỗng. `update_chapter_origin` của Rust ghi nguyên `U+FEFF` xuống
`chapter.origin_author`, trong khi `'   '` về `NULL`. Hai lượt chạy, cùng kết quả. Ca này gọi IPC
trực tiếp nên chỉ đo vế Rust của bản `chapter.rs:582`. Bản `project.rs:3862` ở đường xác nhận URL có
cùng mã `str::trim()` nhưng không chạy được ở đây.

**Chưa chạy, và vì sao:**
- **Đường URL** — cần mạng thật hoặc một máy chủ HTTP cục bộ. Kho đã ghi LuLu chặn máy chủ cục bộ
  của `cargo test` (`c145d3f`); chưa ai đo điều đó với nhị phân ứng dụng, và lượt này không thử. Vì
  vậy **F3 chưa được đo lúc chạy**.
- **`.txt`, `.docx`, song ngữ** — không có móc thay hộp thoại chọn tệp (F2).
- **Luật làm sạch, sửa ranh giới khối, đổi bảng mã** — không được thao tác trong ca 1.

---

## Findings

Mười phát hiện, xếp theo mức hệ quả. Mỗi mục có nguồn trỏ được và một phán quyết. Phát hiện do
subagent nêu đã được đối chiếu lại với nguồn gốc trước khi vào đây; một vế không đứng vững đã bị
bỏ (ghi ở F10).

### F1 — Trạng thái story được quyết mà không lượt nào đọc CI, trong một epic CI đỏ từ đầu tới cuối

**Góc nhìn:** quy trình. **Lần thứ tư** cùng một cơ chế (retro Epic 1 §B7, retro Epic 3 AI-8,
retro Epic 5 F4/AI-9).

| Dữ kiện | Nguồn |
|---|---|
| 18/18 story được đặt `review` **ngay trong commit tính năng của chính nó**, tức trước khi có bất kỳ lượt CI nào để đọc | `git log -G "^  <khoá>.*: review" -- sprint-status.yaml`: từ `bb1e91f` (6.1) tới `3c25e9a` (6.17) |
| Lượt `schedule` đỏ **12/12 đêm**, 2026-09-03 → 09-14 | `gh run list --workflow=ci.yml`, ví dụ `33802182437` … `34898445729` |
| `check (windows-2025)` đỏ ngay lượt `push` đầu tiên của epic (`62ddf8a`, 09-04, run `33833753206`) và đỏ tiếp tới `c145d3f` (09-11, run `34560064976`), cùng một ca `store_contract::the_wal_stops_growing_once_it_crosses_the_threshold` | `gh run view --log-failed` |
| HEAD của lượt `schedule` đứng yên ở `b69a345` (Story 6.5) sáu đêm liền 09-05 → 09-10: mười story 6.6 → 6.15 chỉ nằm trên máy, lên `origin` ngày 09-11 | cột `headSha` của `gh run list` |
| Nhật ký phiên 09-03 → 09-10 (mọi phiên dựng 6.1 → 6.15): **0** lệnh `gh run` | grep trên các tệp `.jsonl` theo ngày sửa (giới hạn ở §Phạm vi) |
| Bản vá trần WAL `574c869` xanh đúng một lượt `push` (`34737934378`, 09-13), rồi đỏ ở mệnh đề 2a `store_contract.rs:890` trên **Windows** đêm 09-13 (`34780086488`, đỉnh +160.680 B) và trên **macOS** đêm 09-14 (`34898445729`, +16.480 B) | `gh run view --log-failed` |
| Hai lượt đỏ sau vá **chưa được ghi ở đâu** | grep hai run ID và hai con số trên `_bmad-output`, `src-tauri/tests`, `e2e` = 0; không commit nào chạm ca này sau `574c869` |
| Chính spec của bản vá đã nói trước: *"A strict 'the peak did not rise at all' may be red on `macos-26` CI"* (`spec-ca-wal-do-tren-windows.md:29`), và để bước "CI phải xanh" lại cho CI (`:144`) — trong khi frontmatter ghi `status: 'done'` | tệp spec |
| Hai bản vá e2e (`6d68dce`, `7863afb`) và `1eb781b` chưa đẩy. Lượt duyệt 18 story lên `done` hôm nay diễn ra khi lượt `schedule` gần nhất đỏ cả `check` lẫn `e2e` | `git log origin/master..HEAD`; run `34898445729` |

**Phán quyết: fix now** — AI-1. Retro Epic 5 đã đề xuất đúng một cổng (AI-9) và viết rõ *"cần
một CỔNG, không một lời nhắc"*. Cổng chưa được dựng — `ci.yml:67` là một dòng chú thích — và hình
dạng lỗi lặp lại trọn vẹn.

*Bài học ngược dòng:* `review` đang được ghi **cùng commit** với mã, nên nó đo "agent đã dừng",
không đo "mã đã được một cổng ngoài máy nhìn thấy". Chừng nào hai việc còn nằm chung một commit,
không lời nhắc nào tách được chúng.

### F2 — Epic "bề mặt đầu tiên người dùng chạm" thêm 0 spec e2e, và bốn trong năm nguồn không có đường nghiệm thu qua giao diện thật

**Góc nhìn:** verification-gap. `epics.md:937` gọi đường nhập là *"bề mặt đầu tiên người dùng
chạm vào sản phẩm"*.

- `git log --name-status 193ec73..1eb781b -- e2e/specs` chỉ có 3 dòng `M` (sửa spec Epic 5), 0 dòng `A`.
- Kho có đúng hai móc e2e (`AURATRANSLATE_E2E_DATA_DIR`, `AURATRANSLATE_E2E_LIBRARY_ROOT`), không
  móc nào thay hộp thoại chọn tệp ⇒ `.txt`, `.docx`, `.csv`/`.tsv` không lái được bằng WebDriver;
  đường URL cần mạng thật.
- Hai spec duy nhất đi qua `ImportPreviewOverlay.vue` dán đúng một ký tự `'x'`
  (`e2e/support/importForm.mjs:167`).
- Các phép kiểm tay mà chính spec ghi "CHƯA LÀM": `.docx` hỏng trên bản `--release`
  (`panic="abort"`) — lý do tồn tại của cả bộ đọc OOXML tự viết (spec 6.12 §Manual checks);
  `.atproj` chép sang máy ngoại tuyến vẫn đủ ảnh, và vị trí ảnh thật trong WKWebView (spec 6.14).
- Cùng lớp với retro Epic 3 AI-8 (*"grep e2e cho Glossary = 0"*).

**Phán quyết: fix now** — AI-2, AI-3.

### F3 — Lượt xác nhận nhập URL tải ảnh qua mạng bằng một lệnh đồng bộ, nằm ngoài cổng "không chặn luồng chính"

**Góc nhìn:** chỗ nối mạng (6.7 ↔ 6.11). Subagent rà mạng nêu; đã đối chiếu lại nguồn, kể cả mã
nguồn `tauri-macros`.

- `confirm_import_with_encoding` (`commands/project.rs:5984`) và `confirm_bilingual_import` (`:6186`)
  khai `#[tauri::command]` trơn. Hai lệnh cùng tệp có gọi mạng — `start_url_import` (`:6268`),
  `reload_url_import_item` (`:6310`) — khai `#[tauri::command(async)]`, kèm chú thích `:6265` giải
  thích vì sao.
- Đường gọi: `confirm_import_with_encoding` → `create_work` → `prepare_chapter_images` (`:975`) →
  vòng tuần tự `fetch_and_write_one_asset` (`:1135`) → `webimport::fetch(…, ResourceKind::Image)`
  (`:1282`); mỗi lượt chờ tới `REQUEST_TIMEOUT` 20 s.
- `tauri-macros-2.6.3/src/command/wrapper.rs:249-265`: hàm không `async` và không cờ `(async)` đi
  `body_blocking`; có cờ thì đi `"sync_threadpool"`.
- Cổng `config_invariants.rs::the_blocking_wires_run_off_the_main_thread` (`:987`) canh bằng một
  **danh sách tay** (`:1002-1083`, chỉ `glossary.rs`, `library.rs`, `lifecycle.rs`, `chapter.rs`).
  `commands/project.rs` không có mặt; lần nhắc duy nhất của nó trong tệp (`:1196`) thuộc một cổng
  khác (`resolve_library_root`).
- Sổ nợ đã ghi vế trải nghiệm — *"Vòng lặp tải là TUẦN TỰ … pha ảnh thì không có gì"*, không tiến
  độ, không huỷ (`deferred-work.md:11083-11087`, chủ Ice) — nhưng **không** ghi rằng pha ấy chạy
  trên luồng chính.

Hệ quả suy ra từ nguồn, **chưa đo lúc chạy** (đường URL cần mạng, xem §Behavior verification): một
lượt nhập URL có N ảnh ở host chậm hoặc chết làm đơ cả cửa sổ tới N × 20 s, trong khi vẫn giữ khoá
`PendingImportSourceState`.

**Phán quyết: fix now** — AI-4.

*Bài học ngược dòng:* một cổng liệt kê tay chỉ canh những tệp đã có mặt lúc nó được viết. Tệp lớn
nhất và nhiều lời gọi mạng nhất của epic nằm ngoài danh sách từ đầu.

### F4 — Bản xem trước song ngữ nhiều Chương không có "cần xem" (FR132), và không ai quyết điều đó

**Góc nhìn:** đối chiếu spec (6.10 ↔ 6.16). Lệch **im lặng** duy nhất lượt này tìm thấy. Subagent
đối chiếu spec cho 6.10a–6.17 có **cả hai** story trong phạm vi mà vẫn báo "0 lệch im lặng", vì nó
đọc AC của từng story trên mã của chính story ấy. Không bước nào hỏi AC của 6.10 có áp lên đường mà
6.16 mở ra sau hay không. Lệch này lộ ra ở subagent rà chỗ nối pipeline, vì nó được hỏi đích danh
"song ngữ và đơn ngữ: gì áp giống, gì lệch" — và agent ấy còn tự hạ nó xuống "quan sát". Đã đối
chiếu lại nguồn.

- AC đầu của Story 6.10: *"Given một lần nhập nhiều Chương … Then đầu màn hình luôn hiện hai con
  số"* (`epics.md:4971-4973`) — không loại trừ nguồn nào. Story 6.16 dựng đúng một lần nhập nhiều
  Chương cho song ngữ (tách theo mẫu).
- Nhánh song ngữ dựng mọi `ImportedChapter` với `cleanup_report: None` và `joined_line_count: None`
  (`core/segment/pipeline.rs:1006-1012`); `BilingualImportPreviewOverlay.vue` và
  `bilingualImportPreviewState.ts` có 0 lần nhắc `needs_review` / `cần xem`.
- `spec-6-10-*.md`, `spec-6-10a-*.md` không nhắc song ngữ; `spec-6-16-*.md`, `spec-6-17-*.md` không
  nhắc 6.10, FR132 hay "cần xem"; `deferred-work.md` không có mục nào.
- Hai trong năm nguyên nhân vẫn có nghĩa với tệp song ngữ: bảng mã tin cậy thấp, và luật làm sạch
  xoá quá nhiều.

**Phán quyết: defer, cần quyết định của Ice** — AI-5. 6.10 đi trước 6.16 nên không story nào sai
riêng; đây là một chỗ nối không ai đứng ở cả hai phía.

*Bài học ngược dòng:* một story mở đường song song (6.16) sau một tính năng cắt ngang (6.10) nên
liệt kê tường minh những AC cắt ngang nào nó thừa kế, và những AC nào không.

### F5 — Hai tiêu chí khai báo của epic chưa đạt: độ chính xác dò bảng mã chưa đo, và NFR3/4/5 chưa đóng

- **Mũi thăm dò bảng mã.** `epics.md:948` đặt `chardetng` + `encoding_rs` là một trong hai mũi
  thăm dò "ngay đầu epic"; AC Story 6.1 đòi đo và ghi độ chính xác trên GBK/Big5 thật
  (`epics.md:4500-4502`). Tới cuối epic: ca đo `webimport_probe.rs:260` vẫn `#[ignore]` và thoát đỏ
  trên 0 mẫu; thư mục `fixtures/encoding/` không tồn tại; `deferred-work.md:9191` và `:9415` ghi
  *"CHƯA có số đo (0 mẫu)"*, chủ Ice, không có mục đóng. Tiêu đề `bb1e91f` tự nói: *"vế bảng mã
  ghim được nhưng chưa đo được"*.
- **NFR3/4/5.** `epics.md:941` khai chúng "đóng ở Story 6.18". 🔵 *(2026-09-15 — dòng ấy đã sửa: NFR3/4/5 dời sang Epic 10, Story 10.9, và Story 6.18 gộp vào đó qua `correct-course`.)* Story 6.18 tạm ngưng (quyết định của
  Ice lượt này); Q4 còn mở; số duy nhất đang có vẫn là số sơ bộ của 5.14, đo trên ứng dụng 0 lớp từ
  điển (retro Epic 5 F5). §Decisions của 6.18 cũng đã thu hẹp AC "trên cả macOS lẫn Windows" về
  macOS (`deferred-work.md:11980-12001`, có chủ).
- Cùng họ, đã ghi có chủ: nhị phân release từ **9.370.872 B** lên **13.739.088 B** khi Story 6.7
  thành lời gọi thật đầu tiên của `Fetcher`/`Extractor` (`deferred-work.md:9249-9266`, chủ Story 10.1).

**Phán quyết: defer** (có chủ) — nhưng cả hai được tính vào phán quyết nghiệm thu như tiêu chí chưa đạt.

### F6 — `commands/project.rs` từ 2.044 lên 6.630 dòng trong một epic

**Góc nhìn:** god-class.

- 17/38 commit của dải chạm tệp này. `create_work` một mình chiếm `:354-1420`. Lớp hàm thuần trộn
  sáu mối quan tâm: tạo Tác phẩm, bảng mã, song ngữ, khối tầng 2, sổ nhập URL, xuất xứ Chương.
- Tệp vẫn đúng chữ luật hai lớp `src-tauri/AGENTS.md:13` (vỏ `wire` mỏng: 15 lệnh). Nhưng mọi
  logic mới khác của epic đều vào `core/segment/`, `core/webimport/`, `core/docx/`; riêng phần điều
  phối nhập dồn vào `commands/`.
- `AGENTS.md:19` đã gọi tên chính tệp này: 9,4% chi phí token của các agent nặng, và *"No gate
  measures file size today"*.
- Nó cũng là nơi F3 và F7(a) nằm.

**Phán quyết: defer** — AI-6.

### F7 — Luật "ô xuất xứ chỉ có khoảng trắng ⇒ NULL" được viết ba lần, và phía JS với phía Rust lệch nhau đúng ở ký tự kho đã đo

**Góc nhìn:** duplication map + chỗ nối 6.7 ↔ 6.15. Subagent rà chỗ nối pipeline nêu; đã đối chiếu
nguồn; phía chạy thật ở §Behavior verification.

- Hai bản Rust cùng tên `trimmed_or_none` — `commands/project.rs:3862` (xác nhận nhập) và
  `commands/chapter.rs:582` (sửa sau khi nhập) — đều dùng `str::trim()`. Phía JS của màn xem trước
  dùng `.trim()` (`importPreviewState.ts:496`).
- `U+FEFF` là chỗ hai phía lệch, và chính kho đã đo điều đó ở Story 6.7 (`project.rs:4094-4098`) rồi
  vá bằng `trim_like_the_paste_box` (`:4109`). Bản vá không đi sang Story 6.15.
- Hệ quả: ở màn xem trước nhập URL — chỗ duy nhất `ChapterOrigin` hiện trong màn xem trước
  (`ImportPreviewOverlay.vue:1059`, `v-if="importPreviewLastSubmittedFrom === 'urls'"`) — một ô chỉ
  chứa `U+FEFF` hiện "không tìm thấy", nhưng đĩa ghi `U+FEFF`. Sau đó `LibraryMode` hiện một ô trống
  **không** nhãn "không tìm thấy" (`ChapterOrigin.vue:66-67`: placeholder chỉ hiện khi value rỗng) —
  trái AC4 Story 6.15 (*"không bao giờ để trống"*).
- Hai ca canh luật này (`tests/frontend/importPreviewChapterOrigin.test.ts:195`,
  `src-tauri/tests/chapter_origin_contract.rs:348`) chỉ dùng `'   '` — giá trị cho cùng một kết quả
  ở cả hai phía, nên không ca nào canh chỗ lệch.

**Phán quyết: fix now** — AI-7.

*Bài học ngược dòng:* khi một phép đo tìm ra chỗ lệch JS/Rust, bản vá nên là **một** hàm dùng chung
có tên (và một ca test mang đúng ký tự đã đo), không một hàm riêng cho đúng chỗ vừa tìm ra.

### F8 — Quy ước tiêu đề commit của AI-7 retro Epic 5 bị phá 4/4 lần sau khi viết, và tiền đề của nó hẹp hơn kết luận

- Luật ở `AGENTS.md:54`, vào kho ở `5097f07` (2026-09-10 22:06): *"write the story id in its `5-9`
  form, not `story-5.9`: the dotted form made `git_evidence.py` match 1 commit out of 25"*.
- Bốn commit story viết sau luật đều dùng dạng chấm: `a81e3de`, `66c76cc`, `3c25e9a` (`story-6.x`),
  `1eb781b` (`Story 6.18`).
- Đo lượt này: `git_evidence.py --stories 6.1,…,6.18` khớp **24/38** commit; `--stories 6-1,…,6-18`
  khớp **0/38**. Script khớp đúng dạng id được truyền vào; retro Epic 5 đã truyền dạng gạch cho một
  kho viết dạng chấm. Con số "1/25" đúng, nhưng nó không đỡ kết luận "đổi cách viết mọi commit".
- Hai commit story lệch khuôn `type(scope): câu nói điều đã tìm ra`: `fcc7b3e` (*"feat: Implement
  Story 6.10a…"*) và `1eb781b` (*"feat(tests): add Story 6.18 tasks…"*). Chính commit mang luật,
  `5097f07`, không có tiền tố `type(scope)`.

**Phán quyết: accept + reconcile** — AI-8.

### F9 — Hai story sinh ra trong sổ nợ nhưng chưa vào quy hoạch

- **6.6b** — nhập nhiều tệp cùng lúc, tức AC cuối của Story 6.6 (`epics.md:4724-4726`) bị cắt:
  `deferred-work.md:10100` *"Chủ: Story 6.6b — một story mới đứng NGAY SAU 6.6 và TRƯỚC 6.7, chưa có
  trong `epics.md`/`sprint-status.yaml`"*. Vế "trước 6.7" đã hết đúng: 6.7 → 6.17 đều xong mà không có nó.
- **6.7b** — thêm Chương vào Tác phẩm có sẵn, AC Story 6.7 (`epics.md:4748-4751`):
  `deferred-work.md:10382`.
- grep `6.6b|6.7b|6-6b|6-7b` trên `epics.md` và `sprint-status.yaml` = 0. Sổ nợ tự ghi ở `:10385`
  rằng nó đang chở hai mục cùng hình dạng này.

**Phán quyết: fix now** — AI-9, qua `bmad-correct-course`.

### F10 — Ghi nhận nhỏ, có phán quyết

| Mục | Nguồn | Phán quyết |
|---|---|---|
| `UrlImportItemsState` (khối đã bóc của mọi link) sống sót qua lượt huỷ, tới lượt `start_url_import` kế tiếp; `preview_chapter_detail` đọc nó vô điều kiện | `project.rs:3321-3324` (huỷ chỉ dọn `PendingImportSourceState`); `:3951-3963`; `:6566-6583` | xem ghi chú dưới bảng |
| `.docx` có quan hệ `TargetMode="External"`: an toàn nhờ cấu tạo (`docx_boundary.rs:167-206` cấm mọi token mạng trong `core/docx/mod.rs`), nhưng `parse_rels` không đọc `TargetMode` và không ca nào khoá hình dạng này | `core/docx/mod.rs:268-286` | defer — thêm một fixture |
| `core/segment/image.rs:44` import `crate::commands::segment::ChapterSegment` (lõi phụ thuộc tầng lệnh) | tiếp nối hai tiền lệ có trước epic: `reading.rs:29` (`e36599e`, 08-30), `omit.rs:55` (`757a610`, 08-15) | accept — ghi để retro sau không nêu lại |
| `focusableWithin` + `trapTab`: 8 bản chép lúc retro Epic 3 → **10** ở HEAD; hai bản mới là hai overlay nhập của epic này | `ImportPreviewOverlay.vue:504,514`; `BilingualImportPreviewOverlay.vue:236,246` | defer — thuộc AI-11 retro Epic 3 (vẫn `open`) |
| Bản đơn ngữ chặn đổi ứng viên bảng mã ở tầng state khi đang xác nhận (`importPreviewState.ts:1016`); bản song ngữ chỉ có `:disabled` trên radio (`BilingualImportPreviewOverlay.vue:319`) | — | accept. Subagent nêu còn một đường bàn phím; đối chiếu nguồn thì `:121` là `onCandidateChange` của chính radio đó ⇒ không tìm thấy đường vào thứ hai, vế này **bị bác** |
| Sổ nợ thêm 128 mục trong epic; khoảng 74 dòng mang chủ Ice | `git diff 193ec73..1eb781b -- deferred-work.md` | không phán quyết riêng — xem AI-10 retro Epic 5 |
| 56 dòng miễn trừ `aura-allow-*` mới, 44 ở hai overlay nhập | `git diff 193ec73..1eb781b -- src \| grep -c "^+.*aura-allow"` | accept — các mẫu đã đọc đều có mệnh đề lý do |

Ghi chú `UrlImportItemsState`: **accept**. Nó sống sót qua lượt huỷ, nhưng không lọt sang lượt
nhập kế tiếp bằng đường nào đã kiểm được: phía giao diện chỉ gọi `preview_chapter_detail` khi
`lastSubmittedFrom === 'urls'` (`src/importPreviewState.ts:1086`), còn `start_url_import` ghi đè
nó (`project.rs:6283-6304`). Chi phí còn lại là bộ nhớ tới lượt URL kế tiếp — nên ghi một câu vào
doc-comment của state, để lượt rà sau không nêu lại như một chỗ rò.

### Điều Epic 6 làm đúng — ghi ra vì nó cũng là bằng chứng

- **AD-39 là dữ liệu, không phải quy ước.** `PIPELINE_ORDER` (`pipeline.rs:122`); cổng
  `pipeline_order_matches_ad_39_step_by_step` và `run_import_is_the_one_product_call_site`
  (`segment_pipeline_boundary.rs:167,191`), kèm ca tự kiểm cổng (`:259`); triệu chứng cụ thể nhất
  mà epic nêu có ca riêng (`segment_contract.rs:8098`). Hai subagent kiểm độc lập đều thấy mọi nguồn
  — dán, tệp, URL, `.docx`, song ngữ — đi qua một `run_import`.
- **AD-41 có đủ bốn ca tối thiểu**, mỗi ca kèm một ca đối xứng dương để không xanh vì một lý do
  khác (`webimport_contract.rs:92/129`, `:1030`, `:1056/1081`, `:1101`) — đúng lớp lỗi mà `7f7ee1d`
  tự tìm ra và sửa.
- **Quyết định còn treo của epic** (link hỏng, `epics.md:950`) được đóng có chữ ký Ice ngày
  2026-09-06 trong spec 6.7.
- **Tiêu đề commit story nói điều đã tìm ra**, và phần lớn là khuyết tật tự bắt trong vòng rà của
  chính story: neo ảnh vỡ ở ba đường gộp/tách (`8a88306`), đường ghi bỏ qua phần người dùng vừa sửa
  (`24d07c4`), xuất xứ của một lượt URL đã huỷ đóng dấu lên lượt nhập tệp (`39ae75d`).
- **e2e về 24/24** trên máy Ice sau 12 đêm đỏ (§Behavior verification, lượt 1).

---

## Previous-retro follow-through — retro Epic 5 (2026-09-03)

10 mục `epic-5-retro-item-50…59`. AI-8 (`item-57`) đã `done` trong sổ từ trước lượt này. Chín
mục còn lại đều `open`, và **đã kiểm từng mục bằng nguồn**.

| id | Mục | Bằng chứng | Trạng thái đề xuất |
|---|---|---|---|
| `epic-5-retro-item-50-ai-1-cap-cho-story-10-1-mot-dieu-kien-kh` | AI-1 — điều kiện khởi hành cho Story 10.1 | `tauri.conf.json` vẫn không khai `dict` (grep 0). Thân Story 10.1 (`epics.md:6701`) không mang điều kiện "số lớp nạp được > 0" hay đường cho máy dev; trong cửa sổ Epic 6, `epics.md` chỉ đổi ở 2 commit, cả hai về 6.10. Bàn đo 6.18 tự trộn bốn tệp `.db` qua `--config` lúc build (`6-18-ban-do/build.sh:3-18`) — đó là đường của bàn đo, không phải của sản phẩm | không đổi |
| `epic-5-retro-item-51-ai-2-cai-san-phien-ban-cho-harvest_work_` | AI-2 — sàn phiên bản cho lượt thu hoạch | Hạ cánh ở `2cb5e9b` (2026-09-03): `indexer.rs:1309` `MINIMUM_HARVEST_SCHEMA_VERSION = 8`, `HarvestSkipReason` ở `:1319`; ca chiều CŨ `library_index_contract.rs:2613`, ca đúng sàn `:2674` | `done` |
| `epic-5-retro-item-52-ai-3-noi-rebuildoutcome-text_skipped-len` | AI-3 — nối con số bỏ qua lên giao diện | Cùng `2cb5e9b`: `LibraryMode.vue:374` đọc `libraryTextSkippedCount` (`libraryRescan.ts:62`); thân commit giải thích độ phủ được đếm từ chính chỉ mục lúc truy vấn, vì `text_skipped` rỗng sau một lần khởi động lại | `done` |
| `epic-5-retro-item-53-ai-4-quyet-hinh-dang-lam-tuoi-chi-muc-ti` | AI-4 — quyết hình dạng làm tươi chỉ mục | `grep reindex src-tauri/src/commands/segment.rs` vẫn 0; không tìm thấy bản ghi quyết định (grep `AI-4` trong `deferred-work.md` và các proposal 2026-09-0x cho 0) | không đổi |
| `epic-5-retro-item-54-ai-5-chan-doan-10-spec-e2e-do-va-dua-bo` | AI-5 — e2e về xanh TRƯỚC khi Epic 6 mở | Vế "trước khi Epic 6 mở" không đạt: Epic 6 mở ở `bb1e91f` (2026-09-03 23:03) khi e2e còn đỏ, và lượt `schedule` đỏ 12/12 đêm từ 09-03 tới 09-14 (lượt cuối `34898445729`: 16 xanh / 8 đỏ, cả tám là spec Epic 5). Ba bản vá `bd15661` (09-13), `6d68dce`, `7863afb` (09-14); hai bản sau **chưa đẩy lên `origin`**. Lượt chạy của retro này trên HEAD `1eb781b`: **24/24 xanh** trong 2m36s, máy Ice, load 4–5 | `in-progress` — lên `done` khi một lượt `schedule` trên `origin` xanh |
| `epic-5-retro-item-55-ai-6-ghi-gioi-han-0-lop-tu-dien-vao-5-14` | AI-6 — ghi giới hạn "0 lớp từ điển" | Một nửa: điều kiện đã chuyển sang Story 6.18 (`spec-6-18-…md:18-20`, `6-18-ban-do/build.sh:3-18`). Nhưng `5-14-ban-do/REPORT.md` và `environment.txt` không có chữ nào về từ điển (grep `từ điển\|dict\|layer` = 0), nên số sơ bộ của 5.14 vẫn đứng mà không kèm giới hạn | `in-progress` |
| `epic-5-retro-item-56-ai-7-anh-xa-5-commit-khong-ten-story-95b` | AI-7 — ánh xạ commit + tiêu đề là giao diện máy đọc | Một nửa: luật vào `AGENTS.md:54` ở `5097f07` (2026-09-10 22:06; chính commit ấy không có tiền tố `type(scope)`). Bảng ánh xạ trong `epic-5-context.md` không có (grep `95b54e3` = 0). Và bốn commit story viết SAU luật (`a81e3de`, `66c76cc`, `3c25e9a`, `1eb781b`) vẫn viết `story-6.x` / `Story 6.18` — xem F7 | `in-progress` |
| `epic-5-retro-item-58-ai-9-doi-nhip-doc-ci-mot-luot-gh-run-lis` | AI-9 — một cổng đọc CI trước khi ghi `done` | Không có cổng: chỉ một dòng chú thích ở `.github/workflows/ci.yml:67`. Mọi bằng chứng ở F1 | không đổi |
| `epic-5-retro-item-59-ai-10-quyet-so-phan-15-muc-retro-epic-3` | AI-10 — số phận 15 mục retro Epic 3 | 15/15 mục `epic-3-retro-item-*` vẫn `open` | không đổi |

Mục của các epic trước nữa, **chưa kiểm từng mục** (tức "không tìm thấy bằng chứng", không phải
"đã kiểm và thấy chưa làm"): Epic 1 — 4 `open` + 1 `in-progress`; Epic 2 — 8 `open`; Epic 3 — 15
`open` (chính là AI-10); Epic 10 — 2 `open`.

**Ice xác nhận ngày 2026-09-15, và đã ghi** bằng `sprint_status.py update --set-action-status`
(`action_items_updated: 5`): `item-51` và `item-52` lên `done`; `item-54`, `item-55`, `item-56` sang
`in-progress`. Bốn mục còn lại (`item-50`, `item-53`, `item-58`, `item-59`) giữ `open`.

---

## Action items

Đây là đề xuất, chưa thi hành; Ice quyết mục nào chạy. Retro Epic 5 AI-10 đã cảnh báo *"một sổ 25
mục không ai đọc thì tệ hơn một sổ 5 mục có chủ"*, nên các mục dưới được chia làm hai nhóm.

### Nhóm A — đề xuất đóng trước khi Epic 7 mở

> 🔵 *(2026-09-15 — đọc "Epic 7" ở đây thành **Epic 4**: theo thứ tự chốt 2026-08-13
> (`1 → 2 → 3 → 5 → 6 → 4 → 7 …`, `build-sequence.md` §Thứ tự thực thi đầy đủ và `epics.md`
> §Epic List), epic kế tiếp sau Epic 6 là Epic 4, không phải Epic 7.)*

| # | Mục | Chủ | Loại | Nguồn |
|---|---|---|---|---|
| AI-1 | **Dựng cổng trạng thái mà AI-9 retro Epic 5 đã đòi.** (a) `review` không được ghi chung commit với mã story; nó được ghi sau khi đẩy, khi lượt `push` của chính commit đó xanh cả hai job `check`. (b) Cổng `pre-push` từ chối một diff `sprint-status.yaml` đưa story lên `done` khi lượt `schedule` gần nhất trên `origin` đỏ mà `deferred-work.md` không có một dòng mang run ID của lượt đó. (c) Việc làm ngay: ghi hai lượt đỏ sau vá WAL (`34780086488`, `34898445729`, mệnh đề 2a `store_contract.rs:890`) vào sổ nợ của `spec-ca-wal-do-tren-windows.md`; hai con số +160.680 B và +16.480 B chính là phép đo mà §Always của spec ấy đòi trước khi được đặt dung sai. Đẩy ba commit local rồi đọc lượt `schedule` kế tiếp | Ice (cổng) · Dev (c) | quy trình | F1 |
| AI-4 | **Chuyển `confirm_import_with_encoding` và `confirm_bilingual_import` sang `#[tauri::command(async)]`**, và thêm hai hàng vào `config_invariants.rs::the_blocking_wires_run_off_the_main_thread`. Tốt hơn nữa là đổi cổng từ danh sách tay sang quét `src/commands/**`, để một tệp mới không lọt ra ngoài như `project.rs` đã lọt. Kèm một câu hỏi phạm vi cho Dev trình Ice: `create_work_from_file` và `preview_import_encoding_from_file` đọc tệp tới 40 MB và chạy trọn pipeline, cũng đồng bộ — chúng có nằm trong ngân sách luồng chính không | Dev | mã | F3 |
| AI-7 | **Gộp ba hàm cắt** — `trimmed_or_none` ở `project.rs:3862` và `chapter.rs:582`, cùng `trim_like_the_paste_box` ở `project.rs:4109` — thành một hàm dùng chung khớp `String.prototype.trim()`. Thêm một ca mang `U+FEFF` vào `chapter_origin_contract.rs` và `importPreviewChapterOrigin.test.ts`. Ca 2 của behavior check lượt này là đối chứng đỏ sẵn có: nó phải ĐỎ trước bản vá và XANH sau | Dev | mã | F7 |
| AI-9 | **Chạy `bmad-correct-course` để đưa 6.6b và 6.7b vào `epics.md` + `sprint-status.yaml`**, với thứ tự đúng thực tế: vế "6.6b đứng trước 6.7" ở `deferred-work.md:10100` đã hết đúng. 🔵 *(Xong 2026-09-15 — cả hai đã vào `epics.md` và `sprint-status.yaml` qua `sprint-change-proposal-2026-09-15.md`; action item `epic-6-retro-item-63` chuyển `done`)* | Ice | quy hoạch | F9 |

### Nhóm B — có chủ, không chặn Epic 7

| # | Mục | Chủ | Loại | Nguồn |
|---|---|---|---|---|
| AI-2 | **Một móc e2e thay hộp thoại chọn tệp**, cùng khuôn hai lớp AD-45 của `AURATRANSLATE_E2E_DATA_DIR`, rồi mỗi nguồn tệp của Epic 6 một spec: `.txt` GBK có mẫu Chương, `.docx` có bảng và ảnh, `.csv` song ngữ có hàng lệch. Móc chạm AD-45 nên cần Ice duyệt hình dạng trước | Dev (Ice duyệt) | bàn đo | F2 |
| AI-3 | **Một lượt nghiệm thu tay có chủ** cho các mục mà spec tự ghi "CHƯA LÀM": `.docx` hỏng trên bản `--release`; `.atproj` chép sang máy khác vẫn đủ ảnh; vị trí ảnh thật ở lưới và Chế độ đọc; một lượt nhập URL thật có ảnh ở host chậm — lượt này đồng thời đo F3 | Ice | nghiệm thu | F2, F3 |
| AI-5 | **Quyết FR132 cho song ngữ:** (A) dựng "cần xem" cho bản xem trước song ngữ với hai nguyên nhân còn nghĩa (bảng mã tin cậy thấp, luật làm sạch xoá quá nhiều); hoặc (B) ghi vào AC Story 6.10 rằng FR132 chỉ phủ đường đơn ngữ, qua `bmad-correct-course`. 🔵 *(Quyết 2026-09-15 — Ice chọn **(A)**: **Story 6.16b**, thêm vào `epics.md` cùng lượt; AC Story 6.10 giữ nguyên. Action item `epic-6-retro-item-66` chuyển `done`)* | Ice | quyết định | F4 |
| AI-6 | **Tách `commands/project.rs` theo mối quan tâm** trong một story có chủ, và quyết có dựng cổng đo kích thước tệp mà `AGENTS.md:19` ghi là chưa có hay không | Ice → Dev | quy hoạch | F6 |
| AI-8 | **Hoà giải quy ước tiêu đề commit:** sửa `AGENTS.md:54` theo thực hành (`story-6.x`), kèm ghi rằng retro gọi `git_evidence.py --stories` bằng dạng chấm; hoặc giữ luật và dựng một hook `commit-msg` để luật thành cổng. Chọn một trong hai | Ice | quy ước | F8 |
| AI-10 | **Đóng mũi thăm dò bảng mã:** Ice cấp fixture GBK/Big5 thật vào `6-1-ban-do/fixtures/encoding/` rồi chạy `webimport_probe.rs:260`; hoặc hạ tiêu chí "đo độ chính xác" thành nợ có chủ trong `epics.md` qua `bmad-correct-course`. 🔵 *(2026-09-15 — Ice chưa có tệp GBK/Big5 thật, và chọn **giữ nợ có chủ, không sửa `epics.md`**. Lượt correct-course chỉ sửa đường dẫn sai ở `deferred-work.md:9424` (bàn đo đọc `6-1-ban-do/fixtures/encoding/`) và dòng A13 đã cũ ở `SPEC.md:125`. Mục vẫn `open`)* | Ice | đo / quyết định | F5 |

Story 6.18 không có mục riêng ở đây: nó đã có trong `sprint-status.yaml` và đang tạm ngưng theo quyết
định của Ice. Nó là điều kiện để NFR3/4/5 và Q4 đóng.

**Đã ghi vào sổ (Ice chọn "cả 10 mục", 2026-09-15):** `sprint_status.py update --add-action` trả
`action_items_added: 10`, id `epic-6-retro-item-60` … `epic-6-retro-item-69`, cả mười ở trạng thái
`open`, `ref` trỏ về tài liệu này. Ghi vào sổ không có nghĩa là đã thi hành.

---

## Acceptance verdict

**rejected** — phán quyết máy, và **Ice giữ nguyên** khi được hỏi (2026-09-15). Tiêu chí **đã khai
báo** (`epics.md:935-951` và AC của 19 story ở `:4476-5360`).

Theo `references/acceptance-verdict.md`, một `pending_stories` không rỗng buộc phán quyết là
`rejected`. Bằng chứng: `6-18-đo-lại-nfr3-nfr4-nfr5-trên-thư-viện-5-000-chương-thật` — `in-progress`,
Ice tạm ngưng.

Ngoài luật hình thức ấy, ba dữ kiện độc lập cùng đỡ phán quyết:

1. **Tiêu chí khai báo chưa đạt.** NFR3/4/5 khai "đóng ở Story 6.18" (`epics.md:941`) — chưa đóng.
   Mũi thăm dò độ chính xác bảng mã (`epics.md:948`, AC Story 6.1) — 0 mẫu (F5). FR132 không phủ bản
   xem trước song ngữ nhiều Chương, và không ai quyết điều đó (F4).
2. **Cổng lúc nghiệm thu đang đỏ.** Lượt `schedule` gần nhất (`34898445729`) đỏ `check (macos-26)`
   ở ca WAL và đỏ `e2e` 16/8. Hai bản vá e2e xanh trên máy Ice nhưng chưa lên `origin` (F1).
3. **Bề mặt chính của epic không có nghiệm thu tự động qua giao diện thật** cho bốn trong năm nguồn (F2).

Điều **đã** đạt, ghi cho cân:
- 18/19 story xong. Theo bảng đối chiếu của hai subagent (đã lấy mẫu kiểm lại), mọi AC của 18 story
  hoặc có mã kèm ca test, hoặc có lệch được ghi có chủ, trừ F4.
- AD-39, AD-40, AD-41 đều thành cổng máy đọc được (§Điều Epic 6 làm đúng).
- Quyết định còn treo duy nhất của epic (link hỏng) đã đóng có chữ ký.
- Trên ứng dụng thật, đường dán + mẫu Chương giữ bất biến "xem trước bằng ghi xuống" (behavior
  check, lượt 2), và bộ e2e 24/24 trên máy Ice.

Ice có quyền lật phán quyết này. Nếu lật, đường tới `accepted-with-open-items` là: Story 6.18 xong
(hoặc được dời khỏi Epic 6 qua `bmad-correct-course`, kéo theo việc sửa `epics.md:941`); một lượt
`schedule` trên `origin` xanh (AI-1c, AI-4); và AI-5 được quyết.

> 🔵 **Cập nhật 2026-09-15 sau `correct-course`** (`sprint-change-proposal-2026-09-15.md`). Đường ấy
> đã đổi, và **dài hơn** chứ không ngắn hơn: Story 6.18 đã rời Epic 6 (gộp vào Story 10.9), nhưng
> cùng lượt ấy Epic 6 nhận **ba** story mới — 6.6b, 6.7b, 6.16b — nên `pending_stories` của Epic 6
> vẫn không rỗng. Ba việc còn lại không đổi: ba story mới xong · một lượt `schedule` xanh trên
> `origin` (AI-1c, AI-4) · AI-5 **đã** được quyết ngày 2026-09-15.

---

## Open questions

1. **Story 6.18 tạm ngưng trong Epic 6, hay dời ra khỏi epic?** Câu trả lời đổi phán quyết. Nếu dời
   qua `bmad-correct-course`, `pending_stories` rỗng và epic chỉ còn bị xét theo tiêu chí — nhưng khi
   đó `epics.md:941` ("đóng ở Story 6.18") cũng phải sửa theo.
   🔵 **ĐÃ TRẢ LỜI 2026-09-15 — dời.** Story 6.18 gộp vào **Story 10.9** qua `correct-course`;
   `epics.md:941`, bảng NFR, PRD Q4/A6 và `SPEC.md`/`requirements.md` đã sửa theo. ⚠️ Vế
   *"`pending_stories` rỗng"* KHÔNG xảy ra: cùng lượt ấy Epic 6 nhận ba story mới.
2. **Ca WAL mệnh đề 2a (dung sai 0) đỏ trên cả hai runner sau bản vá.** Đây là hai phép đo mà
   §Always của `spec-ca-wal-do-tren-windows.md:29` đòi trước khi được phép đặt dung sai. Mở lại spec
   ấy với hai con số làm đầu vào, hay giữ nguyên và chấp nhận `check` đỏ trong lúc chờ?
3. **F3 nặng tới đâu trên máy thật?** Cơ chế đọc từ nguồn là chắc, nhưng chưa ai đo một lượt nhập URL
   có ảnh ở host chậm. Muốn đo cần mạng thật hoặc một quyết định về LuLu cho nhị phân ứng dụng.
4. **FR132 cho song ngữ (AI-5): dựng hay thu hẹp phạm vi?** Retro này không đủ dữ kiện để chọn.
   Người dùng nhập tệp song ngữ có thật sự cần bộ lọc ấy không là câu hỏi sản phẩm.
5. **Có giữ Epic 7 chờ AI-1 không?** 🔵 *(2026-09-15 — đọc thành **Epic 4**: đó mới là epic kế tiếp.
   Câu hỏi giữ nguyên nội dung, và vẫn mở.)* Đây là lần thứ tư cùng một cơ chế. Nếu Epic 7 mở trước khi có
   cổng, bằng chứng của bốn epic nói nó sẽ lặp lại lần thứ năm.
