---
scope: Toàn bộ AuraTranslate v1 — 10 nhóm năng lực C1–C10, 112 FR, 16 NFR
purpose: build-substrate
altitude: feature
updated: 2026-08-05T15:05
---

- (constraint) Nền tảng: desktop native macOS + Windows, hành vi tương đương (NFR14). Không web, không mobile.
- (constraint) Local-first tuyệt đối: không tài khoản, không cloud sync, không telemetry (NFR12, NFR13, FR104). Ra ngoài mạng CHỈ khi người dùng chủ động gọi AI.
- (constraint) Giấy phép GPL v3; mọi crate/thư viện phải tương thích GPLv3 (NFR15). GPLv3 chọn vì tương thích Apache-2.0.
- (constraint) Ứng dụng MỘT người dùng, một tiến trình. Cộng tác chỉ qua trao đổi file. KHÔNG CRDT, không đồng bộ, không giải xung đột đa người.
- (constraint) FR96-FR98: .atproj là nguồn sự thật, tự chứa, copy sang máy khác mở được. Chỉ mục Library là DẪN XUẤT, dựng lại được hoàn toàn.
- (constraint) FR77: ứng dụng phải chạy đầy đủ khi KHÔNG cấu hình AI. Mọi năng lực ngoài C6/C7 không được phụ thuộc AI.
- (constraint) FR36 + §8.6 PRD: nguồn từ điển theo mô hình lớp nền sạch + lớp gỡ rời. Thiều Chửu, Cổ hán văn, VietPhrase, HVTĐTD đều là LỚP GỠ RỜI. Gỡ bất kỳ lớp nào không làm hỏng tra cứu.
- (constraint) FR31/FR32: mọi định nghĩa hiển thị nguồn, không có chế độ ẩn; nguồn bất đồng thì hiển thị đồng thời, KHÔNG hợp nhất.
- (constraint) NFR16: toàn bộ chuỗi giao diện nằm ngoài mã nguồn, trong file tài nguyên, ngay từ dòng code đầu tiên. v1 chỉ tiếng Việt.
- (constraint) FR27 + NFR6: toàn bộ dữ liệu từ điển nhúng trong bản cài, 150-200MB, KHÔNG có cơ chế tải thêm sau cài.
- (constraint) FR111: cơ chế cập nhật chỉ kiểm tra và thông báo. Không tự tải, không tự cài (vì bản phát hành không ký số).
- (constraint) NFR11/FR67: API key nằm trong keychain/credential manager OS. Không ghi vào file cấu hình, file dự án hay log.
- (constraint) Cấu hình hai tầng Global -> Tác phẩm, tầng Tác phẩm ghi đè (FR46, FR68, FR69, FR103, FR57).
- (version) Giai đoạn 0 (2026-08-02) đã đo thật: lookup Rust p50 0,022ms / p95 0,046ms, payload IPC 679 byte; DB 130MB với 3 nguồn; FTS5 trigram trả RỖNG cho truy vấn 1-2 ký tự; unicode61 mặc định xoá dấu tiếng Việt.
- (decision) [ADOPTED] Chuyển mọi từ điển sang SQLite ở bước build, không đọc định dạng gốc lúc chạy. Parser chỉ nằm trong build tool, không vào bản phát hành. | Binds: C3, C9 | Prevents: mỗi nguồn một tầng truy vấn riêng + tự xây tầng hợp nhất + ràng buộc giấy phép parser lên bản phát hành.
- (decision) [ADOPTED] Ba nhánh truy vấn tiếng Trung: B-tree (tra chính xác, 0,02ms) / char_idx bảng đảo ngược (1-2 ký tự, 0,15-4,5ms) / FTS5 trigram (3+ ký tự). LIKE bị loại vì 20-50ms. | Binds: C3, C1 | Prevents: truy vấn 1-2 ký tự trả rỗng âm thầm.
- (decision) [ADOPTED] Chỉ mục FTS chính dùng remove_diacritics 0 (phân biệt dấu); chỉ mục xoá dấu chỉ tồn tại như chỉ mục PHỤ, không bao giờ là mặc định (NFR8, FR9). | Binds: C1, C3 | Prevents: gộp má/ma/mà/mả/mã/mạ thành một kết quả.
- (decision) [ADOPTED] Bỏ tauri-wire khỏi kế hoạch. Payload 679 byte, xa ngưỡng 10KB; chi phí serialize 0,002ms. | Binds: ranh giới IPC | Prevents: tối ưu hoá sớm không cần thiết.
- (event) Ice chọn Coaching path; deliverable duy nhất là ARCHITECTURE-SPINE.md (không cần deck/solution design/bản đồ giai đoạn).
- (direction) Altitude: feature — spine giữ cho các epic (dẫn xuất từ C1-C10) nhất quán. Downstream kế tiếp: bmad-create-epics-and-stories.
- (decision) Webview mỏng: 100% quy tắc nghiệp vụ ở Rust; frontend TypeScript chỉ render + giữ state UI (focus, scroll, selection, bố cục panel). Cấm cài đặt lại bất kỳ quy tắc nghiệp vụ nào ở TS. Ngoại lệ tường minh: văn bản đang gõ trong Editor là state cục bộ frontend, chỉ qua IPC khi auto-save / xác nhận segment / rời segment. | Binds: tất cả | Prevents: hai bản cài đặt tách câu/matching lệch nhau -> TM khớp sai âm thầm; quy tắc nghiệp vụ nằm trong webview mà Tauri v2 coi là không đáng tin.
- (decision) Paradigm: Hexagonal liều thấp trong Rust core. Đúng BA cổng: DictionarySource (FR36 lớp gỡ rời), TranslationProvider (FR77 chạy không AI), ProjectStore (.atproj vs chỉ mục). Phần còn lại là module Rust thường, KHÔNG trait hoá. | Binds: tất cả | Prevents: FR77 và FR36 thoái hoá thành kỷ luật cá nhân; đồng thời tránh rừng trait một người không nuôi nổi.
- (direction) Ice loại: B (lặp logic ở TS), C (frontend dày), hexagonal đủ liều, layered đơn giản, modular monolith theo C1-C10. Lý do loại C1-C10 thành crate: các nhóm năng lực chồng lấn nặng (TM+Glossary+RAG dùng chung matching) nên không phải ranh giới kỹ thuật tốt.
- (decision) Segment mang ID bền, không bao giờ đổi và không bao giờ tái dùng. Thứ tự trong Chương là cột RIÊNG (ord), sắp lại được mà không đụng id. | Binds: C2, C5, C7, C9 | Prevents: định danh theo vị trí -> tách một segment làm lịch sử phiên bản và ghi nhớ proofreader của mọi segment sau nó trỏ sai chỗ, không báo lỗi. Cũng loại băm nội dung: trong truyện dài, câu lặp lại hàng trăm lần sẽ dùng chung danh tính.
- (decision) TM khoá theo CẶP VĂN BẢN (nguồn -> đích) + metadata, KHÔNG theo segment id. Chuẩn ngành CAT tool. | Binds: C5, C2, C8 | Prevents: TM vỡ mỗi khi segment bị gộp/tách/tái tách; đồng thời là điều kiện để xuất TMX (FR64) có nghĩa. Hệ quả: sửa bản dịch đã xác nhận thì GHI THÊM cặp mới, không sửa cặp cũ (khớp FR63).
- (decision) Ranh giới segment được tính MỘT LẦN lúc nhập Chương và LƯU xuống .atproj. Không bao giờ tính lại lúc nạp. Cải thiện quy tắc tách câu chỉ áp dụng qua thao tác tái tách CHỦ ĐỘNG của người dùng trên từng Chương, có cảnh báo về dữ liệu sẽ về hưu. | Binds: C2, C5, C7, C9 | Prevents: một lần sửa quy tắc tách câu (FR23 [A4]) âm thầm tách lại toàn bộ Thư viện, làm lịch sử phiên bản + trạng thái xác nhận + ghi nhớ proofreader của mọi Chương cũ trỏ sai chỗ.
- (decision) Gộp/tách segment (FR78) là thao tác VỀ HƯU + TẠO MỚI: segment cũ đánh dấu về hưu (lịch sử phiên bản vẫn tra lại được), segment mới bắt đầu ở trạng thái CHƯA XÁC NHẬN với lịch sử rỗng. Cặp TM đã ghi ở lại nguyên. | Binds: C2, C5, C7, C9 | Prevents: segment ghép từ hai segment đã xác nhận tự nhận là đã xác nhận dù chưa ai đọc nó — cùng triết lý FR58 (hệ thống không bao giờ tự coi là đã xong).
- (decision) Năm loại kho, ranh giới sở hữu cứng: dict-core.db + <lớp gỡ rời>.db (chỉ đọc lúc chạy) | .atproj/ (nguồn sự thật của một Tác phẩm) | global.db (nguồn sự thật tầng Global) | library-index.db (DẪN XUẤT) | OS keychain (chỉ API key). | Binds: tất cả | Prevents: ba giai đoạn xây dựng cách nhau nhiều tháng chọn lệch nhau về nơi dữ liệu sống.
- (decision) library-index.db BẮT BUỘC là file riêng, không nằm chung global.db. | Binds: C1, C9 | Prevents: thao tác sửa chữa hiển nhiên nhất (xoá chỉ mục hỏng rồi dựng lại) xoá luôn Glossary/TM toàn cục người dùng tích luỹ hàng năm — vi phạm NFR10.
- (decision) Chỉ MỘT component (Indexer) được ghi vào library-index.db, và chỉ ghi SAU KHI .atproj đã ghi xong. Không có đường ghi nào khác. | Binds: C1, C9 | Prevents: chỉ mục thôi là dẫn xuất — có chỗ ghi thẳng vào chỉ mục cho nhanh thì FR98/NFR10 mất hiệu lực.
- (decision) .atproj là một THƯ MỤC: meta.json (metadata Library đọc được không cần mở SQLite) + project.db + assets/ (ảnh là file thật). | Binds: C1, C9, C2 | Prevents: Library phải mở hàng trăm db chỉ để lấy tên/bìa/tiến độ (đe doạ NFR4 <3s); ảnh phải qua IPC mỗi lần render (FR42, FR43). Đổi lại rủi ro copy thiếu thư mục con — chấp nhận.
- (decision) Mỗi lớp từ điển gỡ rời là MỘT FILE .db độc lập; năm nguồn nền sạch gộp trong dict-core.db. Runtime không có mã riêng cho từng nguồn. Gỡ một lớp = xoá một file. | Binds: C3, C10 | Prevents: FR112 (chính sách gỡ bỏ) biến thành thay đổi mã + dựng lại 150-200MB; FR36 chỉ nghiệm thu được bằng suy luận thay vì bằng test thật (xoá file -> chạy lại bộ test tra cứu). Tổng 5 file, dưới giới hạn ATTACH 10 của SQLite.
- (decision) TM hai tầng có ngữ nghĩa HỢP NHẤT (không phải ghi đè như Glossary/prompt/cấu hình AI): tra cứu trả kết quả cả hai tầng, TM Tác phẩm xếp trước. Xác nhận segment (FR56) CHỈ ghi vào TM Tác phẩm; TM toàn cục chỉ nhận qua thao tác chủ động (nhập TMX hoặc đẩy lên tầng toàn cục). | Binds: C5, C6 | Prevents: FR103 nói chung là 'tầng dự án ghi đè' -> ai đó cài TM theo ghi đè, TM toàn cục không bao giờ được dùng tới; hoặc ghi vào cả hai làm TM tiên hiệp trộn vào TM hợp đồng pháp lý, không gỡ ra được.
- (decision) Mỗi kho ghi được (.atproj/project.db, global.db, library-index.db) có ĐÚNG MỘT kết nối ghi, đặt sau một hàng đợi nối tiếp. Đọc dùng pool nhiều kết nối song song (WAL). Không module nào được tự mở kết nối ghi. | Binds: tất cả module ghi dữ liệu | Prevents: Giai đoạn 4 mở kết nối riêng ghi TM cho nhanh -> writer starvation + gai trễ không dự đoán được, NFR2 mất hiệu lực mà không lần ra được nguyên nhân. Hệ quả tốt: NFR2 đo được ở MỘT chỗ.
- (decision) FR77 cưỡng chế bằng quy tắc hướng phụ thuộc có test tự động: không module nào ngoài ai/ được import ai/. Chiều ngược lại hợp lệ (ai/ đọc glossary/, tm/, segment/ cho FR70). | Binds: tất cả | Prevents: FR77 thoái hoá thành kỷ luật cá nhân qua 7 giai đoạn; lỗi chỉ lộ ra khi một người dùng không có API key thử.
- (decision) Lắp prompt là một HÀM THUẦN trả về prompt đã lắp hoàn chỉnh; lời gọi AI nhận prompt đã lắp làm đầu vào. Không nối chuỗi rải rác ở chỗ gọi. | Binds: C6, C7 | Prevents: FR71 (xem prompt cuối cùng đã gửi) trở thành công việc đắt và không bao giờ chính xác 100%; đồng thời là công cụ chẩn đoán duy nhất khi AI không tuân thủ Glossary.
- (decision) Pipeline từ điển: build tool chạy tay trên máy Ice sinh các file .db, đẩy lên GitHub Release riêng có phiên bản (vd tag dict-v1). Repo chứa mã build tool + dict-manifest.toml ghi URL, SHA-256 và phiên bản nguồn thô của từng file. CI tải theo manifest, đối chiếu checksum, rồi đóng gói. | Binds: C3, C10, C9 | Prevents: nguồn thô 1,13GB hoặc .db 150-200MB lọt vào git; đồng thời giữ FR107 (ai cũng dựng lại được từ nguồn thô và đối chiếu checksum khớp).
- (version) Kiểm chứng web 2026-08-02: tauri 2.11.5 (Apache-2.0 OR MIT); rusqlite 0.40.1 (MIT); jieba-rs 0.10.3 (MIT); tantivy-stemmers 0.4.0 (BSD-3-Clause); docx-rs 0.4.22 (MIT); similar 3.1.1 (Apache-2.0 — nay đã xác nhận, tương thích GPLv3); dissimilar (Apache-2.0 OR MIT); keyring 4.1.6 (MIT OR Apache-2.0); dockview 7.0.4.
- (version) SỬA LỖI RESEARCH: WAL2 KHÔNG phải tính năng đã phát hành. Diễn đàn SQLite xác nhận vẫn là nhánh thử nghiệm, không có kế hoạch đưa vào bản chính, chưa ứng dụng thật nào dùng, gần như không có test. SQLite 3.53.x (06/2026) không có. Báo cáo technical research liệt kê WAL2 với dấu xanh làm biện pháp cho NFR2 — biện pháp đó không tồn tại.
- (decision) Chống gai trễ auto-save (NFR2): PRAGMA wal_autocheckpoint = 0 (TẮT auto-checkpoint). Một luồng nền trên kết nối RIÊNG gọi wal_checkpoint(PASSIVE) khi người dùng ngừng gõ một khoảng, và wal_checkpoint(TRUNCATE) khi đóng Tác phẩm/thoát app. Thời điểm checkpoint là quyết định của ứng dụng, không phải của SQLite. Phải có ngưỡng kích thước WAL buộc checkpoint để .db-wal không phình vô hạn khi gõ liên tục hàng giờ. | Binds: C9, C2 | Prevents: checkpoint mặc định (1000 trang) rơi đúng lúc đang gõ.
- (decision) Dùng crate keyring 4.1.6 TRỰC TIẾP trong Rust, KHÔNG dùng tauri-plugin-keyring. Lý do kiến trúc (mạnh hơn lý do phiên bản 0.1.0 quá non): một Tauri plugin tồn tại để phơi API ra JavaScript — đúng thứ NFR11 và nguyên tắc 'key không bao giờ qua IPC' cấm. Với webview mỏng, frontend không có việc gì với key. | Binds: C6 | Prevents: tự tạo ra một bề mặt tấn công không cần thiết.
- (decision) Frontend: Vue 3 + TypeScript, bố cục panel dùng dockview (bản Vue). FR17/FR18 gần như là mô tả chức năng của dockview. | Binds: C2 | Prevents: tự viết hệ dock/undock/tab/resize/preset.
- (assumption) Chọn giữa similar (diff cấp grapheme — quan trọng với dấu tiếng Việt và ký tự Hán) và dissimilar (semantic cleanup) là điểm HOÃN CÓ ĐIỀU KIỆN: cả hai đều tương thích GPLv3, thử trên dữ liệu thật ở Giai đoạn 5 rồi chốt.
- (decision) MỘT cửa sổ OS duy nhất; Library / Workspace / Chế độ đọc là ba CHẾ ĐỘ trong đó. Review Mode (FR92) là một bố cục dockview, không phải cửa sổ thứ hai. | Binds: C1, C2, C8 | Prevents: tách cửa sổ để lấy bảo mật là trả bằng chính thứ sản phẩm bán (FR16 - một cửa sổ thay cho bốn năm cửa sổ). Bù lại bằng AD CSP + AD cấm render HTML, vốn bịt đúng mối đe doạ thật (file .docx/.md reviewer gửi về).
- (decision) Phạm vi filesystem hai tầng. Scope TĨNH khai trong capabilities: $RESOURCE/dict/** (chỉ đọc), $APPDATA/** (đọc+ghi cho global.db và library-index.db). Scope ĐỘNG cấp lúc chạy khi người dùng chọn qua hộp thoại: các thư mục gốc Library (mặc định ~/Documents/AuraTranslate/) và đường dẫn export từng lần. Không đường mã nào chạm path ngoài scope. | Binds: tất cả | Prevents: 'không ai đọc được tài liệu của bạn' thoái hoá thành lời hứa thay vì ràng buộc do framework cưỡng chế.
- (decision) ĐÚNG HAI điểm ra mạng trong toàn bộ ứng dụng: (1) adapter TranslationProvider gọi endpoint tương thích OpenAI, (2) kiểm tra phiên bản mới (FR111). Không có điểm thứ ba. CSP của Tauri cấm mọi origin từ xa — không CDN, không font ngoài, không ảnh ngoài. | Binds: tất cả | Prevents: NFR12 (không telemetry) bị vi phạm vô tình qua crash reporter, thư viện analytics, hoặc một font CDN trong frontend.
- (decision) Nội dung nhập từ ngoài (.docx, .md, .txt của reviewer) KHÔNG BAO GIỜ được render như HTML. Rust phân tích thành mô hình dữ liệu có cấu trúc; Vue render từ mô hình đó. Không có đường nào đưa chuỗi từ file ngoài vào v-html. | Binds: C8, C1, C2 | Prevents: XSS trong webview có quyền gọi IPC — mối đe doạ thật, và là lý do research đòi tách cửa sổ.
- (decision) MỘT component Matcher dùng chung cho toàn bộ khớp ngôn ngữ (FR40 từ điển, FR51 Glossary, FR61 TM). Tiếng Trung: khớp chính xác + n-gram ký tự, tách từ qua jieba-rs khi cần. Tiếng Anh: stemming rồi token n-gram. | Binds: C3, C4, C5, C6 | Prevents: ba lần cài đặt riêng ở ba giai đoạn khác nhau -> Glossary bắt được biến thể mà TM không bắt được, không ai biết vì sao.
- (decision) MỘT component ScopeResolver dùng chung, và ngữ nghĩa mỗi loại dữ liệu khai báo TƯỜNG MINH: GHI ĐÈ (Glossary FR46, prompt FR69, cấu hình AI FR68) vs HỢP NHẤT (TM FR57). | Binds: C4, C5, C6, C9 | Prevents: FR103 phát biểu chung là 'tầng dự án ghi đè' -> mỗi giai đoạn cài lại theo cách hiểu riêng.
- (decision) Đường tra cứu trả về KẾT QUẢ THEO TỪNG NGUỒN; trong toàn bộ hệ thống KHÔNG TỒN TẠI bước hợp nhất/khử trùng lặp giữa các nguồn. | Binds: C3 | Prevents: ai đó thêm một bước dedupe cho gọn UI và làm mất FR31/FR32 — nguyên tắc nền, không phải tuỳ chọn hiển thị.
- (decision) Mọi đề xuất tự động (FR52 quét khi nhập, FR54 thu hoạch từ bản review) ghi vào BẢNG CHỜ riêng, không bao giờ ghi vào bảng Glossary đang dùng. Chỉ thao tác duyệt của người dùng mới chuyển từ bảng chờ sang Glossary. | Binds: C4, C8 | Prevents: FR55 (không cơ chế nào được tự ghi vào Glossary) thoái hoá thành kỷ luật — trụ định vị #1 phát biểu dưới dạng cấu trúc dữ liệu.
- (decision) Rust KHÔNG BAO GIỜ trả về văn bản hiển thị cho người dùng. Mọi lỗi và thông báo qua IPC là { code, message_key, params, retryable }; frontend phân giải message_key trong file tài nguyên chuỗi. | Binds: tất cả | Prevents: NFR16 (chuỗi giao diện ngoài mã nguồn từ dòng code đầu tiên) bị thủng ngay ở tầng lỗi — chỗ dễ quên nhất và đắt nhất để sửa sau.
- (decision) Streaming bản dịch AI dùng Tauri Channel API (không dùng event rời). Không dùng client SSE tự kết nối lại. Đứt luồng là quyết định tường minh của ứng dụng; thử lại phải do người dùng chủ động (FR75). Mọi lời gọi AI huỷ được giữa chừng (FR73). | Binds: C6, C7 | Prevents: auto-reconnect tạo yêu cầu mới hoàn toàn -> người dùng bị tính phí hai lần và nhận văn bản trùng lặp.
- (decision) Định danh: Tác phẩm mang UUID sinh lúc tạo, lưu trong meta.json (vì library-index tham chiếu xuyên .atproj và người dùng copy .atproj sang máy khác). Chương và Segment mang số nguyên CỤC BỘ trong project.db. Indexer phải phát hiện và cảnh báo hai Tác phẩm trùng UUID (hệ quả của việc nhân bản thư mục). | Binds: C1, C9 | Prevents: nhân bản một .atproj làm hai mục đụng nhau trong chỉ mục.
- (direction) Ice không chọn thêm chủ đề nào ngoài cấu trúc cửa sổ và phạm vi filesystem; các quyết định còn lại do tôi chốt theo phán đoán và đưa ra ở bước review.
- (event) Reviewer Gate chạy inline (session cấm subagent — kiểm tra yếu hơn context độc lập). lint_spine.py: 0 findings.
- (version) SỬA phiên bản sau khi tra thật: Vite 8.2.0 (không phải 7.x), Vue 3.5.40, dockview-vue 7.0.4 MIT peer vue ^3.4.0, libsqlite3-sys 0.38.1. SQLite đến từ feature bundled, không phải SQLite hệ điều hành; sàn cần: FTS5 trigram >=3.34, remove_diacritics 0 >=3.27.
- (decision) AD-30 Lược đồ có phiên bản, di trú chỉ tiến, từ chối mở phiên bản mới hơn. | Binds: C9, C1, tất cả | Prevents: .atproj sống qua 7 giai đoạn và 2-3 năm; FR97 hứa copy sang máy khác mở được nhưng máy đó chạy bản khác — không có quy tắc thì mỗi giai đoạn đổi lược đồ theo cách riêng và làm hỏng dữ liệu cũ im lặng. Đây là CẢ MỘT CHIỀU bị bỏ trắng ở bản chưng cất đầu, lens đối kháng bắt được.
- (decision) AD-31 Vòng đời trạng thái segment là máy trạng thái tường minh: auto-save KHÔNG tạo phiên bản; xác nhận tạo một phiên bản VÀ ghi cặp TM; sửa văn bản của segment đã xác nhận thì MẤT xác nhận. | Binds: C2, C5, C7, C9 | Prevents: (1) auto-save tạo phiên bản -> gõ một giờ sinh hàng trăm phiên bản, FR101 vô dụng; (2) sửa segment đã xác nhận mà vẫn 'đã xác nhận' -> không lần xác nhận nào nữa xảy ra, cặp TM mới không bao giờ được ghi.
- (decision) AD-32 Gộp/tách Chương CHỈ đổi chapter_id và ord; segment.id, lịch sử phiên bản, trạng thái xác nhận giữ nguyên. Khác cố ý với AD-5. | Binds: C1, C2, C5, C9 | Prevents: FR15 cài như 'tạo lại segment' phá sạch lịch sử của những Chương đã dịch xong, trong khi FR15 chỉ là thao tác tổ chức.
- (decision) AD-33 meta.json là cache DẪN XUẤT từ project.db, ghi bởi chính store::Writer của Tác phẩm trong cùng thao tác logic; không thành phần nào khác ghi vào, không thành phần nào coi là nguồn sự thật. | Binds: C1, C9 | Prevents: hai chủ sở hữu cho tiến độ (FR7) — Library hiện tiến độ cũ và không ai biết bản nào đúng.
- (decision) Ghi nhớ proofreader (FR84) khoá theo (work, chữ ký phát hiện), KHÔNG theo segment.id — FR84 nói phạm vi là 'trong cùng Tác phẩm', và nhờ vậy ghi nhớ sống sót qua gộp/tách segment. Sửa lại ERD bản đầu vốn gắn PROOF_IGNORE dưới SEGMENT.
- (decision) Mỗi file .db từ điển tự mang metadata giấy phép và ghi công của chính nó; Attribution (FR109) và ghi công phát hành (FR38) dựng từ các file có mặt. | Binds: C3, C10 | Prevents: thực thi FR112 (gỡ một lớp) để lại ghi công mồ côi cho nguồn không còn trong bản phát hành.
- (decision) Danh sách/lọc/sắp xếp/tìm kiếm của Library đọc từ library-index.db; meta.json là thứ Indexer đọc khi quét (FR99) và là đường dựng lại khi chỉ mục vắng. | Binds: C1 | Prevents: hai đường đọc danh sách Library cài lệch nhau.
- (assumption) Các crate SSE research nêu (reqwest-sse, sseer) CHƯA xác nhận giấy phép — đã gỡ khỏi Stack, chuyển sang Deferred. Vi phạm chính convention NFR15 mà spine đặt ra nếu để lại. Phân tích khung SSE bằng tay là phương án hợp lệ.
- (event) spine finalized
- (event) Q3 ĐÓNG 2026-08-02: tác giả Đặng Thế Kiệt đồng ý bằng văn bản cho dùng data HVTĐTD. Hàng Deferred "HVTĐTD (Q3)" đóng — đúng như đã dự liệu, chỉ thêm một file .db lớp gỡ rời, KHÔNG đổi kiến trúc. AD-10 đã bao trọn
- (decision) AD-10 SỬA RULE TẠI CHỖ (không đánh số mới): trường metadata giấy phép trong mỗi file .db phải biểu diễn được cả giấy phép mở (CC-BY-SA, Unicode License) LẪN phép sử dụng riêng do tác giả cấp — HVTĐTD thuộc loại thứ hai và không thuộc GPL v3. | Prevents: ai đó mô hình hoá license thành enum các giấy phép mở, rồi HVTĐTD không biểu diễn được và bị gán nhầm CC-BY-SA — sai lệch pháp lý ghi thẳng vào màn hình Attribution
- (constraint) Dữ liệu HVTĐTD dùng theo PHÉP chứ không theo giấy phép mở; phép rút lại được (R12 trong PRD). Giữ nguyên hình dạng lớp gỡ rời dù đã được đồng ý — AD-10 + FR112 đã là biện pháp
- (decision) AD-34 MỚI — Sàn khả năng tiếp cận là cấu trúc, không phải kỷ luật (NFR17). | Binds: C1, C2, C8, toàn bộ frontend | Prevents: NFR17 thoái hoá thành kỷ luật cá nhân qua bảy giai đoạn giống hệt cách AD-13 phải cưỡng chế FR77 và AD-21 phải cưỡng chế NFR16; lỗi chỉ lộ ra khi ai đó thử dùng bàn phím và đã quá muộn để sửa rẻ. | Rule ba vế: (1) mọi thao tác đi qua một Command Registry duy nhất, handler chuột chỉ được dispatch một command đã đăng ký chứ không tự cài đặt thao tác — nhờ vậy "thao tác nào chưa gán được phím" là câu hỏi liệt kê được tự động; (2) mỗi chế độ và mỗi panel khai báo điểm vào focus, chuyển panel dockview phải dời focus DOM tường minh; (3) màu chỉ đến từ bộ token đã kiểm tương phản ở cả hai theme, cấm giá trị màu viết thẳng trong component
- (decision) AD-35 MỚI — Hợp đồng flush của Editor (NFR18). | Binds: C2, C9 | Prevents: AD-1 cho phép văn bản đang gõ là state cục bộ frontend nhưng KHÔNG định nghĩa nhịp flush; Giai đoạn 2 dựng Editor và chọn debounce thuần — mà debounce thuần bị reset bởi mỗi phím gõ nên KHÔNG BAO GIỜ kích hoạt khi người dùng gõ liên tục, mất không giới hạn công việc mà vẫn "đúng đặc tả auto-save". | Rule: flush khi (a) ngừng gõ khoảng 2 giây, (b) TRẦN CỨNG 5 giây kể từ lần flush trước — đồng hồ trần KHÔNG được reset bởi phím gõ, (c) xác nhận segment, (d) rời segment, (e) đóng Tác phẩm hoặc thoát app. Flush đi qua đúng store::Writer nối tiếp của AD-11, không mở kết nối riêng. Flush chỉ được coi là xong SAU KHI đã ghi vào WAL — nếu không thì ngưỡng 5 giây của NFR18 không bảo đảm gì
- (decision) Rà AD-11, AD-12, AD-31 với NFR18: cả ba VẪN ĐÚNG, không cần sửa. AD-31 (auto-save không tạo SegmentVersion) là điều kiện để nhịp 2s/5s không sinh hàng trăm phiên bản mỗi giờ; AD-11 cho flush một đường ghi; AD-12 giữ checkpoint khỏi rơi vào lúc gõ. Khoảng trống duy nhất là hợp đồng flush — nay đã đóng bằng AD-35
- (decision) Hàng Deferred "ngưỡng kích thước WAL buộc checkpoint" (Giai đoạn 2) bổ sung điều kiện mở lại: phải dò cùng lúc với nhịp flush của AD-35 sao cho đạt NFR18 mà không phạm NFR2 — hai thứ này đo trên cùng một Editor thật nên tách ra dò riêng là lãng phí
- (version) Phạm vi spine cập nhật: 112 FR giữ nguyên, NFR 16 → 18 (thêm NFR17 sàn a11y, NFR18 cửa sổ mất dữ liệu). AD-1..AD-33 giữ nguyên không đánh số lại; AD-34 và AD-35 nhận số cuối dãy
- (event) Reviewer Gate chạy inline (session cấm subagent). lint_spine.py: 0 findings trước và sau khi vá
- (decision) Lens đối kháng bắt lỗ 1 ở AD-34: hai giai đoạn cách nhau nhiều tháng đăng ký command id trần trùng nhau sẽ ghi đè âm thầm. Vá: command id dùng khoá chấm có tiền tố miền, cùng hình dạng khoá chuỗi i18n
- (decision) Lens đối kháng bắt lỗ 2 ở AD-35: chấp nhận thay đổi từ Review Mode (FR94) và điền sẵn từ TM khớp 100% (FR58) là hành động dứt khoát chứ không phải văn bản đang soạn. Nếu định tuyến qua bộ đệm gõ thì một thao tác người dùng thấy đã xong lại nằm chờ tới 5 giây và biến mất khi app sập. Vá: thao tác rời rạc ghi ngay, không đi qua bộ đệm
- (decision) Lens phiên bản: AD-34 và AD-35 không ràng buộc công nghệ mới nào — CommandRegistry là mẫu thiết kế trong mã dự án, WCAG AA là chuẩn ổn định. Không có phụ thuộc mới cần rà GPLv3 (NFR15), bảng Stack giữ nguyên
- (event) spine updated — AD-34, AD-35 thêm mới; AD-10 sửa Rule tại chỗ; hàng Deferred HVTĐTD đóng. AD-1..AD-33 giữ nguyên không đánh số lại
- (event) Đồng bộ bốn thay đổi upstream dồn lại. PRD 112 → 118 FR. Áp bài kiểm "hai đơn vị dựng độc lập có chọn lệch nhau không": phần lớn là SỬA RULE TẠI CHỖ, chỉ một thứ đáng thành AD mới
- (decision) AD-23 SỬA RULE TẠI CHỖ: scope tĩnh thêm $RESOURCE/fonts/** chỉ đọc, bên cạnh $RESOURCE/dict/**. Không phải AD mới vì bất biến không đổi — vẫn là phạm vi filesystem hai tầng do Tauri cưỡng chế, chỉ thêm một đường tài nguyên. AD-15 CSP cấm origin từ xa nên font BẮT BUỘC đóng gói, điều này đã đúng sẵn
- (assumption) Font Source Serif 4 + Source Han Serif Regular + Source Sans 3 là SIL OFL và tương thích GPLv3 khi đóng gói kèm — CHƯA KIỂM CHỨNG. Dung lượng ước 30-50MB trên nền database 130MB, trần NFR6 150-200MB — CHƯA ĐO. Cả hai vào Deferred, không được ghi thành decision
- (decision) AD-18 SỬA RULE TẠI CHỖ — đây là chỗ nguy hiểm nhất trong cả lượt này: FR118 ưu tiên theo XUẤT XỨ chồng lên ngữ nghĩa HỢP NHẤT hai tầng của TM. Hai chiều sắp xếp trên cùng một truy vấn mà không khai thứ tự thì Giai đoạn 4 và Giai đoạn 6 sẽ cài lệch nhau. CHỐT: khoá sắp xếp CHÍNH là xuất xứ (của tôi trước), khoá PHỤ là tầng (Tác phẩm trước Global). Lý do xuất xứ thắng: một cặp TM toàn cục do CHÍNH người dùng dịch vẫn giống văn phong của họ hơn một cặp Tác phẩm do người khác dịch — mà mục đích của FR70 là học văn phong
- (decision) AD-31 SỬA RULE TẠI CHỖ: bảng máy trạng thái segment thêm cột XUẤT XỨ, ghi tại chuyển tiếp sang đã xác nhận. Kèm hợp đồng phụ bắt buộc — hệ thống phải theo dõi "văn bản đích có bị sửa kể từ khi nạp segment hay không". Đây là chỗ dễ cài lệch: cờ dirty hay so sánh văn bản là hai cách cho kết quả khác nhau ở ca người dùng gõ rồi hoàn tác về nguyên trạng. CHỐT dùng SO SÁNH VĂN BẢN với bản lúc nạp, không dùng cờ dirty
- (decision) AD-4 SỬA RULE TẠI CHỖ: đường nhập song ngữ FR115 tính ranh giới segment ở CẢ HAI PHÍA cùng lúc, khác đường nhập hiện có vốn chỉ tạo phía nguồn. Bất biến không đổi — vẫn tính một lần lúc nhập và lưu xuống, không bao giờ tính lại
- (decision) AD-5 LÀM RÕ, không đổi Rule: thao tác nối câu ở FR116 xảy ra TRƯỚC KHI segment được ghi xuống đĩa, trong màn xem trước — nên AD-5 về hưu + tạo mới KHÔNG áp dụng ở đó vì chưa có segment nào tồn tại. Ngăn đúng một cách cài sai rất dễ xảy ra: cài FR116 thành thao tác gộp sau khi nhập, tức là tạo rồi cho về hưu ngay hàng nghìn segment
- (decision) AD-36 MỚI — Vòng đời mục Glossary ba trạng thái, và chỉ trạng thái cuối được chèn vào prompt. | Binds: C4, C6, C3 | Prevents: (1) một giai đoạn sau truy vấn thẳng bảng Glossary và chèn cả mục chờ chốt vào prompt, làm FR70 gửi trường trống và gợi ý sai cho mô hình rằng thuật ngữ không có bản dịch quy ước; (2) âm Hán Việt bị cài lại lần thứ hai bên trong glossary/ thay vì đọc qua cổng DictionarySource, sinh ra hai nguồn sự thật cho cùng một dữ kiện
- (decision) Rule của AD-36: ba trạng thái là ứng viên (bảng chờ, AD-20) → chờ chốt bản dịch → đã chốt. Trường bản dịch nullable cho tới khi chốt. glossary/ phơi ra một truy vấn DUY NHẤT trả về mục ĐỦ ĐIỀU KIỆN CHÈN; ai/ không có đường nào khác chạm dữ liệu Glossary. Âm Hán Việt cho FR113 đọc qua cổng DictionarySource, không cài lại — thêm cạnh glossary/ → dict/ vào đồ thị phụ thuộc, không tạo chu trình
- (constraint) ERD cập nhật ba thực thể: GLOSSARY_ENTRY thêm trạng thái bản dịch và cho phép bản dịch rỗng; SEGMENT thêm xuất xứ; TM_UNIT thêm xuất xứ trong metadata (AD-6 đã cho phép vì mục TM là cặp văn bản cộng metadata, độc lập segment.id)
- (event) Reviewer Gate chạy inline. lint_spine.py 0 findings. Kiểm chu trình đồ thị phụ thuộc sau khi thêm cạnh glossary → dict: KHÔNG có chu trình
- (decision) Lens đối kháng rà lại AD-13 với cạnh mới: AD-13 cấm module ngoài ai/ import ai/, chiều ngược lại hợp lệ. Cạnh glossary → dict KHÔNG chạm AD-13 vì cả hai đều không phải ai/. Đồ thị vẫn một chiều
- (event) spine updated — AD-36 thêm mới; AD-4, AD-5, AD-18, AD-23, AD-31 sửa Rule tại chỗ; ERD ba thực thể thêm trường; đồ thị module thêm cạnh glossary → dict; Deferred thêm hai hàng cho mũi thăm dò font. AD-1..AD-35 giữ nguyên không đánh số lại. Phạm vi frontmatter 112 → 118 FR
- (event) Lượt cập nhật 2026-08-03. Đính chính: hàng bàn giao FR115-FR118 KHÔNG còn tồn, lượt 2026-08-02 23:26 đã đóng hết. Khoảng cách thật là spine 118 FR vs SPEC 121 FR — thiếu FR119, FR120, FR121. Ice chọn đường KÈM CẶP
- (decision) Ice CHỐT: cấu trúc đoạn lưu bằng CỜ KẾT ĐOẠN trên SEGMENT, tính lúc nhập cùng lượt với ranh giới câu. Loại phương án thực thể PARAGRAPH riêng (nặng: AD-3/AD-4/AD-5 đều phải mở rộng, gộp segment xuyên đoạn thành ca mới) và phương án suy ra lúc xuất (đi ngược AD-4: đổi quy tắc đoán đoạn sẽ đổi im lặng kết quả xuất của mọi Chương cũ, và cột phải không có văn bản gốc để đoán nên phải suy ngược qua segment — đúng chỗ hai giai đoạn cài lệch nhau)
- (question) FR120 rà xong: KHÔNG cần gì ở tầng kiến trúc, chỉ đọc trạng thái segment sẵn có mà AD-31 đã bao trọn
- (decision) Ice CHỐT cách chặn nhập ngược bản .docx một khối: ĐOÁN THEO HÌNH DẠNG BẢNG, không đóng dấu metadata. Lý do chọn: chạy được với mọi file dù ai tạo, không phụ thuộc metadata mà công cụ trung gian có thể bóc mất. Loại phương án đóng dấu (không phủ được file người khác tạo) và phương án quy ước tên file (tên đổi được nên là gợi ý chứ không phải rào chắn)
- (decision) TINH CHỈNH trong phương án Ice đã chọn, để bịt ca từ chối oan: dấu hiệu KHÔNG phải số hàng đơn thuần mà là MỘT HÀNG DUY NHẤT VÀ ô chứa NHIỀU HƠN MỘT ĐOẠN. Chương một câu ra một hàng một đoạn nên đi qua bình thường. Dùng được chính cờ kết đoạn vừa chốt nên không thêm cơ chế mới
- (assumption) Ca sót lại sau tinh chỉnh, ghi để không ai tưởng đã kín: Chương ngắn chỉ có MỘT đoạn nhưng nhiều câu, xuất một khối ra một hàng một đoạn — không phân biệt được với bản theo câu của cùng Chương đó. Hẹp hơn hẳn ca gốc và hậu quả nhẹ vì đúng là một đoạn thật, nhưng vẫn ghi đè nhiều segment bằng một khối. Chấp nhận có ý thức
- (decision) AD-37 MỚI — Cấu trúc đoạn là dữ liệu được lưu, không phải thứ suy ra lúc xuất. Binds C1/C2/C8/C9. Prevents: FR121 hứa hai ô giữ đúng số lần xuống đoạn như nhau nhưng mô hình dữ liệu không có khái niệm đoạn; Giai đoạn 5 sẽ đoán từ văn bản gốc, mà cột phải KHÔNG có văn bản gốc để đoán nên phải suy ngược qua segment bằng quy tắc thứ hai — hai quy tắc trên cùng một Chương ra số đoạn lệch nhau
- (decision) AD-38 MỚI — Nhập .docx phân biệt hai hình dạng bảng trước khi chạy alignment. Binds C8. Prevents: bản một khối (FR121) và bản theo segment (FR87) cùng đuôi cùng là bảng hai cột; kéo nhầm bản một khối vào app thì FR91 alignment ghi đè cả Chương đã xác nhận bằng một khối văn bản, KHÔNG báo lỗi vì vẫn là bảng hai cột hợp lệ
- (decision) AD-5 SỬA RULE TẠI CHỖ: chỗ đánh dấu khi đọc (FR119) trỏ tới segment về hưu thì Ở LẠI, không xoá im lặng — hiện kèm ghi chú câu này đã đổi, vẫn mở về đúng vị trí. Cùng triết lý FR58 mà AD-5 đang mang: hệ thống không tự quyết thay người dùng rằng một thứ đã hết giá trị
- (constraint) ERD thêm READING_MARK (thuộc WORK, trỏ tới SEGMENT, sống sót khi segment về hưu) và SEGMENT thêm cờ kết đoạn
- (event) Reviewer Gate chạy inline (session cấm subagent). lint_spine.py: 0 findings trước và sau khi vá
- (decision) Lens đối kháng bắt ba lỗ ở AD-37, cả ba là chỗ hai giai đoạn chọn khác nhau mà vẫn tin mình đúng. Vá bằng bảng ba ca biên: GỘP cờ theo câu cuối nhóm gộp; TÁCH cờ theo mảnh cuối, mọi mảnh trước tắt (một đoạn tách làm ba câu vẫn là một đoạn); SEGMENT CUỐI CHƯƠNG cờ luôn TẮT vì đoạn cuối kết thúc do hết Chương chứ không do một lần xuống dòng — không khai thì mỗi Chương xuất ra thừa một dòng trống, nhân với 2000 Chương
- (decision) Lens đối kháng bắt lỗ thứ tư: AD-38 bản đầu gọi module 'import' KHÔNG TỒN TẠI trong cây nguồn — core/export/ mới là nơi sở hữu cả phân tích .docx lẫn alignment. Sửa Rule gọi đúng tên: kiểm hình dạng là CỔNG VÀO của đường nhập .docx trong core/export/
- (version) Lens phiên bản, kiểm trên web: docx-rs 0.4.22 đang ghim LÀ bản mới nhất (phát hành 2026-07-21), pin đúng. Crate CÓ read_docx và đủ kiểu Table/TableRow/TableCell/TableCellContent nên AD-38 khả thi. NHƯNG nó định vị là bộ GHI, tài liệu nội tuyến 5,53%, và hệ sinh thái đã sinh fork chuyên đọc (docx-reader) — dấu hiệu đường đọc là phần yếu. Thêm hàng Deferred cho mũi thăm dò đường đọc trước Giai đoạn 5; docx-reader và rdocx CHƯA xác nhận giấy phép, phải rà GPLv3 (NFR15) trước khi vào Stack, cùng khuôn hàng SSE
- (constraint) AD-37 và AD-38 KHÔNG ràng buộc crate nào — bất biến là cấu trúc đoạn được lưu và kiểm hình dạng ở cổng vào trước mọi lệnh ghi, đúng với bất kỳ thư viện docx nào. Nên rủi ro thư viện đường đọc không lan vào spine
- (event) spine updated — AD-37, AD-38 thêm mới; AD-5 sửa Rule tại chỗ; ERD thêm READING_MARK và cờ kết đoạn trên SEGMENT; bảng nhóm năng lực C1/C2/C8/C9 nhận AD mới; Deferred thêm hàng thư viện đường đọc .docx. AD-1..AD-36 giữ nguyên không đánh số lại. Phạm vi frontmatter 118 → 121 FR, updated 2026-08-03
- (direction) Bàn giao từ bmad-prd 2026-08-03 lượt hai: PRD lên 131 FR / 19 NFR. Hai nhu cầu mới — nhập văn bản gốc TỪ URL WEBSITE (FR122-FR128, thay vì chỉ từ file/dán tay), và use case dịch bài báo tiếng Anh gửi người khác đăng (FR129-FR131 caption + xuất ảnh theo link gốc + khối ghi nguồn). NFR19 mới ràng buộc điểm ra mạng thứ ba
- (decision) AD-2 GIỮ NGUYÊN BA CỔNG — Ice chọn sau khi được trình bày ba phương án. Phát hiện làm câu hỏi sáng ra: 'tải nội dung' là HAI việc chứ không phải một — lấy byte (mãi mãi một cách làm) và bóc nội dung (FR123 nói v1 không có bộ đọc riêng theo site, tức là đã thấy trước hướng mở rộng). Lý do loại port thứ tư: ba port hiện có mỗi cái biến MỘT FR KHÓ thành hệ quả cấu trúc (FR36, FR77, FR96-98); port thứ tư không phục vụ FR nào đang có, và nó sẽ lên bốn để phục vụ đúng thứ v1 tuyên bố không làm. Loại luôn phương án gộp một module vì nó dính HTTP với phân tích HTML, khó kiểm chứng NFR19 bằng test
- (decision) AD-39 MỚI — Đường nhập là một pipeline có thứ tự cố định, dùng chung cho mọi nguồn. Binds C1/C2/C9. Prevents: FR126 (bảng mã) và FR125 (chuẩn hoá) đều PHẢI chạy trước bước tính ranh giới segment, mà AD-4 lại nói ranh giới tính một lần lúc nhập và lưu xuống — nếu một giai đoạn đặt chuẩn hoá sau khi tách hoặc coi nó là lớp hiển thị thì ranh giới đã lưu không khớp thứ người dùng nhìn thấy, và không có gì báo. Đồng thời ngăn ba đường nhập (file, URL, song ngữ) mỗi đường cài một thứ tự riêng
- (decision) AD-40 MỚI — Tách Fetcher khỏi Extractor, không nâng thành port. Binds C1. Fetcher: URL -> byte + content-type + charset khai báo, ĐÚNG MỘT cài đặt, không trait hoá. Extractor: byte -> mô hình nội dung có cấu trúc, là ĐIỂM MỞ RỘNG ĐÃ ĐẶT TÊN, v1 có đúng một cài đặt dùng chung (FR123). Bộ đọc riêng theo site khi xuất hiện sẽ thay Extractor tại chỗ, và CHỈ LÚC ĐÓ mới xét nâng thành cổng thứ tư theo thủ tục AD-2
- (decision) AD-41 MỚI — Scope mạng cưỡng chế ở tầng ứng dụng, KHÔNG phải ở capabilities Tauri, và phải nói thẳng đây là chỗ yếu hơn scope filesystem. Lý do: allowlist của FR122 dựng từ danh sách link người dùng vừa dán LÚC CHẠY, mà capabilities Tauri là khai báo TĨNH lúc build — không có cách nào diễn đạt 'chỉ các domain trong danh sách vừa dán'. AD-23 được framework cưỡng chế, AD-41 thì không; giấu sự khác biệt này sẽ khiến người ta tưởng NFR19 cũng cứng như NFR13
- (decision) AD-42 MỚI — Caption và alt-text là HAI Segment khác vai, không phải trường của ASSET. Binds C1/C2/C8. Prevents: FR129 tách bạch caption khỏi alt-text; một giai đoạn sẽ mô hình hoá caption thành cột text trên ASSET (rẻ hơn) còn giai đoạn khác thành Segment — cột text thì KHÔNG tham gia TM/Glossary/luồng xác nhận, nên FR129 hỏng im lặng đúng chỗ không ai nhìn. Rule: cả hai là Segment mang trường VAI (alt / caption); alt-text mang ord đúng vị trí ảnh (quy ước cũ giữ nguyên), caption mang ord NGAY SAU ảnh vì nó là chữ người đọc thấy dưới ảnh
- (decision) AD-43 MỚI — Xuất xứ tài liệu (FR128) là nguồn sự thật ở CHAPTER trong project.db; khối ghi nguồn (FR131) DỰNG LÚC XUẤT chứ không lưu sẵn. Binds C1/C8/C9. Prevents: lưu sẵn chuỗi ghi nguồn đã định dạng thì sửa một trường ở FR128 sẽ không lan sang các bản xuất cũ, và có hai nguồn sự thật cho cùng một dữ kiện. Kèm quy tắc FR130: đường xuất phải KIỂM TRƯỚC và liệt kê ảnh không có URL gốc; không có đường mã nào bỏ qua ảnh thiếu URL trong im lặng
- (constraint) ERD: ASSET thêm source_url (metadata FR127, có thể rỗng với ảnh người dùng tự thêm); CHAPTER thêm bốn trường xuất xứ FR128 (tác giả, tên báo/website, URL bài gốc, ngày đăng gốc); cạnh SEGMENT }o--|| ASSET đổi từ 'alt-text' sang 'alt-text hoặc caption — mang trường vai'. meta.json vẫn là cache dẫn xuất theo AD-33, không phải nguồn sự thật của xuất xứ
- (assumption) Chưa ghim crate nào cho ba việc mới: phát hiện bảng mã (FR126), HTTP client cho Fetcher (đã có reqwest trong Stack, nhiều khả năng dùng lại), và bóc nội dung chính kiểu Readability (FR123). Cả ba phải rà tương thích GPLv3 theo NFR15 trước khi vào Stack. AD-39/AD-40 KHÔNG ràng buộc crate nào — bất biến là thứ tự pipeline và ranh giới Fetcher/Extractor, đúng với bất kỳ thư viện nào; nên rủi ro thư viện không lan vào spine, cùng khuôn AD-37/AD-38
- (version) Lens phiên bản, kiểm trên crates.io 2026-08-03 cho ba thư viện mới. BẢNG MÃ: chardetng 1.0.0 (Apache-2.0 OR MIT) + encoding_rs 0.8.35 ((Apache-2.0 OR MIT) AND BSD-3-Clause) — CẢ HAI tương thích GPLv3, chardetng là bộ dò của Firefox cho nội dung web cũ nên đúng bài toán GBK/Big5 của FR126. BÓC NỘI DUNG: dom_smoothie 0.18.0 MIT, cập nhật 2026-06-07, ~214k lượt tải gần đây — chọn làm ứng viên dẫn đầu; readability 0.3.0 MIT nhiều lượt tải hơn nhưng ĐỨNG IM TỪ 12/2023 nên loại khỏi vị trí nền. HTTP: reqwest đã có trong Stack, nhiều khả năng dùng lại. KHÔNG crate nào được ghim vào Stack lần này — cả ba vào Deferred, đúng khuôn hàng SSE và hàng docx đường đọc
- (decision) Ba mũi thăm dò mới đặt TRƯỚC GIAI ĐOẠN 1 chứ không phải Giai đoạn 5 như các hàng Deferred cũ — lý do: đường nhập là thao tác ĐẦU TIÊN người dùng chạm vào sản phẩm, và FR126 đã ghi rõ hạng lỗi là thất bại im lặng. Bóc nội dung và phát hiện bảng mã chạy chung một mũi thăm dò trên site và file .txt Ice thật sự dùng. Tỉ lệ bóc sai cao KHÔNG chặn tiến độ vì đường sửa tay của FR123 vốn đã là điều kiện nghiệm thu — nó chỉ đổi mức đầu tư vào màn xem trước
- (event) Reviewer Gate chạy inline (session cấm subagent, cùng cách phiên trước). lint_spine.py 0 findings trước và sau vá. Ba lens: rubric walker, lens phiên bản, lens đối kháng — ghi ở reviews/*-2026-08-03b.md. Lens đối kháng bắt 6 lỗ, 4 nghiêm trọng, tất cả nằm trong vùng vừa thêm
- (decision) VÁ F1 (nghiêm trọng nhất) — AD-41 allowlist một tầng CHẶN LUÔN ảnh của FR127: bài ở example.com nhưng ảnh ở cdn.example.net hoặc CDN dùng chung, host ảnh không nằm trong danh sách link người dùng dán. AD-41 và FR127 mâu thuẫn ở tầng spine chứ không phải do hiểu sai. Vá bằng allowlist HAI TẦNG: tầng 1 host của link người dùng cấp (được tải tài liệu), tầng 2 host của tài nguyên được tham chiếu từ trang tầng 1 trong cùng lần nhập (CHỈ ẢNH, không bao giờ tài liệu). Tầng 2 hiện trong nhật ký domain dưới nhãn riêng. Ranh giới tầng 2 chỉ-ảnh là thứ giữ nguyên lệnh cấm 'tự đi tìm nội dung' của FR122
- (decision) VÁ F2 — ASSET mang NEO VỊ TRÍ CỦA CHÍNH NÓ trong Chương, độc lập với segment đi kèm; alt và caption treo vào neo đó. Lỗ: quy ước cũ để segment alt-text giữ vị trí ảnh, giả định NGẦM rằng ảnh nào cũng có alt-text — đứng được khi mọi ảnh đến từ .docx của người dùng, GÃY NGAY khi nhập từ web vì ảnh web thường không có thuộc tính alt. Không có neo riêng thì ảnh không alt không caption mất vị trí và FR42/FR43 hỏng ở chỗ không ai kiểm
- (decision) VÁ F3 — AD-39 nói 'một chuỗi duy nhất' nhưng không nói chuỗi sống ở đâu, trong khi AD-38 đặt cổng vào .docx ở core/export/ và AD-40 đặt web ở core/webimport/ — hai đơn vị sẽ mỗi bên cài một bản các bước dùng chung, đúng thứ AD-39 tồn tại để cấm và vi phạm luôn AD-1. Chốt: chuỗi sống ở core/segment/ (module vốn đã sở hữu tách/gộp/về hưu theo AD-3/4/5); các module nguồn chỉ CUNG CẤP BƯỚC ĐẦU VÀO rồi trao lại, không giữ bản sao
- (decision) VÁ F4 — bảng AD-18 thêm hai hàng cho loại dữ liệu hai tầng mới. LUẬT LÀM SẠCH (FR124) = HỢP NHẤT, không phải ghi đè: rác web có hai vòng đời khác nhau (loại chung cho mọi nguồn ở tầng toàn cục, loại riêng của một site hay bộ truyện ở tầng Tác phẩm), ngữ nghĩa ghi đè sẽ buộc người dùng chép lại toàn bộ luật chung vào từng Tác phẩm chỉ để thêm một mẫu. Là dữ liệu cộng dồn, giống TM chứ không giống Glossary. TÊN NGƯỜI DỊCH (FR131) = ghi đè
- (decision) VÁ F5 + F6 (mức vừa). F5: bước giải mã bảng mã của AD-39 chỉ áp cho nguồn KHÔNG tự khai bảng mã (.txt, .md, phản hồi HTTP); .docx bỏ qua vì là zip chứa XML đã khai encoding, chạy bộ dò thống kê trên byte zip cho ra rác. F6: 'không tải lại ảnh đã có' (NFR19) khoá theo source_url trong phạm vi cùng Tác phẩm, KHÔNG theo băm nội dung — hai khoá khác nhau cho số lời gọi mạng khác nhau, làm bộ test bắt buộc của AD-41 mất tính xác định
- (event) spine updated 2026-08-03 lượt hai — AD-39 (pipeline nhập thứ tự cố định), AD-40 (Fetcher|Extractor tách rời, KHÔNG nâng cổng), AD-41 (scope mạng tầng ứng dụng, hai tầng allowlist), AD-42 (caption và alt-text là hai Segment khác vai, ASSET mang neo), AD-43 (xuất xứ ở CHAPTER, khối ghi nguồn dựng lúc xuất) thêm mới. Sửa tại chỗ: AD-2 (ghi kết luận không nâng cổng), AD-4 (văn bản vào bước tách đã đi hết pipeline), AD-9 (ảnh web là file thật, URL là metadata), AD-15 (hai → BA điểm ra mạng, CSP giữ nguyên không nới), AD-16 (mở rộng sang HTML từ internet + ba ràng buộc siết thêm), AD-18 (hai hàng mới), AD-23 (trỏ sang AD-41). ERD thêm SOURCE_ORIGIN, neo ASSET, source_url, vai segment. Sơ đồ paradigm thêm webimport và điểm ra mạng thứ ba. Cây nguồn thêm core/webimport/. Bản đồ năng lực C1/C2/C8/C9/C10 cập nhật. Deferred thêm 4 hàng. AD-1..AD-38 giữ nguyên ID, không đánh số lại. Frontmatter 121 FR/18 NFR → 131 FR/19 NFR
- (decision) AD-39 SỬA RULE TẠI CHỖ — bổ sung bước TÁCH CHƯƠNG theo mẫu phân tách (FR14) vào chuỗi pipeline, đặt SAU chuẩn hoá và TRƯỚC xem trước. Lỗ hổng do bmad-ux phát hiện khi dựng màn xem trước. Lý do vị trí: màn xem trước của FR14 hiển thị 'đã nhận ra N Chương' cùng ba Chương đầu ba Chương cuối, nên tách Chương phải xong trước khi màn đó dựng được. KHÔNG thêm AD mới — đây là một mệnh đề còn thiếu của AD-39, không phải quyết định riêng
- (decision) ĐIỀU KIỆN ÁP DỤNG của bước tách Chương phát biểu theo HÌNH DẠNG ĐẦU VÀO chứ không theo danh sách đường nhập — khác đề xuất của Sally vốn liệt kê 'áp cho file, không áp cho URL'. Lý do: danh sách sai ngay khi có đường nhập thứ tư, hình dạng thì đúng mãi. Hai hàng: đầu vào đến thành MỘT DÒNG CHƯA CHIA CHƯƠNG (file txt/md/docx, dán tay) thì CÓ tách; đầu vào đã một đơn vị một Chương (mỗi link của FR122) thì KHÔNG
- (decision) THÊM VÀO PREVENTS của AD-39 ca hỏng cụ thể nhất và dễ viết test nhất: đặt tách Chương TRƯỚC giải mã bảng mã thì mẫu phân tách chạy trên chữ rác, không khớp gì, cả file 40MB ra ĐÚNG MỘT CHƯƠNG, không lỗi nào được ném, màn xem trước hiện 'đã nhận ra 1 Chương'. Đây chính là lỗi thất bại im lặng mà FR126 tồn tại để chặn — để hở nó là AD-39 tự mâu thuẫn với lý do nó ra đời
- (question) CÂU HỎI MỚI, ghi vào Deferred: ranh giới Chương ở đường nhập song ngữ FR115. Theo tiêu chí hình dạng thì file hai cột chứa cả bộ truyện CẦN tách Chương, nhưng PRD không nói mẫu phân tách khớp vào cột nguồn, cột đích, hay cả hai — ba lựa chọn cho ba kết quả khác nhau khi hai cột đánh số Chương lệch. Đây là câu hỏi SẢN PHẨM không phải kiến trúc; AD-39 đã cố định phần bất biến nên câu trả lời không lan ra ngoài một hàm. Hỏi chủ dự án trước Giai đoạn 3, KHÔNG chặn tiến độ vì ca một Tác phẩm một Chương (bài báo) không cần nó. Sally đọc FR115 là KHÔNG cần tách Chương; tôi đọc là CÓ — chỗ lệch này chính là dấu hiệu PRD chưa nói rõ
- (event) spine updated 2026-08-03 lượt ba, phạm vi hẹp: chỉ AD-39 sửa Rule và Prevents tại chỗ, thêm một hàng Deferred. AD-1..AD-43 giữ nguyên ID, không AD mới, không đụng AD nào khác. lint_spine.py 0 findings. status giữ final. Còn nợ: bmad-ux đóng mục 🔴 trong EXPERIENCE.md § Còn thiếu
- (decision) ĐÓNG hàng Deferred 'Ranh giới Chương ở đường nhập song ngữ (FR115)' — PRD chốt mẫu phân tách ÁP LÊN CỘT NGUỒN ngày 2026-08-03. Đúng như dự liệu khi ghi hàng đó: AD-39 đã cố định phần bất biến (bước tách Chương sau chuẩn hoá, trước xem trước) nên câu trả lời KHÔNG ĐỔI KIẾN TRÚC, chỉ điền vào một hàm. Không thêm AD, không đổi tiêu chí hình dạng đầu vào
- (change) Ghi chú FR115 trong Rule của AD-39 sửa từ 'PRD không nói khớp vào cột nào, cơ chế còn để ngỏ, xem hàng Deferred' thành câu trả lời thẳng: mẫu áp lên cột nguồn, kèm lý do một dòng. Tiêu chí hình dạng đầu vào GIỮ NGUYÊN không đụng
- (note) BÀI HỌC GHI VÀO CHÍNH HÀNG ĐÃ ĐÓNG: hàng Deferred đó RỘNG HƠN THỰC TẾ — tôi ghi ba lựa chọn để ngỏ trong khi tầng thiết kế đã chọn cột nguồn từ hôm trước và ghi kèm biểu thức thật trong bilingual-import.html. Sally phát hiện khi được nhờ đóng một mục khác. Trước khi ghi một hàng Deferred, soát xem tầng dưới đã trả lời chưa — nếu không thì spine tạo ra công việc giả và làm downstream tưởng còn phải quyết
- (event) spine updated 2026-08-03 lượt bốn, phạm vi một hàng Deferred và một ghi chú trong Rule AD-39. 43 AD giữ nguyên ID, không AD mới, lint 0 findings, status final
- (version) BA HÀNG FONT VÀO BẢNG STACK, đo và rà thật ở Story 1.1 ngày 2026-08-03: Noto Serif CJK TC 2.003 (chỉ Regular) · Source Serif 4 4.004 · Source Sans 3 3.052 — cả ba SIL OFL 1.1. Phiên bản ĐỌC TỪ BẢNG name CỦA CHÍNH TỆP chứ không chép từ trang release: Source Serif 4 trên kênh Google là 4.004, ĐI SAU bản Adobe 4.005R một phát hành (commit cuối chạm tệp trong google/fonts là 2021-11-17). Đừng chép 4.005R vào Stack. Rà giấy phép làm bằng cách mở tệp LICENSE trong bản release đã tải mà đọc, không tin nhãn GitHub — NFR15 đòi rà tường minh, chép kết luận người khác không phải là rà. Ba tệp ba tình trạng RFN: Noto Serif CJK không có cả dòng bản quyền; Source Serif 4 bản Google có bản quyền nhưng KHÔNG khai RFN; Source Sans 3 CÓ khai RFN 'Source'. Hệ quả: subset về sau chỉ tệp Sans bắt buộc đổi tên font
- (version) SỐ ĐO DUNG LƯỢNG FONT, đo trên macOS 15.7.7 x86_64, tauri-cli 2.11.4, crate tauri 2.11.5, profile release lto+opt-level=s. Payload 4 tệp trên đĩa 25,991 MiB (CJK Regular 23,405 + Latin biến thiên 2,585). .dmg baseline không font 1.402.311 B; có font 22.688.024 B; CHÊNH LỆCH 21.285.713 B = 20,300 MiB. AC1 nguyên văn quy về byte: 21.285.713 + 130.000.000 = 151,29 MB, DƯỚI trần NFR6 200 MB. (Bản đầu ghi 150,3 MB — phép cộng 20,30 MiB với 130 MB rồi dán nhãn MiB, không tồn tại trong hệ đơn vị nào; rà soát 2026-08-03 rút khỏi lưu thông. Quy ước từ nay: NFR6 đọc theo MB thập phân, quy về byte trước, 200 MB là TRẦN còn 150 MB là mốc kỳ vọng chứ không phải điều kiện đạt.) Hai ước tính cũ đều sai: 30–50 MB quá cao, 21,6 MB quá thấp — phần CJK là 23,4 MiB chứ không phải 19 MB vì phép chia zip 7 nét cho 7 giả định các nét bằng nhau mà chúng không bằng
- (assumption) RỦI RO NFR6 CÒN MỞ, đã ghi vào hàng Deferred đã đóng và vào mục Cần Ice quyết của báo cáo: 130 MB là database của BA NGUỒN ĐẦU TIÊN. Giả định A2 ước database đủ nguồn là 150–200 MB. ĐỌC SAI VÀ ĐÃ SỬA 2026-08-03 sau rà soát: A2 nói ngân sách 150-200 MB là trần CỦA CẢ BẢN CÀI ĐÃ GỒM FONT (prd.md:826, prd.md:1020), không phải dự báo dung lượng database. Cộng font lên trên trần đó là cộng font hai lần. Phép tính đúng là TRỪ DƯ ĐỊA: 200 - 21,29 font - 1,40 baseline app RỖNG - 130 ba nguồn đầu = CÒN ~47 MB, và 47 MB đó phải chứa các nguồn từ điển còn lại, chỉ mục FTS phụ ~17 MB, VÀ TOÀN BỘ MÃ SẢN PHẨM CHƯA VIẾT. Font không phải thủ phạm. Đây là quyết định tầng PRD đúng như AC4 nói, KHÔNG tự subset font, KHÔNG tự bỏ họ font, KHÔNG tự đổi sang font hệ điều hành. Khuyến nghị: không làm gì bây giờ, thêm một bước nghiệm thu vào Story 1.9 cộng 20,30 MiB font vào database thật rồi đối chiếu lại
- (decision) BIẾN THỂ VÙNG CHỐT TC — NotoSerifCJKtc-Regular.otf. Lý do nặng nhất KHÔNG phải thứ bàn giao dự đoán: bàn giao nói khác biệt là 'dáng chữ ưu tiên ở mã dùng chung', đúng nhưng phụ. Bản dựng thật cho thấy khác biệt lớn hơn là VỊ TRÍ DẤU CÂU — TC đặt 「，。」 giữa ô chữ, SC đặt góc dưới trái; dáng chữ khác ở một nhúm mã, dấu câu khác ở MỌI CÂU, mà Panel Nguyên văn là nơi mắt ở lâu nhất. Hai lý do còn lại: phạm vi dự án là dịch thuật TỔNG QUÁT (Ice bác giả định ngách truyện mạng từ giai đoạn brief, nên lập luận 'SC hợp truyện mạng' mất trọng lượng), và hai lớp từ điển Cổ hán văn + HVTĐTD đều là ngữ liệu cổ văn. CHI PHÍ ĐỔI Ý BẰNG 0: hai tệp lệch nhau 1.176 byte, đổi biến thể là thay một tệp và sửa một chuỗi
- (constraint) CẤU HÌNH TAURI ĐÃ KIỂM CHỨNG, Story 1.2 chép lại: assetProtocol.enable true + scope ["$RESOURCE/fonts/**"] đúng AD-23; csp font-src phải chứa 'asset:' và 'http://asset.localhost'; Cargo.toml BẮT BUỘC feature tauri/protocol-asset (tauri-build tự thêm và báo lỗi nếu thiếu). font-src asset: KHÔNG phải nới CSP theo nghĩa AD-15 cấm — AD-15 cấm ORIGIN TỪ XA, asset protocol là tài nguyên cục bộ đã đóng gói, không byte nào ra mạng. Đã dựng thật: cả 5 tệp font nạp thành công trong bản release qua resolveResource + convertFileSrc + FontFace API
- (note) BA BẪY GẶP THẬT, ghi để Story 1.2/1.3 không mất buổi lần mò. (1) bundle_dmg.sh CHẾT ở bước AppleScript khi không có phiên Finder tương tác ('Finder got an error: Can't set Finder window id … to 128'); đặt CI=true là Tauri truyền --skip-jenkins và bỏ bước trang trí đó — runner GitHub Actions của Story 1.3 sẽ gặp đúng lỗi này. (2) Khai [lib] trong Cargo.toml mà không có src/lib.rs thì cargo metadata gãy, Tauri CLI dừng trước cả khi biên dịch. (3) Số crate tauri và số CLI npm KHÔNG đi cùng nhau: crate có 2.11.5, @tauri-apps/cli mới nhất là 2.11.4 — Stack ghim 2.11.5 là ghim CRATE, đừng cố cài CLI 2.11.5 vì không tồn tại
- (event) STORY 1.1 KHÔNG ĐÓNG ĐƯỢC NỬA WINDOWS CỦA AC1. tauri-cli 2.11.4 trên macOS từ chối target msi: "invalid value 'msi' for '--bundles', possible values: ios, app, dmg" — .msi dựng bằng WiX v3 mà candle/light là chương trình Windows; target x86_64-pc-windows-msvc đã cài sẵn nên rào chặn ở tầng ĐÓNG GÓI chứ không ở tầng biên dịch Rust. AC1 đòi .msi chứ không phải NSIS nên không có đường vòng. Ước 16,0–20,3 MiB bằng phương pháp tự hiệu chuẩn: nén rời cùng payload rồi kiểm phương pháp trên chính phép đo macOS — .dmg hoá ra là UDZO/deflate (không phải UDBZ như tôi đoán) và zip -9 dự đoán tỉ lệ thật lệch 0,1%. Đề nghị thêm vào phạm vi Story 1.3 một bước ghi dung lượng .msi có/không font
- (event) spine updated 2026-08-03 lượt năm, phạm vi ba hàng Stack và hai hàng Deferred đóng lại. 43 AD giữ nguyên ID, không AD mới, không đụng AD nào. lint_spine.py 0 findings. Thêm font-spike-results-2026-08-03.md vào sources. status giữ final
- (decision) ĐÓNG nửa Windows của AC1 bằng cách THU HẸP AC chứ không bằng cách bỏ im lặng — Ice quyết 2026-08-03. Hai phép đo .msi chuyển sang Story 1.3 và trở thành AC mới trong epics.md, kèm yêu cầu ghi lại chế độ cài WebView2 và ghi lại ở MỖI LẦN PHÁT HÀNH. Bản gốc AC1 giữ dạng gạch ngang ở cả epics.md lẫn story file để đọc lại được. Lý do chấp nhận đo muộn: ước 16,0–20,3 MiB lọt trần với biên rộng hơn macOS và phương pháp ước đã tự kiểm sai số 0,1%; rủi ro Windows thật sự nằm ở chế độ cài WebView2 chứ không ở font, mà thứ đó chỉ CI bắt được chứ một phép đo một lần thì không. Bài học: khi một AC không chạy được vì ranh giới công cụ, thu hẹp AC và bàn giao tường minh sang story có đúng môi trường — đừng để AC treo, cũng đừng tick bừa
- (note) CÂU HỎI 'BẢN ADOBE CÓ ĐẸP HƠN KHÔNG' ĐÃ ĐÓNG, KHÔNG ĐỔI GÌ. Source Han Serif và Noto Serif CJK là CÙNG MỘT FONT, khác đúng cái tên trong bảng name — mà phần Hán chiếm 23,4/26 MiB của bộ font, nên đổi kênh ở đó được con số không. Phần Latin thì khác biệt CÓ THỂ tồn tại (Adobe 4.005R OTF/CFF tĩnh vs Google 4.004 TTF biến thiên, và hinting TrueType lộ rõ trên ClearType hơn trên macOS) nhưng chỉ chiếm 2,6/26 MiB, và đổi lại sẽ MẤT lợi thế RFN của Source Serif 4 bản Google cộng với 3 tệp thành 5. Ice chốt giữ kênh Google, không chạy phép so. Không tài liệu nào phải sửa
- (decision) RÀ SOÁT CODE-REVIEW 2026-08-03 SỬA MỘT LỖI ĐỌC ĐÃ LAN RA SÁU TÀI LIỆU. Báo cáo Story 1.1 hiểu giả định A2 ('ngân sách 150-200 MB') thành DỰ BÁO DUNG LƯỢNG DATABASE rồi cộng font lên trên ra 220,3 MB và gắn nhãn rủi ro đỏ. Sai: prd.md:826 nói 150-200 MB là trần CỦA CẢ BẢN CÀI KÈM TOÀN BỘ TỪ ĐIỂN, tức đã bao gồm font; prd.md:1020 nói ngân sách đó ĐỦ CHO toàn bộ nguồn. Cộng font vào chính cái trần là cộng font hai lần. Phép tính đúng là TRỪ DƯ ĐỊA và kết quả còn khó chịu hơn: 200 - 21,29 font - 1,40 baseline app RỖNG - 130 ba nguồn đầu = CÒN ~47 MB, phải chứa các nguồn còn lại + chỉ mục FTS phụ ~17 MB + TOÀN BỘ MÃ SẢN PHẨM CHƯA VIẾT. Hàng rủi ro đỏ GIỮ NGUYÊN, chỉ đổi thủ phạm: không phải font. BÀI HỌC: khi một giả định của PRD nói 'ngân sách X đủ cho Y', đừng đọc ngược thành 'Y sẽ nặng X'
- (decision) QUY ƯỚC ĐƠN VỊ NFR6, khai một lần dùng mãi: đọc theo MB THẬP PHÂN đúng đơn vị PRD đang viết, mọi phép đối chiếu QUY VỀ BYTE TRƯỚC rồi mới đổi. 200 MB là TRẦN; 150 MB là mốc kỳ vọng chứ KHÔNG phải điều kiện đạt — nhỏ hơn 150 MB là tốt, không phải trượt. Lý do phải khai: con số 150,3 MB mà sáu tài liệu ghim là phép cộng 20,30 MiB với 130 MB rồi dán nhãn MiB, không tồn tại trong hệ đơn vị nào; số đúng là 151,29 MB. Ở mốc 200 thì MB/MiB lệch 7%, đủ lật một phép nghiệm thu. AC1 còn viết 'phải NẰM TRONG trần 150-200 MB' khiến một bản cài GỌN HƠN 150 MB đọc thành trượt. Đề nghị tu chính PRD đã ghi vào báo cáo, KHÔNG tự sửa PRD
- (event) BA BÀN GIAO TREO NAY THÀNH AC THẬT trong epics.md, phát hiện khi rà soát 2026-08-03. (1) Story 1.2 nhận AC đưa bốn tệp font vào src-tauri/resources/fonts/ khớp SHA-256, kèm ba tệp giấy phép OFL — trước đó bốn tệp CHỈ tồn tại trong scratchpad và không story nào nhận việc. (2) Story 1.3: AC .msi đã sửa vì bản đầu đòi 'dựng bản phát hành' và 'cộng với dung lượng database' — mâu thuẫn trực tiếp với hai AC sẵn có của chính story ('không phải FR107' và 'không tải dữ liệu từ điển'), và 1.3 chạy TRƯỚC 1.9 nên chưa có dict-core.db; Covers thêm NFR6. (3) Story 1.9 nhận AC cộng font và baseline app thật vào TỔNG MỌI artifact dữ liệu (gồm bốn lớp gỡ rời của 1.10, không chỉ dict-core.db) — trước đó năm tài liệu viết 'đối chiếu lại ở Story 1.9' như thể đã lên lịch trong khi 1.9 chưa có AC nào như vậy
- (note) BA DANH SÁCH NGUỒN TỪ ĐIỂN ĐANG ĐÁ NHAU, chưa hợp nhất được, đã ghi vào báo cáo: prd.md:1020 (A2) liệt kê Unihan/Thiều Chửu/Cổ hán văn/VietPhrase; epics.md Story 1.9 liệt kê CVDICT/Unihan/CC-CEDICT/viwiktionary/en.wiktionary. Chỉ Unihan có mặt cả hai. Báo cáo Story 1.1 còn tự thêm HVTĐTD vào danh sách A2 — sai, vì AD-10 xếp HVTĐTD là lớp GỠ RỜI không thuộc GPL v3, tức Story 1.10 chứ không phải 1.9; danh sách bịa đó đã gỡ. Không hợp nhất được ba danh sách thì KHÔNG tính được dư địa 47 MB đủ hay thiếu. Việc tầng PRD
- (decision) KHÔNG DÙNG tauri-plugin-fs — Ice chốt 2026-08-03, áp ở Story 1.2. Cùng câu lý do đã loại tauri-plugin-keyring (AD-29 nguyên văn: 'một Tauri plugin tồn tại để PHƠI API RA JAVASCRIPT, đúng thứ NFR11 cấm'), nay áp cho fs: plugin fs phơi API hệ thống file ra JS, nhưng AD-1 đã nói frontend CHỈ render và giữ state UI — nó không có việc gì với filesystem. Cài plugin rồi thu hẹp scope là TỰ TẠO MỘT BỀ MẶT TẤN CÔNG RỒI RÀO LẠI; không cài là không có bề mặt để rào. Cùng lượt loại luôn tauri-plugin-sql (AD-11, rusqlite trực tiếp) và tauri-plugin-dialog. Ba tên đã vào danh sách 'Không dùng, đã loại có lý do' của bảng Stack. | Binds: tất cả | Prevents: một story sau cài fs 'cho tiện' rồi thu hẹp scope và tưởng như vậy là tương đương — nay scripts/check-deps.sh trả mã thoát khác 0 nếu bất kỳ tên nào xuất hiện trong cargo tree.
- (decision) PHẠM VI FILESYSTEM CỦA STORY 1.2 CƯỠNG CHẾ BẰNG assetProtocol.scope, KHÔNG bằng capabilities permissions. Capabilities chỉ khai core:default. Lý do: capabilities canh BỀ MẶT IPC tức webview, và vì không cài plugin fs/sql/dialog nào thì không có quyền filesystem nào để khai. Hai vùng $RESOURCE/** do assetProtocol.scope canh và framework CƯỠNG CHẾ THẬT (đo được: /etc/hosts trả HTTP 403). Vùng $APPDATA/** nghiệm thu bằng VẮNG MẶT BỀ MẶT, không bằng khai báo — mã Rust gọi std::fs không đi qua capabilities. | Binds: AD-23, AD-1, AD-29 | Prevents: báo cáo 'framework đã cưỡng chế mọi truy cập file', sai ở nửa $APPDATA và là đúng loại tự tin giả mà AD-41 lập tiền lệ phải nói thẳng.
- (note) RÀ NFR15 LƯỢT HAI, Story 1.2: 19 hàng phần mềm đọc bằng cách MỞ TỆP trong nguồn đã tải. 15 hàng khớp nhãn (✓). 3 hàng bằng chứng yếu hơn (⚠️) đã ghi thành bảng riêng trong spine: jieba-rs không kèm LICENSE trong .crate vì license.workspace=true, xác nhận bằng README trong nguồn; docx-rs KHÔNG có LICENSE lẫn mục README, bằng chứng duy nhất là license='MIT' trong Cargo.toml.orig + một dòng header trong src/xml_json/mod.rs — YẾU NHẤT CẢ BẢNG, nếu Giai đoạn 5 đổi sang docx-reader/rdocx thì rà lại từ đầu; dockview-vue không kèm tệp, xác nhận bằng banner @license MIT nhúng trong bundle dockview-core đã phát hành. 1 hàng SUÝT CHẤM SAI: tantivy-stemmers có LICENSE dùng gạch đầu dòng thay vì điều khoản đánh số nên bộ nhận dạng tự động đọc thành BSD-2; đọc bằng mắt thì mệnh đề 'Neither the name … may be used to endorse' CÓ MẶT → đúng BSD-3-Clause, nhãn đúng. BÀI HỌC LẶP LẠI TIỀN LỆ STORY 1.1 (source-han-serif bị GitHub gắn NOASSERTION): nhãn là dẫn xuất, và LẦN NÀY cả bộ nhận dạng tự động cũng là dẫn xuất — chỉ mắt người đọc văn bản mới là nguồn sự thật.
- (version) Phiên bản GIẢI QUYẾT THẬT đọc từ Cargo.lock và package-lock.json ngày 2026-08-03 (Story 1.2 cài trọn bảng Stack): reqwest 0.13.4 (thay chuỗi 'mới nhất lúc dựng' trong bảng) · tauri-build 2.6.3 · serde 1.0.229 · serde_json 1.0.151 · typescript 5.9.3 (⚠️ latest npm = 7.0.2, cài mặc định là vi phạm AC2 mà KHÔNG ném lỗi) · @tauri-apps/cli 2.11.4 (2.11.5 không tồn tại trên npm) · @tauri-apps/api 2.11.1 · @vitejs/plugin-vue 6.0.8 · vue-tsc 3.3.9. Bảy hàng mới đã thêm vào bảng Stack.
- (direction) Lượt cập nhật 2026-08-05: thêm AD cho ĐƯỜNG TRA CỨU TIẾNG ANH. Bàn giao từ correct-course (sprint-change-proposal-2026-08-05.md §2.4/§5) và deferred-work.md; mục này 🔴 CHẶN Story 1.11b. Đã xác minh: spine có AD-1..AD-43, KHÔNG AD nào phủ chiến lược truy vấn tiếng Anh; AD-26 tên đầy đủ là 'Ba nhánh truy vấn TIẾNG TRUNG'.
- (version) SỐ ĐO THẬT trên dict-core.db 194.998.272 byte (Story 1.10b + 1.11, 2026-08-05), là dữ kiện nền của AD mới: nguồn viwiktionary-en sinh ĐÚNG 9 cặp char_idx trên 119.039 đầu mục (0,0076%), chiếm 0,00067% tổng 1.341.179 cặp ⇒ nhánh 2 của AD-26 KHÔNG áp được cho tiếng Anh. FTS5 trigram thì CÓ áp: entry_fts MATCH '"dic"' ⇒ 572 hàng, 100% lang='en'. p95 release đo được: nhánh 1 B-tree 0,083 ms; nhánh 2 char_idx 1 ký tự 7,324 ms (trần 10 ms, dư địa 27%); nhánh 3 trigram 0,448 ms.
- (decision) AD-44 quyết định #1 — ĐIỀU PHỐI THEO HÌNH DẠNG CHUỖI TRUY VẤN, không theo ngôn ngữ Tác phẩm. Ice chốt 2026-08-05 sau khi được trình bày ba phương án. Truy vấn chứa ký tự Hán ⇒ đường zh; toàn Latin ⇒ đường en. Loại phương án 'chỗ gọi truyền source_language của Work': bôi đen 'API' trong một truyện Trung sẽ lọc lang='zh' và trả RỖNG IM LẶNG dù mục 'API' có thật ở lang='en' — tái tạo đúng lớp lỗi FR39/AD-26 tồn tại để chặn. Loại phương án 'chạy cả hai đường': nhánh char_idx 1 ký tự đã ở 7,324 ms trên trần 10 ms, dư địa 27%. Đây KHÔNG mâu thuẫn Quyết định #5 của Story 1.11 (cấm đoán LookupMode từ nội dung): LookupMode là luật về Ý ĐỊNH người dùng, script của chuỗi là thuộc tính CỦA DỮ LIỆU và deterministic.
- (decision) AD-44 quyết định #2 — ĐỊNH NGHĨA 'KÝ TỰ HÁN' LÀ MỘT, VÀ NÓ LÀ ĐỊNH NGHĨA MÀ char_idx ĐƯỢC DỰNG THEO (tools/dict-build/src/char_idx.rs::is_han). Lỗ nếu không khai: vị từ điều phối lúc chạy và vị từ lập chỉ mục lúc build là hai đơn vị ở hai workspace tách rời (chốt từ Story 1.9 AC4, không có cổng kiểm chéo) — hai định nghĩa lệch nhau làm một truy vấn được định tuyến sang đường zh rồi tra vào một char_idx chưa bao giờ lập chỉ mục ký tự đó, ⇒ rỗng, không lỗi nào được ném.
- (decision) AD-44 quyết định #3 — TẬP KHOÁ TRA CHÍNH XÁC TIẾNG ANH = {nguyên văn, dạng hạ chữ thường}, MỘT truy vấn (headword IN (?1,?2)), không fallback dây chuyền. STEMMING KHÔNG nằm trên đường nóng tra từ điển. Ice chốt 2026-08-05 SAU KHI SỐ ĐO LẬT lựa chọn trước đó của chính Ice. Bằng chứng đo trên dict-core.db thật: (a) đầu ra Porter không phải từ thật nên khoá stem khớp vào hư không — dictionari ⇒ 0 hàng, studi ⇒ 0, happi ⇒ 0, chỉ run ⇒ 1; (b) Wiktionary ĐÃ có sẵn mọi dạng biến thể làm đầu mục riêng — mẫu 16/16 có mặt GỒM CẢ BẤT QUY TẮC went/gone/children/happiest, thứ stemming về nguyên tắc KHÔNG làm được; quy mô corpus 7.656 đầu mục -ing, 8.855 -ed, 19.616 -s, 228 -est; (c) lỗ chữ HOA là thật và chưa ai ghi — headword='running' ⇒ 1 hàng nhưng 'Running' ⇒ 0, tức bôi đen một từ ở ĐẦU CÂU trả rỗng im lặng. Hạ chữ thường là THÊM khoá không phải THAY khoá: 1.635 đầu mục en có chữ hoa (API, Wikipedia, English) và 184 nhóm chỉ khác nhau ở chữ hoa.
- (decision) AD-44 quyết định #4 — CHUỖI CON 1–2 KÝ TỰ TIẾNG ANH KHAI LÀ KHÔNG HỖ TRỢ, và trả một trạng thái PHÂN BIỆT ĐƯỢC với 'không có kết quả'. Ice chốt 2026-08-05. Lý do: không chỉ mục nào phục vụ được — char_idx sinh đúng 9 cặp trên 119.039 đầu mục (0,0076%) và FTS5 trigram không lập chỉ mục token ngắn hơn 3 ký tự (đo: entry_fts MATCH '"山"' ⇒ 0 hàng). Loại phương án 'làm tràn qua nhánh exact' (QueryBranch trả về sẽ nói dối về đường đã đi) và 'hạ ngưỡng trigram xuống 1' (trả rỗng trong 0,01 ms không lỗi — đúng Bẫy 1 của Story 1.11). Tinh thần AD-26 giữ nguyên: rỗng IM LẶNG bị cấm, rỗng CÓ LÝ DO thì không.
- (decision) AD-44 quyết định #5 — RANH GIỚI 'mã riêng theo ngôn ngữ'. ĐƯỢC PHÉP đúng một chỗ: CHIẾN LƯỢC TRUY VẤN (chọn nhánh + SQL + tập khoá) bên trong core/dict/. CẤM ở ba chỗ: (1) cổng DictionarySource là một adapter cho mỗi TỆP .db (AD-10), không bao giờ cho mỗi NGÔN NGỮ — một adapter theo ngôn ngữ phá mệnh đề 'gỡ một lớp = xoá một file' của FR36; (2) hình dạng kết quả là MỘT — lang là một TRƯỜNG trên bản ghi, không phải một KIỂU riêng, nên không có EnglishEntryHit; (3) không bước hợp nhất nào giữa kết quả zh và en, cùng luật AD-19.
- (decision) AD-26 SỬA RULE TẠI CHỖ (không đánh số lại, không AD mới): (a) khai tường minh trong Rule rằng phạm vi là TIẾNG TRUNG và trỏ sang AD-44 cho tiếng Anh — hôm nay chỉ TIÊU ĐỀ nói 'tiếng Trung' còn thân Rule đọc như thể áp cho mọi ngôn ngữ; (b) dải hiệu năng công bố 0,15–4,5 ms của nhánh 2 đánh dấu LỖI THỜI — đo thật 2026-08-05 trên bản release là 7,324 ms cho truy vấn 1 ký tự, gấp 1,6 lần cận trên. Số cũ đo ở Giai đoạn 0 trên database BA nguồn; dict-core.db nay có SÁU, char_idx của 山 đi từ ~2.576 lên 3.177 hàng. Bàn giao từ §Completion Notes ② của Story 1.11.
- (assumption) GIỚI HẠN ĐÃ KHAI CỦA AD-44, ghi để không ai tưởng đã kín: (1) Đường tiếng Anh dựa vào tính chất 'corpus có sẵn mọi dạng biến thể làm đầu mục' — đúng với Wiktionary vi-extract đo hôm nay, KHÔNG phải một tính chất phổ quát của mọi nguồn EN. Nguồn EN thứ hai nghèo dạng biến thể hơn sẽ mở lại câu hỏi stemming. (2) 8.283 đầu mục EN chứa dấu cách (cụm từ) — bôi đen một cụm vẫn đi nhánh exact, hành vi đúng, nhưng chưa đo. (3) 184 nhóm đầu mục chỉ khác nhau ở chữ hoa ⇒ hạ chữ thường làm tập kết quả RỘNG HƠN ở đúng 184 chỗ đó; chấp nhận có ý thức vì AD-19 vốn đã nói kết quả giữ nguyên bất đồng và mang nhãn nguồn.
- (constraint) AD-17 LÀM RÕ RANH GIỚI (không sửa Rule): 'một component Matcher dùng chung' nói MỌI NƠI CẦN KHỚP NGÔN NGỮ DÙNG CHUNG MỘT CÀI ĐẶT — nó KHÔNG nói mọi đường đều phải gọi Matcher. Đường tra từ điển tiếng Anh không gọi (số đo: stem mua được ~0 recall). Glossary (FR51) và TM (FR61) thì CÓ — ở đó thuật ngữ do người dùng tự viết nên corpus không có tính chất 'mọi dạng biến thể đều là một mục', và stemming thật sự đáng tiền. ⇒ Story 1.12 vẫn dựng Matcher đầy đủ; nó chỉ không còn CHẶN Story 1.11b.
- (event) Reviewer Gate chạy inline tuần tự (phiên không tự gọi subagent — đúng tiền lệ hai lượt trước). lint_spine.py 0 findings CẢ TRƯỚC LẪN SAU khi vá. Ba lens: rubric walker, lens phiên bản, lens đối kháng — ghi ở reviews/review-ad-44-2026-08-05.md. Bắt 5 phát hiện phải xử, tất cả đã vá tại chỗ: A1 (nghiêm trọng — AD nói vị từ điều phối LÀ GÌ mà không nói CHẠY Ở ĐÂU; 1.11b và 1.13 sẽ đặt hai chỗ khác nhau) · A2 (nghiêm trọng — không luật nào về việc TỆP NÀO ĐƯỢC TRA, nên một đơn vị sẽ dựng sổ đăng ký 'tệp nào chứa ngôn ngữ nào', tức nguồn sự thật thứ hai sai im lặng khi thêm/gỡ lớp theo FR112) · A3 (vừa — truy vấn không Hán không Latin: 'không có kết quả' hay 'không hỗ trợ', hai đơn vị đọc khác nhau) · A4 (vừa — hạ chữ thường theo locale, ca Thổ Nhĩ Kỳ I/ı, cùng truy vấn ra hai kết quả trên hai máy) · V1 (cao — ba chuỗi stem trong bảng là ASSERTION từ hành vi kinh điển của Porter, CHƯA chạy qua stemmer thật; số hàng thì đo thật). A5/A6 rà xong kết luận không phải lỗ.
- (event) spine updated 2026-08-05 — AD-44 MỚI (đường tra cứu tiếng Anh: điều phối theo hình dạng truy vấn · hai nhánh en · tập khoá {nguyên văn, hạ chữ thường} · chuỗi con 1-2 ký tự khai không hỗ trợ · ranh giới mã-riêng-theo-ngôn-ngữ · NFR1 đo trên đường en). Sửa tại chỗ: AD-26 (phạm vi TIẾNG TRUNG đưa vào thân Rule + đánh dấu dải hiệu năng công bố đã LỖI THỜI, thay bằng số đo 2026-08-05), AD-17 (ghi chú ranh giới: dùng chung MỘT cài đặt không có nghĩa mọi đường đều phải gọi Matcher). Consistency Conventions hàng Tra cứu, Cây nguồn dict/, bản đồ năng lực C3+C4, hai hàng Deferred mới (stemming · cụm từ nhiều chữ). AD-1..AD-43 GIỮ NGUYÊN ID, không đánh số lại, không AD nào bị làm yếu. frontmatter updated 2026-08-03 → 2026-08-05, status giữ final. ⇒ MỤC 🔴 CHẶN Story 1.11b nay ĐÓNG.
