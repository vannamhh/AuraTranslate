---
scope: Toàn bộ AuraTranslate v1 — 10 nhóm năng lực C1–C10, 112 FR, 16 NFR
purpose: build-substrate
altitude: feature
updated: 2026-08-03T06:31
---

- (constraint) Nền tảng: desktop native macOS + Windows, hành vi tương đương (NFR14). Không web, không mobile.
- (constraint) Local-first tuyệt đối: không tài khoản, không cloud sync, không telemetry (NFR12, NFR13, FR104). Ra ngoài mạng CHỈ khi người dùng chủ động gọi AI.
- (constraint) Giấy phép GPL v3; mọi crate/thư viện phải tương thích GPLv3 (NFR15). GPLv3 chọn vì tương thích Apache-2.0.
- (constraint) Ứng dụng MỘT người dùng, một tiến trình. Cộng tác chỉ qua trao đổi file. KHÔNG CRDT, không đồng bộ, không giải xung đột đa người.
- (constraint) FR96-FR98: .atproj là nguồn sự thật, tự chứa, copy sang máy khác mở được. Chỉ mục Library là DẪN XUẤT, dựng lại được hoàn toàn.
- (constraint) FR77: ứng dụng phải chạy đầy đủ khi KHÔNG cấu hình AI. Mọi năng lực ngoài C6/C7 không được phụ thuộc AI.
- (constraint) FR36 + §8.6 PRD: nguồn từ điển theo mô hình lớp nền sạch + lớp gỡ rời. Thiều Chửu, Cổ hán văn, VietPhrase, HVTĐTD đều là LỚP GỠ RỜI. Gỡ bất kỳ lớp nào không làm hỏng tra cứu.
- (constraint) FR31/FR32: mọi định nghĩa hiển thị nguồn, không có chế độ ẩn; nguồn bất đồng thì hiển thị đồng thời, KHÔNG hợp nhất.
- (constraint) NFR16: toàn bộ chuỗi giao diện nằm ngoài mã nguồn, trong file tài nguyên, ngay từ dòng code đầu tiên. v1 chỉ tiếng Việt.
- (constraint) FR27 + NFR6: toàn bộ dữ liệu từ điển nhúng trong bản cài, 150-200MB, KHÔNG có cơ chế tải thêm sau cài.
- (constraint) FR111: cơ chế cập nhật chỉ kiểm tra và thông báo. Không tự tải, không tự cài (vì bản phát hành không ký số).
- (constraint) NFR11/FR67: API key nằm trong keychain/credential manager OS. Không ghi vào file cấu hình, file dự án hay log.
- (constraint) Cấu hình hai tầng Global -> Tác phẩm, tầng Tác phẩm ghi đè (FR46, FR68, FR69, FR103, FR57).
- (version) Giai đoạn 0 (2026-08-02) đã đo thật: lookup Rust p50 0,022ms / p95 0,046ms, payload IPC 679 byte; DB 130MB với 3 nguồn; FTS5 trigram trả RỖNG cho truy vấn 1-2 ký tự; unicode61 mặc định xoá dấu tiếng Việt.
- (decision) [ADOPTED] Chuyển mọi từ điển sang SQLite ở bước build, không đọc định dạng gốc lúc chạy. Parser chỉ nằm trong build tool, không vào bản phát hành. | Binds: C3, C9 | Prevents: mỗi nguồn một tầng truy vấn riêng + tự xây tầng hợp nhất + ràng buộc giấy phép parser lên bản phát hành.
- (decision) [ADOPTED] Ba nhánh truy vấn tiếng Trung: B-tree (tra chính xác, 0,02ms) / char_idx bảng đảo ngược (1-2 ký tự, 0,15-4,5ms) / FTS5 trigram (3+ ký tự). LIKE bị loại vì 20-50ms. | Binds: C3, C1 | Prevents: truy vấn 1-2 ký tự trả rỗng âm thầm.
- (decision) [ADOPTED] Chỉ mục FTS chính dùng remove_diacritics 0 (phân biệt dấu); chỉ mục xoá dấu chỉ tồn tại như chỉ mục PHỤ, không bao giờ là mặc định (NFR8, FR9). | Binds: C1, C3 | Prevents: gộp má/ma/mà/mả/mã/mạ thành một kết quả.
- (decision) [ADOPTED] Bỏ tauri-wire khỏi kế hoạch. Payload 679 byte, xa ngưỡng 10KB; chi phí serialize 0,002ms. | Binds: ranh giới IPC | Prevents: tối ưu hoá sớm không cần thiết.
- (event) Ice chọn Coaching path; deliverable duy nhất là ARCHITECTURE-SPINE.md (không cần deck/solution design/bản đồ giai đoạn).
- (direction) Altitude: feature — spine giữ cho các epic (dẫn xuất từ C1-C10) nhất quán. Downstream kế tiếp: bmad-create-epics-and-stories.
- (decision) Webview mỏng: 100% quy tắc nghiệp vụ ở Rust; frontend TypeScript chỉ render + giữ state UI (focus, scroll, selection, bố cục panel). Cấm cài đặt lại bất kỳ quy tắc nghiệp vụ nào ở TS. Ngoại lệ tường minh: văn bản đang gõ trong Editor là state cục bộ frontend, chỉ qua IPC khi auto-save / xác nhận segment / rời segment. | Binds: tất cả | Prevents: hai bản cài đặt tách câu/matching lệch nhau -> TM khớp sai âm thầm; quy tắc nghiệp vụ nằm trong webview mà Tauri v2 coi là không đáng tin.
- (decision) Paradigm: Hexagonal liều thấp trong Rust core. Đúng BA cổng: DictionarySource (FR36 lớp gỡ rời), TranslationProvider (FR77 chạy không AI), ProjectStore (.atproj vs chỉ mục). Phần còn lại là module Rust thường, KHÔNG trait hoá. | Binds: tất cả | Prevents: FR77 và FR36 thoái hoá thành kỷ luật cá nhân; đồng thời tránh rừng trait một người không nuôi nổi.
- (direction) Ice loại: B (lặp logic ở TS), C (frontend dày), hexagonal đủ liều, layered đơn giản, modular monolith theo C1-C10. Lý do loại C1-C10 thành crate: các nhóm năng lực chồng lấn nặng (TM+Glossary+RAG dùng chung matching) nên không phải ranh giới kỹ thuật tốt.
- (decision) Segment mang ID bền, không bao giờ đổi và không bao giờ tái dùng. Thứ tự trong Chương là cột RIÊNG (ord), sắp lại được mà không đụng id. | Binds: C2, C5, C7, C9 | Prevents: định danh theo vị trí -> tách một segment làm lịch sử phiên bản và ghi nhớ proofreader của mọi segment sau nó trỏ sai chỗ, không báo lỗi. Cũng loại băm nội dung: trong truyện dài, câu lặp lại hàng trăm lần sẽ dùng chung danh tính.
- (decision) TM khoá theo CẶP VĂN BẢN (nguồn -> đích) + metadata, KHÔNG theo segment id. Chuẩn ngành CAT tool. | Binds: C5, C2, C8 | Prevents: TM vỡ mỗi khi segment bị gộp/tách/tái tách; đồng thời là điều kiện để xuất TMX (FR64) có nghĩa. Hệ quả: sửa bản dịch đã xác nhận thì GHI THÊM cặp mới, không sửa cặp cũ (khớp FR63).
- (decision) Ranh giới segment được tính MỘT LẦN lúc nhập Chương và LƯU xuống .atproj. Không bao giờ tính lại lúc nạp. Cải thiện quy tắc tách câu chỉ áp dụng qua thao tác tái tách CHỦ ĐỘNG của người dùng trên từng Chương, có cảnh báo về dữ liệu sẽ về hưu. | Binds: C2, C5, C7, C9 | Prevents: một lần sửa quy tắc tách câu (FR23 [A4]) âm thầm tách lại toàn bộ Thư viện, làm lịch sử phiên bản + trạng thái xác nhận + ghi nhớ proofreader của mọi Chương cũ trỏ sai chỗ.
- (decision) Gộp/tách segment (FR78) là thao tác VỀ HƯU + TẠO MỚI: segment cũ đánh dấu về hưu (lịch sử phiên bản vẫn tra lại được), segment mới bắt đầu ở trạng thái CHƯA XÁC NHẬN với lịch sử rỗng. Cặp TM đã ghi ở lại nguyên. | Binds: C2, C5, C7, C9 | Prevents: segment ghép từ hai segment đã xác nhận tự nhận là đã xác nhận dù chưa ai đọc nó — cùng triết lý FR58 (hệ thống không bao giờ tự coi là đã xong).
- (decision) Năm loại kho, ranh giới sở hữu cứng: dict-core.db + <lớp gỡ rời>.db (chỉ đọc lúc chạy) | .atproj/ (nguồn sự thật của một Tác phẩm) | global.db (nguồn sự thật tầng Global) | library-index.db (DẪN XUẤT) | OS keychain (chỉ API key). | Binds: tất cả | Prevents: ba giai đoạn xây dựng cách nhau nhiều tháng chọn lệch nhau về nơi dữ liệu sống.
- (decision) library-index.db BẮT BUỘC là file riêng, không nằm chung global.db. | Binds: C1, C9 | Prevents: thao tác sửa chữa hiển nhiên nhất (xoá chỉ mục hỏng rồi dựng lại) xoá luôn Glossary/TM toàn cục người dùng tích luỹ hàng năm — vi phạm NFR10.
- (decision) Chỉ MỘT component (Indexer) được ghi vào library-index.db, và chỉ ghi SAU KHI .atproj đã ghi xong. Không có đường ghi nào khác. | Binds: C1, C9 | Prevents: chỉ mục thôi là dẫn xuất — có chỗ ghi thẳng vào chỉ mục cho nhanh thì FR98/NFR10 mất hiệu lực.
- (decision) .atproj là một THƯ MỤC: meta.json (metadata Library đọc được không cần mở SQLite) + project.db + assets/ (ảnh là file thật). | Binds: C1, C9, C2 | Prevents: Library phải mở hàng trăm db chỉ để lấy tên/bìa/tiến độ (đe doạ NFR4 <3s); ảnh phải qua IPC mỗi lần render (FR42, FR43). Đổi lại rủi ro copy thiếu thư mục con — chấp nhận.
- (decision) Mỗi lớp từ điển gỡ rời là MỘT FILE .db độc lập; năm nguồn nền sạch gộp trong dict-core.db. Runtime không có mã riêng cho từng nguồn. Gỡ một lớp = xoá một file. | Binds: C3, C10 | Prevents: FR112 (chính sách gỡ bỏ) biến thành thay đổi mã + dựng lại 150-200MB; FR36 chỉ nghiệm thu được bằng suy luận thay vì bằng test thật (xoá file -> chạy lại bộ test tra cứu). Tổng 5 file, dưới giới hạn ATTACH 10 của SQLite.
- (decision) TM hai tầng có ngữ nghĩa HỢP NHẤT (không phải ghi đè như Glossary/prompt/cấu hình AI): tra cứu trả kết quả cả hai tầng, TM Tác phẩm xếp trước. Xác nhận segment (FR56) CHỈ ghi vào TM Tác phẩm; TM toàn cục chỉ nhận qua thao tác chủ động (nhập TMX hoặc đẩy lên tầng toàn cục). | Binds: C5, C6 | Prevents: FR103 nói chung là 'tầng dự án ghi đè' -> ai đó cài TM theo ghi đè, TM toàn cục không bao giờ được dùng tới; hoặc ghi vào cả hai làm TM tiên hiệp trộn vào TM hợp đồng pháp lý, không gỡ ra được.
- (decision) Mỗi kho ghi được (.atproj/project.db, global.db, library-index.db) có ĐÚNG MỘT kết nối ghi, đặt sau một hàng đợi nối tiếp. Đọc dùng pool nhiều kết nối song song (WAL). Không module nào được tự mở kết nối ghi. | Binds: tất cả module ghi dữ liệu | Prevents: Giai đoạn 4 mở kết nối riêng ghi TM cho nhanh -> writer starvation + gai trễ không dự đoán được, NFR2 mất hiệu lực mà không lần ra được nguyên nhân. Hệ quả tốt: NFR2 đo được ở MỘT chỗ.
- (decision) FR77 cưỡng chế bằng quy tắc hướng phụ thuộc có test tự động: không module nào ngoài ai/ được import ai/. Chiều ngược lại hợp lệ (ai/ đọc glossary/, tm/, segment/ cho FR70). | Binds: tất cả | Prevents: FR77 thoái hoá thành kỷ luật cá nhân qua 7 giai đoạn; lỗi chỉ lộ ra khi một người dùng không có API key thử.
- (decision) Lắp prompt là một HÀM THUẦN trả về prompt đã lắp hoàn chỉnh; lời gọi AI nhận prompt đã lắp làm đầu vào. Không nối chuỗi rải rác ở chỗ gọi. | Binds: C6, C7 | Prevents: FR71 (xem prompt cuối cùng đã gửi) trở thành công việc đắt và không bao giờ chính xác 100%; đồng thời là công cụ chẩn đoán duy nhất khi AI không tuân thủ Glossary.
- (decision) Pipeline từ điển: build tool chạy tay trên máy Ice sinh các file .db, đẩy lên GitHub Release riêng có phiên bản (vd tag dict-v1). Repo chứa mã build tool + dict-manifest.toml ghi URL, SHA-256 và phiên bản nguồn thô của từng file. CI tải theo manifest, đối chiếu checksum, rồi đóng gói. | Binds: C3, C10, C9 | Prevents: nguồn thô 1,13GB hoặc .db 150-200MB lọt vào git; đồng thời giữ FR107 (ai cũng dựng lại được từ nguồn thô và đối chiếu checksum khớp).
- (version) Kiểm chứng web 2026-08-02: tauri 2.11.5 (Apache-2.0 OR MIT); rusqlite 0.40.1 (MIT); jieba-rs 0.10.3 (MIT); tantivy-stemmers 0.4.0 (BSD-3-Clause); docx-rs 0.4.22 (MIT); similar 3.1.1 (Apache-2.0 — nay đã xác nhận, tương thích GPLv3); dissimilar (Apache-2.0 OR MIT); keyring 4.1.6 (MIT OR Apache-2.0); dockview 7.0.4.
- (version) SỬA LỖI RESEARCH: WAL2 KHÔNG phải tính năng đã phát hành. Diễn đàn SQLite xác nhận vẫn là nhánh thử nghiệm, không có kế hoạch đưa vào bản chính, chưa ứng dụng thật nào dùng, gần như không có test. SQLite 3.53.x (06/2026) không có. Báo cáo technical research liệt kê WAL2 với dấu xanh làm biện pháp cho NFR2 — biện pháp đó không tồn tại.
- (decision) Chống gai trễ auto-save (NFR2): PRAGMA wal_autocheckpoint = 0 (TẮT auto-checkpoint). Một luồng nền trên kết nối RIÊNG gọi wal_checkpoint(PASSIVE) khi người dùng ngừng gõ một khoảng, và wal_checkpoint(TRUNCATE) khi đóng Tác phẩm/thoát app. Thời điểm checkpoint là quyết định của ứng dụng, không phải của SQLite. Phải có ngưỡng kích thước WAL buộc checkpoint để .db-wal không phình vô hạn khi gõ liên tục hàng giờ. | Binds: C9, C2 | Prevents: checkpoint mặc định (1000 trang) rơi đúng lúc đang gõ.
- (decision) Dùng crate keyring 4.1.6 TRỰC TIẾP trong Rust, KHÔNG dùng tauri-plugin-keyring. Lý do kiến trúc (mạnh hơn lý do phiên bản 0.1.0 quá non): một Tauri plugin tồn tại để phơi API ra JavaScript — đúng thứ NFR11 và nguyên tắc 'key không bao giờ qua IPC' cấm. Với webview mỏng, frontend không có việc gì với key. | Binds: C6 | Prevents: tự tạo ra một bề mặt tấn công không cần thiết.
- (decision) Frontend: Vue 3 + TypeScript, bố cục panel dùng dockview (bản Vue). FR17/FR18 gần như là mô tả chức năng của dockview. | Binds: C2 | Prevents: tự viết hệ dock/undock/tab/resize/preset.
- (assumption) Chọn giữa similar (diff cấp grapheme — quan trọng với dấu tiếng Việt và ký tự Hán) và dissimilar (semantic cleanup) là điểm HOÃN CÓ ĐIỀU KIỆN: cả hai đều tương thích GPLv3, thử trên dữ liệu thật ở Giai đoạn 5 rồi chốt.
- (decision) MỘT cửa sổ OS duy nhất; Library / Workspace / Chế độ đọc là ba CHẾ ĐỘ trong đó. Review Mode (FR92) là một bố cục dockview, không phải cửa sổ thứ hai. | Binds: C1, C2, C8 | Prevents: tách cửa sổ để lấy bảo mật là trả bằng chính thứ sản phẩm bán (FR16 - một cửa sổ thay cho bốn năm cửa sổ). Bù lại bằng AD CSP + AD cấm render HTML, vốn bịt đúng mối đe doạ thật (file .docx/.md reviewer gửi về).
- (decision) Phạm vi filesystem hai tầng. Scope TĨNH khai trong capabilities: $RESOURCE/dict/** (chỉ đọc), $APPDATA/** (đọc+ghi cho global.db và library-index.db). Scope ĐỘNG cấp lúc chạy khi người dùng chọn qua hộp thoại: các thư mục gốc Library (mặc định ~/Documents/AuraTranslate/) và đường dẫn export từng lần. Không đường mã nào chạm path ngoài scope. | Binds: tất cả | Prevents: 'không ai đọc được tài liệu của bạn' thoái hoá thành lời hứa thay vì ràng buộc do framework cưỡng chế.
- (decision) ĐÚNG HAI điểm ra mạng trong toàn bộ ứng dụng: (1) adapter TranslationProvider gọi endpoint tương thích OpenAI, (2) kiểm tra phiên bản mới (FR111). Không có điểm thứ ba. CSP của Tauri cấm mọi origin từ xa — không CDN, không font ngoài, không ảnh ngoài. | Binds: tất cả | Prevents: NFR12 (không telemetry) bị vi phạm vô tình qua crash reporter, thư viện analytics, hoặc một font CDN trong frontend.
- (decision) Nội dung nhập từ ngoài (.docx, .md, .txt của reviewer) KHÔNG BAO GIỜ được render như HTML. Rust phân tích thành mô hình dữ liệu có cấu trúc; Vue render từ mô hình đó. Không có đường nào đưa chuỗi từ file ngoài vào v-html. | Binds: C8, C1, C2 | Prevents: XSS trong webview có quyền gọi IPC — mối đe doạ thật, và là lý do research đòi tách cửa sổ.
- (decision) MỘT component Matcher dùng chung cho toàn bộ khớp ngôn ngữ (FR40 từ điển, FR51 Glossary, FR61 TM). Tiếng Trung: khớp chính xác + n-gram ký tự, tách từ qua jieba-rs khi cần. Tiếng Anh: stemming rồi token n-gram. | Binds: C3, C4, C5, C6 | Prevents: ba lần cài đặt riêng ở ba giai đoạn khác nhau -> Glossary bắt được biến thể mà TM không bắt được, không ai biết vì sao.
- (decision) MỘT component ScopeResolver dùng chung, và ngữ nghĩa mỗi loại dữ liệu khai báo TƯỜNG MINH: GHI ĐÈ (Glossary FR46, prompt FR69, cấu hình AI FR68) vs HỢP NHẤT (TM FR57). | Binds: C4, C5, C6, C9 | Prevents: FR103 phát biểu chung là 'tầng dự án ghi đè' -> mỗi giai đoạn cài lại theo cách hiểu riêng.
- (decision) Đường tra cứu trả về KẾT QUẢ THEO TỪNG NGUỒN; trong toàn bộ hệ thống KHÔNG TỒN TẠI bước hợp nhất/khử trùng lặp giữa các nguồn. | Binds: C3 | Prevents: ai đó thêm một bước dedupe cho gọn UI và làm mất FR31/FR32 — nguyên tắc nền, không phải tuỳ chọn hiển thị.
- (decision) Mọi đề xuất tự động (FR52 quét khi nhập, FR54 thu hoạch từ bản review) ghi vào BẢNG CHỜ riêng, không bao giờ ghi vào bảng Glossary đang dùng. Chỉ thao tác duyệt của người dùng mới chuyển từ bảng chờ sang Glossary. | Binds: C4, C8 | Prevents: FR55 (không cơ chế nào được tự ghi vào Glossary) thoái hoá thành kỷ luật — trụ định vị #1 phát biểu dưới dạng cấu trúc dữ liệu.
- (decision) Rust KHÔNG BAO GIỜ trả về văn bản hiển thị cho người dùng. Mọi lỗi và thông báo qua IPC là { code, message_key, params, retryable }; frontend phân giải message_key trong file tài nguyên chuỗi. | Binds: tất cả | Prevents: NFR16 (chuỗi giao diện ngoài mã nguồn từ dòng code đầu tiên) bị thủng ngay ở tầng lỗi — chỗ dễ quên nhất và đắt nhất để sửa sau.
- (decision) Streaming bản dịch AI dùng Tauri Channel API (không dùng event rời). Không dùng client SSE tự kết nối lại. Đứt luồng là quyết định tường minh của ứng dụng; thử lại phải do người dùng chủ động (FR75). Mọi lời gọi AI huỷ được giữa chừng (FR73). | Binds: C6, C7 | Prevents: auto-reconnect tạo yêu cầu mới hoàn toàn -> người dùng bị tính phí hai lần và nhận văn bản trùng lặp.
- (decision) Định danh: Tác phẩm mang UUID sinh lúc tạo, lưu trong meta.json (vì library-index tham chiếu xuyên .atproj và người dùng copy .atproj sang máy khác). Chương và Segment mang số nguyên CỤC BỘ trong project.db. Indexer phải phát hiện và cảnh báo hai Tác phẩm trùng UUID (hệ quả của việc nhân bản thư mục). | Binds: C1, C9 | Prevents: nhân bản một .atproj làm hai mục đụng nhau trong chỉ mục.
- (direction) Ice không chọn thêm chủ đề nào ngoài cấu trúc cửa sổ và phạm vi filesystem; các quyết định còn lại do tôi chốt theo phán đoán và đưa ra ở bước review.
- (event) Reviewer Gate chạy inline (session cấm subagent — kiểm tra yếu hơn context độc lập). lint_spine.py: 0 findings.
- (version) SỬA phiên bản sau khi tra thật: Vite 8.2.0 (không phải 7.x), Vue 3.5.40, dockview-vue 7.0.4 MIT peer vue ^3.4.0, libsqlite3-sys 0.38.1. SQLite đến từ feature bundled, không phải SQLite hệ điều hành; sàn cần: FTS5 trigram >=3.34, remove_diacritics 0 >=3.27.
- (decision) AD-30 Lược đồ có phiên bản, di trú chỉ tiến, từ chối mở phiên bản mới hơn. | Binds: C9, C1, tất cả | Prevents: .atproj sống qua 7 giai đoạn và 2-3 năm; FR97 hứa copy sang máy khác mở được nhưng máy đó chạy bản khác — không có quy tắc thì mỗi giai đoạn đổi lược đồ theo cách riêng và làm hỏng dữ liệu cũ im lặng. Đây là CẢ MỘT CHIỀU bị bỏ trắng ở bản chưng cất đầu, lens đối kháng bắt được.
- (decision) AD-31 Vòng đời trạng thái segment là máy trạng thái tường minh: auto-save KHÔNG tạo phiên bản; xác nhận tạo một phiên bản VÀ ghi cặp TM; sửa văn bản của segment đã xác nhận thì MẤT xác nhận. | Binds: C2, C5, C7, C9 | Prevents: (1) auto-save tạo phiên bản -> gõ một giờ sinh hàng trăm phiên bản, FR101 vô dụng; (2) sửa segment đã xác nhận mà vẫn 'đã xác nhận' -> không lần xác nhận nào nữa xảy ra, cặp TM mới không bao giờ được ghi.
- (decision) AD-32 Gộp/tách Chương CHỈ đổi chapter_id và ord; segment.id, lịch sử phiên bản, trạng thái xác nhận giữ nguyên. Khác cố ý với AD-5. | Binds: C1, C2, C5, C9 | Prevents: FR15 cài như 'tạo lại segment' phá sạch lịch sử của những Chương đã dịch xong, trong khi FR15 chỉ là thao tác tổ chức.
- (decision) AD-33 meta.json là cache DẪN XUẤT từ project.db, ghi bởi chính store::Writer của Tác phẩm trong cùng thao tác logic; không thành phần nào khác ghi vào, không thành phần nào coi là nguồn sự thật. | Binds: C1, C9 | Prevents: hai chủ sở hữu cho tiến độ (FR7) — Library hiện tiến độ cũ và không ai biết bản nào đúng.
- (decision) Ghi nhớ proofreader (FR84) khoá theo (work, chữ ký phát hiện), KHÔNG theo segment.id — FR84 nói phạm vi là 'trong cùng Tác phẩm', và nhờ vậy ghi nhớ sống sót qua gộp/tách segment. Sửa lại ERD bản đầu vốn gắn PROOF_IGNORE dưới SEGMENT.
- (decision) Mỗi file .db từ điển tự mang metadata giấy phép và ghi công của chính nó; Attribution (FR109) và ghi công phát hành (FR38) dựng từ các file có mặt. | Binds: C3, C10 | Prevents: thực thi FR112 (gỡ một lớp) để lại ghi công mồ côi cho nguồn không còn trong bản phát hành.
- (decision) Danh sách/lọc/sắp xếp/tìm kiếm của Library đọc từ library-index.db; meta.json là thứ Indexer đọc khi quét (FR99) và là đường dựng lại khi chỉ mục vắng. | Binds: C1 | Prevents: hai đường đọc danh sách Library cài lệch nhau.
- (assumption) Các crate SSE research nêu (reqwest-sse, sseer) CHƯA xác nhận giấy phép — đã gỡ khỏi Stack, chuyển sang Deferred. Vi phạm chính convention NFR15 mà spine đặt ra nếu để lại. Phân tích khung SSE bằng tay là phương án hợp lệ.
- (event) spine finalized
- (event) Q3 ĐÓNG 2026-08-02: tác giả Đặng Thế Kiệt đồng ý bằng văn bản cho dùng data HVTĐTD. Hàng Deferred "HVTĐTD (Q3)" đóng — đúng như đã dự liệu, chỉ thêm một file .db lớp gỡ rời, KHÔNG đổi kiến trúc. AD-10 đã bao trọn
- (decision) AD-10 SỬA RULE TẠI CHỖ (không đánh số mới): trường metadata giấy phép trong mỗi file .db phải biểu diễn được cả giấy phép mở (CC-BY-SA, Unicode License) LẪN phép sử dụng riêng do tác giả cấp — HVTĐTD thuộc loại thứ hai và không thuộc GPL v3. | Prevents: ai đó mô hình hoá license thành enum các giấy phép mở, rồi HVTĐTD không biểu diễn được và bị gán nhầm CC-BY-SA — sai lệch pháp lý ghi thẳng vào màn hình Attribution
- (constraint) Dữ liệu HVTĐTD dùng theo PHÉP chứ không theo giấy phép mở; phép rút lại được (R12 trong PRD). Giữ nguyên hình dạng lớp gỡ rời dù đã được đồng ý — AD-10 + FR112 đã là biện pháp
- (decision) AD-34 MỚI — Sàn khả năng tiếp cận là cấu trúc, không phải kỷ luật (NFR17). | Binds: C1, C2, C8, toàn bộ frontend | Prevents: NFR17 thoái hoá thành kỷ luật cá nhân qua bảy giai đoạn giống hệt cách AD-13 phải cưỡng chế FR77 và AD-21 phải cưỡng chế NFR16; lỗi chỉ lộ ra khi ai đó thử dùng bàn phím và đã quá muộn để sửa rẻ. | Rule ba vế: (1) mọi thao tác đi qua một Command Registry duy nhất, handler chuột chỉ được dispatch một command đã đăng ký chứ không tự cài đặt thao tác — nhờ vậy "thao tác nào chưa gán được phím" là câu hỏi liệt kê được tự động; (2) mỗi chế độ và mỗi panel khai báo điểm vào focus, chuyển panel dockview phải dời focus DOM tường minh; (3) màu chỉ đến từ bộ token đã kiểm tương phản ở cả hai theme, cấm giá trị màu viết thẳng trong component
- (decision) AD-35 MỚI — Hợp đồng flush của Editor (NFR18). | Binds: C2, C9 | Prevents: AD-1 cho phép văn bản đang gõ là state cục bộ frontend nhưng KHÔNG định nghĩa nhịp flush; Giai đoạn 2 dựng Editor và chọn debounce thuần — mà debounce thuần bị reset bởi mỗi phím gõ nên KHÔNG BAO GIỜ kích hoạt khi người dùng gõ liên tục, mất không giới hạn công việc mà vẫn "đúng đặc tả auto-save". | Rule: flush khi (a) ngừng gõ khoảng 2 giây, (b) TRẦN CỨNG 5 giây kể từ lần flush trước — đồng hồ trần KHÔNG được reset bởi phím gõ, (c) xác nhận segment, (d) rời segment, (e) đóng Tác phẩm hoặc thoát app. Flush đi qua đúng store::Writer nối tiếp của AD-11, không mở kết nối riêng. Flush chỉ được coi là xong SAU KHI đã ghi vào WAL — nếu không thì ngưỡng 5 giây của NFR18 không bảo đảm gì
- (decision) Rà AD-11, AD-12, AD-31 với NFR18: cả ba VẪN ĐÚNG, không cần sửa. AD-31 (auto-save không tạo SegmentVersion) là điều kiện để nhịp 2s/5s không sinh hàng trăm phiên bản mỗi giờ; AD-11 cho flush một đường ghi; AD-12 giữ checkpoint khỏi rơi vào lúc gõ. Khoảng trống duy nhất là hợp đồng flush — nay đã đóng bằng AD-35
- (decision) Hàng Deferred "ngưỡng kích thước WAL buộc checkpoint" (Giai đoạn 2) bổ sung điều kiện mở lại: phải dò cùng lúc với nhịp flush của AD-35 sao cho đạt NFR18 mà không phạm NFR2 — hai thứ này đo trên cùng một Editor thật nên tách ra dò riêng là lãng phí
- (version) Phạm vi spine cập nhật: 112 FR giữ nguyên, NFR 16 → 18 (thêm NFR17 sàn a11y, NFR18 cửa sổ mất dữ liệu). AD-1..AD-33 giữ nguyên không đánh số lại; AD-34 và AD-35 nhận số cuối dãy
- (event) Reviewer Gate chạy inline (session cấm subagent). lint_spine.py: 0 findings trước và sau khi vá
- (decision) Lens đối kháng bắt lỗ 1 ở AD-34: hai giai đoạn cách nhau nhiều tháng đăng ký command id trần trùng nhau sẽ ghi đè âm thầm. Vá: command id dùng khoá chấm có tiền tố miền, cùng hình dạng khoá chuỗi i18n
- (decision) Lens đối kháng bắt lỗ 2 ở AD-35: chấp nhận thay đổi từ Review Mode (FR94) và điền sẵn từ TM khớp 100% (FR58) là hành động dứt khoát chứ không phải văn bản đang soạn. Nếu định tuyến qua bộ đệm gõ thì một thao tác người dùng thấy đã xong lại nằm chờ tới 5 giây và biến mất khi app sập. Vá: thao tác rời rạc ghi ngay, không đi qua bộ đệm
- (decision) Lens phiên bản: AD-34 và AD-35 không ràng buộc công nghệ mới nào — CommandRegistry là mẫu thiết kế trong mã dự án, WCAG AA là chuẩn ổn định. Không có phụ thuộc mới cần rà GPLv3 (NFR15), bảng Stack giữ nguyên
- (event) spine updated — AD-34, AD-35 thêm mới; AD-10 sửa Rule tại chỗ; hàng Deferred HVTĐTD đóng. AD-1..AD-33 giữ nguyên không đánh số lại
- (event) Đồng bộ bốn thay đổi upstream dồn lại. PRD 112 → 118 FR. Áp bài kiểm "hai đơn vị dựng độc lập có chọn lệch nhau không": phần lớn là SỬA RULE TẠI CHỖ, chỉ một thứ đáng thành AD mới
- (decision) AD-23 SỬA RULE TẠI CHỖ: scope tĩnh thêm $RESOURCE/fonts/** chỉ đọc, bên cạnh $RESOURCE/dict/**. Không phải AD mới vì bất biến không đổi — vẫn là phạm vi filesystem hai tầng do Tauri cưỡng chế, chỉ thêm một đường tài nguyên. AD-15 CSP cấm origin từ xa nên font BẮT BUỘC đóng gói, điều này đã đúng sẵn
- (assumption) Font Source Serif 4 + Source Han Serif Regular + Source Sans 3 là SIL OFL và tương thích GPLv3 khi đóng gói kèm — CHƯA KIỂM CHỨNG. Dung lượng ước 30-50MB trên nền database 130MB, trần NFR6 150-200MB — CHƯA ĐO. Cả hai vào Deferred, không được ghi thành decision
- (decision) AD-18 SỬA RULE TẠI CHỖ — đây là chỗ nguy hiểm nhất trong cả lượt này: FR118 ưu tiên theo XUẤT XỨ chồng lên ngữ nghĩa HỢP NHẤT hai tầng của TM. Hai chiều sắp xếp trên cùng một truy vấn mà không khai thứ tự thì Giai đoạn 4 và Giai đoạn 6 sẽ cài lệch nhau. CHỐT: khoá sắp xếp CHÍNH là xuất xứ (của tôi trước), khoá PHỤ là tầng (Tác phẩm trước Global). Lý do xuất xứ thắng: một cặp TM toàn cục do CHÍNH người dùng dịch vẫn giống văn phong của họ hơn một cặp Tác phẩm do người khác dịch — mà mục đích của FR70 là học văn phong
- (decision) AD-31 SỬA RULE TẠI CHỖ: bảng máy trạng thái segment thêm cột XUẤT XỨ, ghi tại chuyển tiếp sang đã xác nhận. Kèm hợp đồng phụ bắt buộc — hệ thống phải theo dõi "văn bản đích có bị sửa kể từ khi nạp segment hay không". Đây là chỗ dễ cài lệch: cờ dirty hay so sánh văn bản là hai cách cho kết quả khác nhau ở ca người dùng gõ rồi hoàn tác về nguyên trạng. CHỐT dùng SO SÁNH VĂN BẢN với bản lúc nạp, không dùng cờ dirty
- (decision) AD-4 SỬA RULE TẠI CHỖ: đường nhập song ngữ FR115 tính ranh giới segment ở CẢ HAI PHÍA cùng lúc, khác đường nhập hiện có vốn chỉ tạo phía nguồn. Bất biến không đổi — vẫn tính một lần lúc nhập và lưu xuống, không bao giờ tính lại
- (decision) AD-5 LÀM RÕ, không đổi Rule: thao tác nối câu ở FR116 xảy ra TRƯỚC KHI segment được ghi xuống đĩa, trong màn xem trước — nên AD-5 về hưu + tạo mới KHÔNG áp dụng ở đó vì chưa có segment nào tồn tại. Ngăn đúng một cách cài sai rất dễ xảy ra: cài FR116 thành thao tác gộp sau khi nhập, tức là tạo rồi cho về hưu ngay hàng nghìn segment
- (decision) AD-36 MỚI — Vòng đời mục Glossary ba trạng thái, và chỉ trạng thái cuối được chèn vào prompt. | Binds: C4, C6, C3 | Prevents: (1) một giai đoạn sau truy vấn thẳng bảng Glossary và chèn cả mục chờ chốt vào prompt, làm FR70 gửi trường trống và gợi ý sai cho mô hình rằng thuật ngữ không có bản dịch quy ước; (2) âm Hán Việt bị cài lại lần thứ hai bên trong glossary/ thay vì đọc qua cổng DictionarySource, sinh ra hai nguồn sự thật cho cùng một dữ kiện
- (decision) Rule của AD-36: ba trạng thái là ứng viên (bảng chờ, AD-20) → chờ chốt bản dịch → đã chốt. Trường bản dịch nullable cho tới khi chốt. glossary/ phơi ra một truy vấn DUY NHẤT trả về mục ĐỦ ĐIỀU KIỆN CHÈN; ai/ không có đường nào khác chạm dữ liệu Glossary. Âm Hán Việt cho FR113 đọc qua cổng DictionarySource, không cài lại — thêm cạnh glossary/ → dict/ vào đồ thị phụ thuộc, không tạo chu trình
- (constraint) ERD cập nhật ba thực thể: GLOSSARY_ENTRY thêm trạng thái bản dịch và cho phép bản dịch rỗng; SEGMENT thêm xuất xứ; TM_UNIT thêm xuất xứ trong metadata (AD-6 đã cho phép vì mục TM là cặp văn bản cộng metadata, độc lập segment.id)
- (event) Reviewer Gate chạy inline. lint_spine.py 0 findings. Kiểm chu trình đồ thị phụ thuộc sau khi thêm cạnh glossary → dict: KHÔNG có chu trình
- (decision) Lens đối kháng rà lại AD-13 với cạnh mới: AD-13 cấm module ngoài ai/ import ai/, chiều ngược lại hợp lệ. Cạnh glossary → dict KHÔNG chạm AD-13 vì cả hai đều không phải ai/. Đồ thị vẫn một chiều
- (event) spine updated — AD-36 thêm mới; AD-4, AD-5, AD-18, AD-23, AD-31 sửa Rule tại chỗ; ERD ba thực thể thêm trường; đồ thị module thêm cạnh glossary → dict; Deferred thêm hai hàng cho mũi thăm dò font. AD-1..AD-35 giữ nguyên không đánh số lại. Phạm vi frontmatter 112 → 118 FR
- (event) Lượt cập nhật 2026-08-03. Đính chính: hàng bàn giao FR115-FR118 KHÔNG còn tồn, lượt 2026-08-02 23:26 đã đóng hết. Khoảng cách thật là spine 118 FR vs SPEC 121 FR — thiếu FR119, FR120, FR121. Ice chọn đường KÈM CẶP
- (decision) Ice CHỐT: cấu trúc đoạn lưu bằng CỜ KẾT ĐOẠN trên SEGMENT, tính lúc nhập cùng lượt với ranh giới câu. Loại phương án thực thể PARAGRAPH riêng (nặng: AD-3/AD-4/AD-5 đều phải mở rộng, gộp segment xuyên đoạn thành ca mới) và phương án suy ra lúc xuất (đi ngược AD-4: đổi quy tắc đoán đoạn sẽ đổi im lặng kết quả xuất của mọi Chương cũ, và cột phải không có văn bản gốc để đoán nên phải suy ngược qua segment — đúng chỗ hai giai đoạn cài lệch nhau)
- (question) FR120 rà xong: KHÔNG cần gì ở tầng kiến trúc, chỉ đọc trạng thái segment sẵn có mà AD-31 đã bao trọn
- (decision) Ice CHỐT cách chặn nhập ngược bản .docx một khối: ĐOÁN THEO HÌNH DẠNG BẢNG, không đóng dấu metadata. Lý do chọn: chạy được với mọi file dù ai tạo, không phụ thuộc metadata mà công cụ trung gian có thể bóc mất. Loại phương án đóng dấu (không phủ được file người khác tạo) và phương án quy ước tên file (tên đổi được nên là gợi ý chứ không phải rào chắn)
- (decision) TINH CHỈNH trong phương án Ice đã chọn, để bịt ca từ chối oan: dấu hiệu KHÔNG phải số hàng đơn thuần mà là MỘT HÀNG DUY NHẤT VÀ ô chứa NHIỀU HƠN MỘT ĐOẠN. Chương một câu ra một hàng một đoạn nên đi qua bình thường. Dùng được chính cờ kết đoạn vừa chốt nên không thêm cơ chế mới
- (assumption) Ca sót lại sau tinh chỉnh, ghi để không ai tưởng đã kín: Chương ngắn chỉ có MỘT đoạn nhưng nhiều câu, xuất một khối ra một hàng một đoạn — không phân biệt được với bản theo câu của cùng Chương đó. Hẹp hơn hẳn ca gốc và hậu quả nhẹ vì đúng là một đoạn thật, nhưng vẫn ghi đè nhiều segment bằng một khối. Chấp nhận có ý thức
