# Reviewer Gate — lượt cập nhật AD-44 (2026-08-05)

**Phạm vi rà:** vùng vừa sửa của `ARCHITECTURE-SPINE.md` — AD-44 (mới), AD-26 (sửa Rule tại chỗ), AD-17 (thêm ghi chú ranh giới), Consistency Conventions hàng *Tra cứu*, Cây nguồn `dict/`, bản đồ năng lực C3/C4, hai hàng Deferred mới.

**Chế độ chạy:** inline tuần tự — phiên này không tự gọi subagent. Đúng tiền lệ hai lượt gate trước của dự án (`.memlog.md`: *"Reviewer Gate chạy inline (session cấm subagent)"*). ⚠️ Giới hạn phải nói thẳng: một lens chạy inline **không có ngữ cảnh độc lập**, nên nó bắt được chỗ mâu thuẫn nội tại nhưng yếu hơn ở chỗ tác giả đã nói vòng qua. Ghi ra thay vì đánh dấu đạt.

---

## 0. lint_spine.py (deterministic)

**0 findings**, chạy hai lượt — trước khi vá và sau khi vá.

---

## 1. Lens phiên bản / đối chiếu thực tại

> *"Verify every committed decision was web-researched or reality-checked rather than asserted from training data."*

| # | Phát hiện | Mức | Xử lý |
|---|---|---|---|
| V1 | 🔴 **Ba chuỗi stem trong bảng của AD-44 ③ là ASSERTION, không phải phép đo.** `dictionaries→dictionari`, `studies→studi`, `happiest→happi` lấy từ hành vi kinh điển của Porter — **chưa chạy qua stemmer nào**. Số *hàng* (0/0/0/1) thì đo thật trên `dict-core.db`, nhưng nếu chuỗi stem sai thì bảng đo sai thứ. `tantivy-stemmers` phơi nhiều biến thể và Porter ≠ Porter2/English | **Cao** | ✅ **Đã vá.** Tách bảng thành *dữ kiện MẠNH* (16/16 dạng biến thể là đầu mục riêng — đo thật, và là thứ quyết định đứng lên) và *dữ kiện YẾU HƠN* (bảng stem — kèm cảnh báo tường minh rằng chuỗi chưa chạy qua stemmer thật, và ai mở lại câu hỏi thì phải đo trước khi chép tiếp) |
| V2 | AD-44 **không ghim crate nào** vào bảng Stack ⇒ không có mệnh đề phiên bản nào để kiểm. `tantivy-stemmers 0.4.0` chỉ xuất hiện ở hàng Deferred như một *điều kiện mở lại*, không như một cam kết | — | Không cần xử |
| V3 | Mọi con số định lượng của AD-44 truy được về một lượt `sqlite3` trên `dict-core.db` **194.998.272 byte** — nêu đích danh trong `.memlog.md` để tái lập | — | Không cần xử |

---

## 2. Lens đối kháng

> *"Construct two units one level down that each obey every AD to the letter yet still build incompatibly."*

Hai đơn vị dựng: **U-A** = Story 1.11b *(đường tra cứu tiếng Anh)*, **U-B** = Story 1.13 *(cổng `DictionarySource` + tầng gom nhiều tệp)*.

| # | Lỗ — cả hai đơn vị tuân thủ TỪNG CHỮ mà vẫn ghép không được | Mức | Xử lý |
|---|---|---|---|
| A1 | 🔴 **AD-44 ① nói vị từ điều phối là gì, không nói nó CHẠY Ở ĐÂU.** U-A đặt vị từ trong `lookup()` của từng tệp; U-B đặt ở tầng gom. Cả hai đọc đúng chữ. Hậu quả: mỗi tệp `.db` tự phân xử một câu hỏi thuộc về **cả lượt tra**, và hai tệp trả lời khác nhau ngay khi định nghĩa `is_han` của chúng lệch — mà AD-44 vừa nói định nghĩa đó nằm ở workspace KHÁC và không có cổng kiểm chéo | **Nghiêm trọng** | ✅ **Đã vá.** Thêm mệnh đề: vị từ chạy **đúng một lần mỗi lượt tra, TRÊN tầng gom**; adapter nhận nhánh **đã quyết** + bộ lọc `lang`, không tự quyết lại |
| A2 | 🔴 **Không có luật nào về việc *tệp nào được tra*.** U-A tra mọi tệp rồi lọc `lang` trong SQL; U-B dựng một bảng tra *"tệp → ngôn ngữ nó chứa"* để bỏ qua tệp không liên quan (hợp lý về hiệu năng, và không AD nào cấm). Bảng đó là **nguồn sự thật thứ hai cho một dữ kiện đã nằm trong dữ liệu**, và nó sai **im lặng** vào đúng ngày một lớp gỡ rời được thêm hoặc gỡ (FR112) | **Nghiêm trọng** | ✅ **Đã vá.** Thêm mệnh đề cấm: không tồn tại sổ đăng ký *"tệp nào chứa ngôn ngữ nào"*; mọi tệp đang gắn đều được tra, `lang` lọc trong SQL. Nêu đích danh AD-8/AD-33 làm tiền lệ cùng lớp lỗi |
| A3 | 🟡 **Truy vấn không phải Hán cũng không phải Latin** — rỗng, toàn chữ số, dấu câu, một hệ chữ thứ ba. Vị từ *"chứa Hán ⇒ zh, ngược lại ⇒ en"* đẩy hết sang en ⇒ 0 hàng. U-A đọc đó là *"không có kết quả"*; U-B đọc đó là *"không hỗ trợ"* theo khuôn ④ và trả một trạng thái khác. Hai hình dạng phản hồi cho cùng một đầu vào ⇒ Panel Lookup (1.17) hiện hai thứ khác nhau | **Vừa** | ✅ **Đã vá.** Khai vị từ là **nhị phân, không có nhánh thứ ba**; rỗng ở đường en là *"không có kết quả"* thật, và trạng thái không-hỗ-trợ **chỉ** tồn tại ở ca ④ |
| A4 | 🟡 **Hạ chữ thường theo locale.** `"I".to_lowercase()` độc lập locale, nhưng một phép fold theo locale *(ca Thổ Nhĩ Kỳ `I`/`ı`)* làm **cùng một truy vấn cho hai kết quả trên hai máy** cài ngôn ngữ OS khác nhau — một hồi quy không tái lập được trên máy người sửa | **Vừa** | ✅ **Đã vá.** Khai tường minh: phép hạ chữ thường không phụ thuộc locale |
| A5 | Kết quả trùng khi cả hai khoá cùng khớp *(184 nhóm chỉ khác chữ hoa)* | Thấp | Không cần vá — Rule ③ đã bắt buộc **một** truy vấn `IN (?1, ?2)`; `IN` trả mỗi hàng đúng một lần. Chỉ hình dạng `UNION ALL` mới sinh trùng, và hình dạng đó đã bị Rule ③ loại |
| A6 | `LookupMode::Exact` với truy vấn 1–2 ký tự tiếng Anh | Thấp | Không phải lỗ — bảng ② khai độ dài *"bất kỳ"* cho nhánh tra chính xác; ca không-hỗ-trợ ④ chỉ áp cho **chuỗi con** |

---

## 3. Rubric walker — good-spine checklist

| Tiêu chí | Phán quyết |
|---|---|
| Cố định đúng chỗ hai đơn vị dưới có thể chọn lệch nhau | ✅ **Đạt sau khi vá A1/A2/A3/A4.** Trước khi vá thì không đạt — bốn lỗ đều nằm ở chỗ AD nói *cái gì* mà không nói *ở đâu* / *ai* |
| Mỗi Rule cưỡng chế được và thật sự chặn được divergence nó khai | 🟡 **Đạt, một chỗ yếu đã ghi:** mệnh đề *"định nghĩa `is_han` là một"* không có cổng kiểm chéo hai workspace — giới hạn này có **từ trước** (chốt ở Story 1.9 AC4, ghi lại ở `deferred-work.md` sau lượt review Story 1.11). AD-44 **kế thừa** nó chứ không tạo ra nó, và nay nêu đích danh thay vì để ngầm |
| Không hàng Deferred nào để ngỏ chỗ hai đơn vị diverge | ✅ Đạt — hàng *stemming* để ngỏ một lần **mở lại**, không để ngỏ hành vi: AD-44 ③ ràng buộc trạng thái hôm nay. Hàng *cụm từ nhiều chữ* là câu hỏi của Auto-Lookup *(chọn gì để tra)*, không phải của đường tra cứu *(tra thế nào)* |
| Công nghệ có tên đã kiểm chứng | ✅ Đạt — AD-44 không ghim crate nào |
| Ratify chứ không mâu thuẫn mã đang có | ✅ Đạt — AD-44 không đòi sửa một dòng nào của Story 1.11 đã giao. Đường zh giữ nguyên ba nhánh và giữ nguyên bộ lọc `lang='zh'`; vị từ điều phối là một tầng **mới ở trên**, không phải một lần viết lại |
| Không AD mới nào làm yếu đi một AD đang có | ✅ Đạt — AD-19 (không hợp nhất) được **mở rộng** sang cặp zh/en; AD-10 (adapter theo tệp) được **siết**; AD-17 được **làm rõ ranh giới** mà không nới Rule |
| Mọi chiều mà altitude sở hữu đều được quyết / hoãn / thành câu hỏi mở | ✅ Đạt trong phạm vi lượt này. Vỏ vận hành không bị lượt cập nhật này chạm tới và đã được spine phủ từ trước |

---

## Phán quyết gate

✅ **ĐẠT sau khi vá.** 5 phát hiện phải xử — **2 nghiêm trọng** (A1, A2), **2 vừa** (A3, A4), **1 cao** (V1) — **tất cả đã vá tại chỗ trong lượt này**. `lint_spine.py` 0 findings cả trước lẫn sau. 2 phát hiện thấp (A5, A6) rà xong kết luận không phải lỗ.

**Còn nợ, không vá được ở tầng spine:** cổng kiểm chéo cho `is_han` giữa hai workspace *(kế thừa từ Story 1.9)*, và một lượt chạy stemmer thật để thay bảng stem trong AD-44 ③ bằng số đo *(chỉ cần khi ai đó muốn mở lại câu hỏi stemming)*.
